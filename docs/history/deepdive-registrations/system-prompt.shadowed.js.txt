/* 历史归档：此版本曾被 batch-31a-control-boundaries.js 覆盖，不再参与运行时注册。 */
window.DEEPDIVE['system-prompt'] = window.createDeepDive({
  title:'系统提示与角色提示：为一次应用调用建立行为契约', subtitle:'理解指令优先级、职责、边界、工具政策与输出契约，并区分文本指导和真正权限控制。',
  thesis:'系统提示在模型上下文中提供高优先级行为规则，但仍是概率模型读取的文本；它适合描述目标、流程和格式，<b>不能承担鉴权、秘密保护或不可绕过的安全边界</b>。',
  goals:['设计简洁系统契约','处理指令冲突','分离规则与外部数据','用执行层落实权限'],
  sections:[
    {title:'系统提示解决什么',badge:'直觉',lead:'为什么不把所有规则写在每条用户消息里？',body:'<p>它集中定义应用身份、任务范围、优先规则、工具使用和输出风格，使多轮调用保持一致，并让版本与评测可管理。</p>'},
    {title:'指令层级',kind:'eng',badge:'优先级',lead:'用户要求覆盖系统规则时该听谁？',body:'<p>宿主按消息角色提供优先级，模型应遵循高层规则并在冲突时拒绝或说明。外部文档和工具输出属于数据，不应获得与系统指令同等权限。</p>'},
    {title:'写可执行契约',badge:'写法',lead:'“做一个优秀助手”为何不够？',body:'<p>明确允许/禁止范围、何时用工具、缺信息时如何处理、输出 schema 和成功条件。优先写可观察行为与例子，减少重复、否定套娃和互相冲突的条款。</p>'},
    {title:'动态数据不要伪装成规则',kind:'eng',badge:'上下文',lead:'检索文档应该插在系统提示里吗？',body:'<p>用明确分隔和来源标记放在证据区，避免不可信内容获得高优先级。用户身份、当前时间等动态事实由可信宿主提供，并注明作用域。</p>'},
    {title:'提示不是权限系统',kind:'eng',badge:'安全',lead:'写“不要读取其他用户文件”能否阻止越权？',body:'<p>模型可能被注入或误解，真正的账户隔离、工具参数、金额上限和审批必须在执行代码中验证。系统提示只能提醒和引导。</p>'},
    {title:'版本化与评测',badge:'维护',lead:'改一句提示为何要像改代码一样回归？',body:'<p>措辞变化可能影响正常任务、拒答和工具调用。保存版本，运行高频、边界、冲突和攻击样本；用小流量发布并记录命中规则。</p><div class="dd-note warn"><b>越长不一定越可靠。</b>　冗余与冲突会稀释关键规则并增加成本。</div>'}
  ],
  chain:['定义应用职责与风险','写出优先级和可观察行为','将外部内容隔离为数据','模型按契约提出回答或动作','执行层重新鉴权','版本变更用任务和攻击集回归'],
  quiz:[{q:'系统提示主要是什么？',a:'高优先级的文本行为契约。'},{q:'检索文档是什么角色？',a:'不可信或有来源的数据，而非系统规则。'},{q:'权限应在哪里执行？',a:'模型无法绕过的宿主代码。'},{q:'为什么避免冗长？',a:'冲突、稀释、成本和位置影响。'},{q:'改提示后测什么？',a:'正常、边界、冲突、攻击和工具行为。'}],
  sources:[{title:'Instruction Hierarchy',url:'https://arxiv.org/abs/2404.13208',note:'训练模型优先遵循特权指令'},{title:'OpenAI Model Spec',url:'https://model-spec.openai.com/',note:'指令层级与行为规范'},{title:'Not what you’ve signed up for: Compromising Real-World LLM-Integrated Applications with Indirect Prompt Injection',url:'https://arxiv.org/abs/2302.12173',note:'外部数据中的注入'}]
});
