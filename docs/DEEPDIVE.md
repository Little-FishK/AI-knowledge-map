# AI 知识地图 · 「理解原理」深读页标准

> 详情页只给一段概览；**深读页**要让读者<b>彻底</b>弄懂一个概念——为什么必要、怎么运作、根本原理、相关数学。质量对标 [Google ML Crash Course](https://developers.google.com/machine-learning)。首个样板：神经网络（`data/deepdive/neural-network.js`）。

> **六问充分性状态（2026-07-28 归零重审）**：此前记录的 7/130 或 9/130 等任何 L3 通过数字已全部作废。新版门禁已经能高置信阻断「节尾证据集中」和「只追加合同答案」两类收束式修补，但它不能证明不存在所有隐蔽变体，也不能判断定义是否正确、推导是否合逻辑、解释是否充分。当前没有任何页面完成独立六问审计与 L4 复核，因此可正式宣称的 L3 通过数仍为 0/130。

## 当前覆盖进度（2026-07-28 归零重审）

- `data/graph.js` 的核心列表共有 **28 个节点**。
- 28 个核心节点均已注册对应深读页，覆盖率 **28/28（100%）**。
- 另有 **102 个非核心扩展页**，因此当前深读页总数为 **130**，与地图节点数完全一致，覆盖率 **130/130（100%）**。
- 第一批新增的 10 个非核心页为：Agent 循环、LLM 应用评测、护栏、缩放定律、越狱、部署形态、RLHF 与偏好对齐、上下文工程、中间迷失、奖励黑客。
- 第二批新增的 10 个非核心页为：模型选型与成本、损失函数、结构化输出、合成数据、代码执行与沙箱、提示缓存、梯度消失、自监督学习、Agent 记忆、梯度下降。
- 后续补齐的 79 页覆盖训练基础、应用/RAG、智能体工程、生成、安全与前沿架构；分组清单与验收见 [DEEPDIVE_COMPLETION_AUDIT.md](DEEPDIVE_COMPLETION_AUDIT.md)。
- 当前没有节点缺页、深读页指向不存在节点或入口脚本加载失败的情况；自动门禁会从 `index.html` 的真实脚本顺序重新执行并检查全部页面。
- 当前 L1 为 130/130 通过，L2 结构性缺口为 0。L3 教学一致性自动门禁的 130/130 结论已经撤销；合同版本 2 要求每个核心章节分别回答「是什么、解决什么、输入输出、怎样工作、怎样解释、适用边界」。新版工具会检查证据的最低语义、章节内分布和相对 Git 基线的纯追加行为，但仍不能替代内容与教学判断。全量重审从零开始，不引用旧数字。
- 旧版审计曾记录核心页与扩展页的人工分数；这些记录没有绑定当前内容哈希，不能视为新版 L4 人工标杆认证。新版认证状态以 `docs/deepdive-quality-reviews.json` 为准。
- 当前 L4 独立人工认证为 0/130；自动分数不等于事实、公式和教学有效性已经获得独立认证。

核心覆盖清单：LLM、Transformer、注意力机制、嵌入、上下文窗口、神经网络、预训练、微调、多模态、后训练、RAG、检索、提示工程、思维链、模型评测与基准、AI Agent、工具调用、MCP、代码生成、Agent 身份/权限/密钥管理、扩散模型、图像生成、幻觉、提示注入、AI 对齐、训练数据治理、推理模型、推理时计算与验证器。

## 一、机制（怎么接进地图）

- 页面可以是一概念一文件，也可以由有明确清单的共享页面包注册；两者都必须把内容注册到全局：
  ```js
  window.DEEPDIVE = window.DEEPDIVE || {};
  window.DEEPDIVE["<node-id>"] = { title, subtitle, aliases, meta, thesis, html };
  ```
- 在 `index.html` 的 `data/software.js` 之后、`app.js` 之前加载对应文件或页面包。门禁会按入口中的真实顺序重新执行，不能只把文件放进目录而不接入页面。
- 详情页（`app.js` 的 `openDetail`）检测到 `window.DEEPDIVE[id]` 存在，就在**大区标签右侧**渲染「📖 理解原理」按钮；点击调用 `openDeepDive(id)`，打开 `#deepdive` 全屏阅读视图（返回/关闭/Esc 均回到详情）。
- 用 `window.DEEPDIVE` 而非 fetch，同样是为了 `file://` 双击可用。

## 二、写作规约（源于对 Codex 初稿 5 个错误的复盘）

Codex 写第一版时自述犯了 5 个错，这 5 条正是本标准的由来——**写每一页都要主动防这 5 个坑**：

1. **不以「知识点覆盖率」为目标。** 「出现过」≠「读者懂了」。先问「读者读完能自己回答什么」，再决定写什么；不为凑齐清单而堆内容。
2. **每一节回答由前文引出的唯一问题。** 在小节顶部用 `.dd-lead` 把这个问题<b>显式写出来</b>（「上一节留下的问题 / 本节回答」）。一节讲两件事，就拆开或删掉多余的。
3. **严格维护概念依赖顺序。** 用到的词必须已在前文建立。像「计算图」「动态规划」「CNN」「Transformer」这种，读者没准备好时绝不提前抛出——它们只进末尾的「延伸学习」表。
4. **分清三个层级，并用徽标标注、用过渡连接：**
   - **原理**（为什么能表示非线性）→ `.dd-badge.intuition`「直觉」
   - **数学**（梯度、链式法则怎么算）→ `.dd-badge.math`「数学」
   - **工程**（选什么网络、怎么调）→ `.dd-badge.eng`「工程」

   三者都要，但必须分层；不能把工程细节混进原理推导里。
5. **做「初学者困惑检查」，不只做结构检查。** 文件结构/标题/表格齐全 ≠ 课程在认知上连续。在最容易卡住的地方放 `.dd-note.warn`「你可能会困惑」，当场点破并解决。

一句话概括这 5 条：**认知连续 > 技术完整**。读者的困惑走到哪，内容就接到哪。

## 三、可用的内容组件（CSS 已在 style.css 定义）

| 类 | 用途 |
|---|---|
| `.dd-lead` | 小节顶部的「本节回答什么」引导句（规约 ②） |
| `.dd-badge.intuition/.math/.eng` | 章节标题旁的层级徽标（规约 ④） |
| `.dd-note.intuition/.math/.eng/.warn/.key` | 提示框：直觉/数学/工程/困惑/要点。`.warn` 用于初学者困惑（规约 ⑤） |
| `.dd-formula` + `.dd-formula-note` | 教材式展示公式 + 其下符号和含义说明；非平凡公式使用 MathML |
| `.dd-fig` + `<figcaption>` + `.svg-t`/`.svg-tn` | 内联 SVG 原创制图 + 图注（勿复制外部图片） |
| `.dd-table` / `.dd-steps` / `.dd-chain` / `.dd-quiz` / `.dd-answers` | 表格 / 步骤 / 因果链 / 自测题 / 折叠答案 |

## 四、结构模板（神经网络页即按此写）

概览（title/subtitle/aliases/meta/thesis）→「你应该理解什么」清单 →（可选）贯穿全页的运行示例 → 正文各节（每节一问，标注层级）→ 把整条因果链串一遍 → 常见误解 → 自测题+答案 → 概念依赖与延伸 → 来源与改编说明。

### 4.1 定义清楚，但不要写成合同腔

六问要求每个核心章节都能让读者明确找到概念界定，不要求每节都以「`X 是 Y`」开头。可根据语境交替使用「可以理解为」「指的是」「描述了」「核心作用是」「所做的是」「关注的是」「记录的是」「比较的是」等表达，也可以先提出具体困境，再自然给出界定。

- 相邻核心章节不应连续套用同一种定义模板；
- 句式变化不能省略概念的类别、作用对象或关键区别；
- 不为追求「柔和」而使用「差不多、某种东西、大概就是」等含混措辞；
- 六问合同记录语义证据，不规定正文必须照抄合同字段或固定句型。

### 4.2 六问是正文覆盖审计，不是正文可见模板

> 别再给这件事加机械规则：越加规则文章越千篇一律。只钉两个极点，中间全自由。

- **优秀案例（学）= `data/deepdive/image-generation.js`**：六问分散在本节**自己的叙述**里——定义常是开头句、输入输出是中间句、机制是流程句、边界是结尾提示框，每节按自己的内容长成自己的样子。它是「怎么分布六问」的参照，**不是照抄版式**（别让它也变成模板）。
- **禁止项（禁）= 「追加收束段」**：把六问打包成一段、贴在某段结尾或塞成一个独立 `<p>`，让这一段独自承担全部六问、而本节原有叙述句不承载合同证据。
- **自动阻断的高置信信号**：至少五项证据挤在章节后半部的短窗口，报 `integration.tail-evidence-cluster`；与 Git 基线相比，旧正文基本不动而新增尾部承载至少四项合同证据，报 `integration.append-only-contract-fix`。检查基于可见文本，换成多个段落、列表或提示框不能绕过。
- **自动提示而非阻断**：证据异常紧凑但位置或顺序不足以高置信判错，报 `editorial.contract-like-evidence-cluster`，交由人工查看。
- **定义句式重复只作编辑提示**：报 `editorial.repeated-definition-template`，不阻断发布。门禁不能逼作者逐节机械换同义词；真正要判断的是阅读体验是否形成合同腔。
- 六问审计逐步迁移到 `docs/deepdive-audits/<id>.json`，用正文哈希绑定。写作模型不得同时生成或修改审计；审计发现缺口后，应重写原有教学过程，而不是追加六问答案。

## 五、版权

正文、图表、手算**一律原创**；参考 Google ML Crash Course 等（多为 CC BY 4.0）只做<b>结构与要点参考</b>，重新组织、扩写、自绘 SVG，**不复制其文字、图片或交互组件**。来源与访问日期记进页尾 `.dd-src`。数学务必自己逐步核验（神经网络页的手算已端到端验算过）。展示公式与可执行表达式必须分层，完整要求见 [DEEPDIVE_MATH_NOTATION.md](DEEPDIVE_MATH_NOTATION.md)。

## 六、质量门禁

质量状态采用四层模型，完整规范见 [DEEPDIVE_QUALITY_GATE.md](DEEPDIVE_QUALITY_GATE.md)：

- **L1 可发布**：自动检查全节点覆盖、入口注册、最低学习闭环、独立来源与日期。
- **L2 结构候选**：只说明具备进入自动深度审计的教学部件，不授予分数或「标杆」状态。
- **L3 教学一致性**：结构代理分只作诊断；自动阻断可确定举证的渲染污染、结构化依赖、公式、评测、单位，以及高置信的节尾证据集中和纯追加合同修补。注意：它不能穷尽收束写法，也不能判断内容正确性和教学有效性。
- **L4 人工标杆认证**：关键项全部通过、六个维度均达最低分、总分至少 88，并用内容哈希绑定人工审校证据。

每次修改后运行 `node tools/check-deepdive-quality.js`。核心标杆发布还应运行 `node tools/check-deepdive-quality.js --l4-certified <id>`。错误分类、成熟规范来源和规则准入合同见 [DEEPDIVE_GATE_ERROR_CATALOG.md](DEEPDIVE_GATE_ERROR_CATALOG.md)。自动门禁不能替代事实、公式、认知连续与示例有效性的 L4 人工审校。

## 七、进入教学一致性与人工标杆审校的标准

发布线 88 分只说明页面可以使用，不代表已经达到本项目的最高教学标准。后续全量升级以神经网络页为标杆，但「同等水平」指同等的学习闭环和审校强度，**不要求所有概念机械复制相同字数、公式数量或章节名称**。

一页进入标杆审校前，应具备：

1. **明确的问题链**：从「为什么需要它」开始，每一节只回答由前文自然引出的一个问题。
2. **完整的机制链**：输入、内部状态或变换、输出、反馈/更新及适用边界能够首尾相接。
3. **概念适配的形式化解释**：数学概念给推导，系统概念给状态机/时序或权限边界，治理概念给责任链和决策条件；不为显得技术化而硬塞公式。
4. **至少一个可复现的贯穿示例**：可以是手算、代码、请求—响应、故障推演、威胁场景或决策案例，并写出可观察结果。
5. **专属教学制品**：原创 SVG、经过核验的数值表、时序表、决策矩阵或逐步演算；不能只复用通用装饰图。
6. **主动消歧**：在读者最可能卡住的地方解释相邻概念差异，并另设常见误解或失败模式。
7. **实践与验证**：说明怎样选型、怎样知道它有效、哪些指标会骗人、失败后先检查什么。
8. **学习闭环**：因果链、至少 4 道非背诵型自测、完整答案、前置概念和延伸路线。
9. **来源可维护**：至少 3 个优先级清楚的一手或权威来源；快速变化内容标明访问日期和版本边界。
10. **逐页人工复核**：准确性、认知连续、原理深度、示例消歧、练习和来源六项均有文字依据；禁止仅凭字数或组件数给高分。

运行 `node tools/audit-deepdive-gold.js` 可查看页面进入标杆审校前的**结构性缺口**。该脚本只负责找线索：它不能判断公式是否正确、例子是否真的解释机制，也不能代替浏览器阅读和人工评分。
