# 第 1 层地图连线更新 · 2026-09-10

> 方向修正：用户随后指出弯曲方向相反。当前版本以“弧线向外鼓起、凹面朝中心”为准，以下初版向内偏移说明仅为历史记录。

用户授权直接上线，要求核心节点间直线、外围节点连线向内弯曲，且可随时回退。

## 范围

- 第 1 层：没有选中节点的核心/全部地图。核心节点间始终直线；非核心连线按照两端到完整地图中心的平均相对距离，平方递增加深二次贝塞尔弯度。
- 参考中心是完整地图坐标包围盒中心，不随筛选、平移或缩放改变。弯曲控制点取连线中垂线朝中心的一侧，并限制偏移，防止越过中心。恰好穿过中心的连线没有明确内侧，保持直线。
- 第 2 层：选中任何节点后，所有连线恢复原有直线；基础页内容和局部节点布局不变。关闭详情恢复第 1 层规则。
- 第 3 层理解页、官方学习顺序、关系类型颜色、有向/无向语义、六色节点纹理均不变。

## 发布与验证

- 发布提交：`7356c6a68760e3cfb5fdae6ba41376f6e80f1d8b`，分支 `gh-pages`。
- 上一版本：`20a13ecd0984dcda8328c1e70c44b08aac6ea373`。
- MCP 构建（manual-review=hold）：`.tmp/site-releases/production-1789055859154-4e5db0f3`。
- 发布变化仅为 `assets/app/graph-view.js`、入口脚本缓存版本及发布清单；没有数据或理解页变动。
- 本地测试：桌面 1440px、手机 390px，核心直线、358 条外围曲线实际控制点朝内、静止节点、神经网络/监督学习/大语言模型选择及返回、官方路径恢复均通过。
- 既有节点纹理浏览器测试和地图路由回归通过；精确 Git 归档 `.tmp/radial-edges-exact.zip` 展开后通过生产清单验证。
- 2026-09-10 16:04:33 UTC 正式网站精确清单检查通过；随后线上桌面/手机连线几何、第2层隔离与恢复测试全部通过。

## 回滚

- 完整上一版本：`.tmp/before-radial-edges-exact.zip`。
- 上一版本本地目录：`.tmp/site-release-backups/2026-09-10T16-02-36-921Z/`。
- 修改前源文件：`.tmp/graph-view-before-radial-edges.js`、`.tmp/index-before-radial-edges.html`。

按既有发布流程重新发布旧包到 `gh-pages`；不要对主工作区执行 reset，也不重新生成正文。Git 导出必须使用 `-c core.autocrlf=false archive` 保持清单字节一致。

## 方向修正发布

- 提交：`459d77469f8c37028d7926a2f5162228e0de2b4d`。只将控制点偏移符号反转，保留全部弯度、阈值、核心直线与第 2 层隔离规则。
- MCP 产物：`.tmp/site-releases/production-1789057006030-7f3ef672`。
- 修正前完整备份：`.tmp/before-edge-direction-fix.zip`；本地自动备份 `.tmp/site-release-backups/2026-09-10T16-18-00-811Z/`。
- 本版精确归档 `.tmp/edge-direction-fixed-exact.zip` 已通过生产清单验证。桌面/手机测试确认 358 条曲线控制点反向、核心直线、节点静止及选中/退出恢复保持正确。
