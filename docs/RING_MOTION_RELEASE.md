# 节点旋转边框与悬停强调 · 2026-09-10

用户授权正式上线并要求可回退。

## 表现与实现

- 六色边框常态 48 秒/圈；鼠标进入节点图标（不含文字）后 3 秒/圈，外圈平滑放大至 118%。离开恢复。
- 白色强调环内半径为图标直径的 38%，与原彩色圆面边缘重合；白环厚度 3%。旋转圈内半径原为 40%，悬停放大后为 47.2%，因此与白环外沿 41% 之间有间隔。
- 运行时从既有六色纹理提取固定圆面和旋转环，在独立、不可点击的画布绘制；未改原始 PNG。节点坐标、文字、关系边不参与旋转。
- 30fps 上限、设备像素比最高 1.5、跳过隐藏/视口外节点、离开地图暂停绘制。遵循系统减少动态设置：停止旋转，保留悬停白环与大小强调。
- 全部纹理加载成功前使用静态原样；加载失败保持静态。官方学习顺序维持原有数字标记，不叠加动画。

## 发布与回滚

- 发布提交：`9861e0dcc67a19d85d5956729c8d50a597477f61`。
- 上一版：`459d77469f8c37028d7926a2f5162228e0de2b4d`。
- MCP 产物：`.tmp/site-releases/production-1789058008261-8a260dea`（manual-review=hold）。
- 完整回滚包：`.tmp/before-ring-motion-exact.zip`。
- 自动备份：`.tmp/site-release-backups/2026-09-10T16-36-42-697Z/`。
- 当前精确包：`.tmp/ring-motion-exact.zip`，展开后通过生产清单校验。

回滚按既有部署流程发布完整旧包；不得 reset 主工作区或重新生成正文。Git 导出使用 `-c core.autocrlf=false archive` 保持哈希正确。

## 验证

本地浏览器测试通过：圆面像素固定、节点无漂移、外圈像素随时间改变、悬停白边和尺寸扩展、移出恢复、点击打开基础页、减少动态设置、六色素材与官方顺序恢复。桌面/手机两层地图连线隔离与恢复测试通过。发布仅修改地图视图脚本、入口缓存版本和发布清单。

2026-09-10 16:39:11 UTC 公网精确发布清单验证通过，随后线上动画与悬停交互（普通和减少动态设置）测试通过。

## 官方推荐视图同步

用户随后要求官方推荐采用同样的边框和连线，保留序号、主序号金底黑字和子序号黑底金字。本次共保留 130 个官方序号，沿用六领域光环、48 秒慢转/3 秒悬停转动、118% 外圈和白色强调；圆面与数字不转动。推荐模式启用同样的核心直线/外围曲线规则。

- 提交：`3a008bf057f1a5739664de612139fd37e359d615`。
- MCP 产物：`.tmp/site-releases/production-1789058756260-b72d51b5`。
- 上一版完整回滚包：`.tmp/before-official-rings-exact.zip`。
- 自动备份：`.tmp/site-release-backups/2026-09-10T16-48-12-122Z/`。
- 精确归档：`.tmp/official-rings-exact.zip`，已校验。
- 本地验证：官方数字与配色、静止圆面、慢转、悬停放大与白边、点击、退出恢复及减少动态设置通过；桌面/手机连线与纹理回归通过。

此变更取代上文“官方学习顺序不叠加动画”的旧行为，不改学习顺序数据或理解页内容。
