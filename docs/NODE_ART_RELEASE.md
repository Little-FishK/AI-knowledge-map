# 六色节点上线记录 · 2026-09-10

用户确认六色圆形节点与紧贴、略加宽的光环后，授权上线。正式 UI 的修改范围为 `assets/app/graph-view.js` 和 `assets/node-art/` 六张纹理。原始图谱、正文、账号和其他界面保持不变；未引入持续漂浮。既有的点击定位/局部布局过渡仍保留。

领域配色：基础蓝、编程橙、应用绿、内容生成紫、前沿银灰、安全红。领域筛选和详情标签同步采用相同配色。普通关系改为直线；原有有向/无向关系和类型颜色保留。推荐学习顺序继续使用原有数字标记。

## 发布与验证

- 发布分支：`gh-pages`，版本 `7ab5a7bb4ffb1715156a480b57cb4f426d074375`。
- 上一版本：`9a17a2d1f7be7b42ab4a7ea05b5c716a39acbc53`。
- MCP 控制器构建输出：`.tmp/site-releases/production-1789053682449-15ebacd4`，随后晋升为 `site-release/`。
- 仅七个界面文件及发布清单变化，没有新增、删除或修改正式 HTML 页面；无需发送页面索引更新通知。
- 验证通过：地图路由回归；六色纹理加载；130 节点无持续位移；直线关系；官方学习顺序切换与恢复；桌面和手机；发布包与 Git 归档完整性。
- 2026-09-10 15:30:44 UTC 公网清单、页面、Sitemap、robots 与验证包一致。

## 恢复

上一线上版本的完整且已验证发布包：`.tmp/node-art-before-live-exact.zip`；已展开并验证的目录：`.tmp/node-art-previous-exact/`。

本地上一次 `site-release/` 由晋升工具自动保存于 `.tmp/site-release-backups/2026-09-10T15-26-18-048Z/`。

恢复时按 `PHASE8_RELEASE_RUNBOOK.md` 的既有流程，将上述旧包作为精确产物发布到 `gh-pages`，不重新生成正文，不对当前主工作区执行 Git reset。若从 Git 重新导出产物，使用 `git -c core.autocrlf=false archive`，避免 Windows 换行转换破坏清单哈希。

主工作区原本已有的未提交改动保持原样，本次通过隔离发布副本上线，没有提交无关源文件。

## 光环加宽与间隔微调

用户实测后要求增加光环宽度和间隔。保留六色、圆形和静止状态；纹理合成的节点内缩由 8% 改为 12%，四向光环扩展由 0.5px 改为 1px。更新脚本及纹理缓存版本，避免浏览器沿用旧图。

- 发布提交：`20a13ecd0984dcda8328c1e70c44b08aac6ea373`。
- 精确产物：`.tmp/site-releases/production-1789054806783-38b4d609`。
- 上一版完整回滚包：`.tmp/node-art-before-wider-rings.zip`（`7ab5a7b`）。
- 自动备份：`.tmp/site-release-backups/2026-09-10T15-43-25-456Z/`。
- 本版 Git 归档 `.tmp/node-art-wider-rings-exact.zip` 已展开并通过发布清单校验；本地桌面和手机浏览器测试通过。
- 2026-09-10 15:45:37 UTC 公网精确清单检查通过；随后正式网站六色纹理、130 个静止节点、直线边、学习顺序切换恢复及桌面/手机浏览器测试全部通过。
