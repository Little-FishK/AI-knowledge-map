# 监督学习英文样板：执行与验收

更新：2026-09-07。13/13 任务已收齐，完成两轮受限返修，独立全页语义复验通过；图表预览排版已修复并回归。当前 awaiting-resource-browser-human-review，尚未发布。

## 已冻结的范围

- 页面：supervised-learning；中文由维护者现时确认，机器审核状态仍为 audit-queued。
- 快照：`sha256:09e777f03a9f224ad6879ed351ca9d29494f017fb89144a58463e07a4772bc41`。
- 完整内容摘要：`sha256:c4d997c9304c18706ca48a7d7d8fe734d37cdb37e0261e6f03a21b3700645f33`。
- 13 个任务、376 个单元：正文 371 个，页头 5 个。旧摘要的 371 只统计 HTML，并非遗漏页头。
- 每个任务由 MCP 完整分页返回，保留固定提示词、上下文、术语和输出合同；准备前后均由 MCP 确认快照 prepared。
- 本地副本保存在 Git 忽略的 `.tmp/website-preview/translation/supervised-learning/<snapshotHash>/`，仅供准备，不是生产翻译账本或批准记录。

| 任务 | 单元数 |
|---|---:|
| 页头 | 5 |
| 章节外文本、目录及参考来源 | 33 |
| 1 什么是监督学习 | 35 |
| 2 分类与回归 | 28 |
| 3 学习在学什么 | 71 |
| 4 泛化 | 46 |
| 5 标注瓶颈 | 22 |
| 6 四种学习范式 | 28 |
| 7 大模型时代 | 23 |
| 8 因果链 | 20 |
| 9 常见误解 | 24 |
| 10 自测 | 25 |
| 11 概念依赖与延伸 | 16 |

## 已授权的实际调用计划

用户已选 DeepSeek API V4 Pro。沿用项目现有英文翻译控制器，不另行生成中文内容。

- planId：`sha256:793c89b38f24ae6cc92047db5879fad3df337f739b7209e8532cdce05b5d8e17`。
- model：deepseek-v4-pro；账号标签 deepseek-main；reasoningEffort=high。
- contextWindow=1000000；每任务 maxOutputTokens=32768；超时 600000 毫秒。
- maxAttempts=1：当前计划不自动或重复尝试失败章；不确定结果按控制器流程停止。
- 采用峰时/缓存未命中输入 1.32 美元/百万 tokens、输出 3.96 美元/百万 tokens。2026-09-07 核对 [DeepSeek 官方价格页](https://api-docs.deepseek.com/quick_start/pricing/)的近期检索内容；直接页面抓取超时，未采用同一页面旧缓存中的优惠价。实际执行跨期时应重新核验。
- 控制器离线保守预留 3.0770968800000005 美元；提议计划预算 5 美元。估算按输入字节上界与每章最大输出计算，不是账单或提供方硬限额。
- 用户在本任务中回复“是”，明确同意本页 5 美元预算；启动环境绑定此精确计划，逐任务调用 MCP，不自动重试或发布。
- 通过现有启动器在子进程内加载加密凭据；未将密钥展示或写入命令参数。首个页头请求已成功返回，验证当前凭据可调用 API；未查询账号余额。

## 三类评价分开记录

| 评价 | 当前证据 | 后续动作 |
|---|---|---|
| 内容准确性与翻译忠实性 | 中文维护者确认；英文未生成 | 独立核对术语、否定、条件、分类/回归、训练/验证/测试等区别及整页逻辑 |
| 讲解有效性 | 初学者测试方案已存在，没有真实结果 | 目标读者完成判断任务并解释泛化与数据划分；保存观察，不用翻译门禁代替学习效果 |
| 记录完整性 | 中文确认回执、源快照、13 个任务和离线计划齐全 | 每章响应和用量由控制器保存；生成者与审查者分离；保留缺陷、返修与最终验收证据 |

## 英文页面验收清单

- 2 幅内嵌 SVG：图中文字和 aria-label 全部英文化；检查文字是否超出图框、连线是否遮挡文字，不能只看程序覆盖率。
- 7 个 table：标题、表头、单元格、长词及手机滚动可读；不删除列或缩写掉原意。
- 31 个代码保护片段：原样保留。数学解释仍由语义审查核对，不把代码字节相同当成公式含义正确。
- 1 个 details 自测区域：问题、选项、答案与解释完整对应；实际展开和收起可用。
- 6 个外部参考链接：目标保持，链接文字可翻译。当前仅确认保留，不宣称目标可达或逐条支持正文。
- 页头、目录、章节外文本、无障碍标签均计入完整性；不只检查正文段落。
- 英文独立页无脚本可读；桌面及手机检查正文、SVG、表格、导航和键盘焦点。
- 中英文往返定位同一概念，返回地图选择监督学习；不把未翻译的下一概念冒充英文页面。
- 独立语义审核、资源/浏览器验收及人工最终确认完成前，保持未审预览和 publicationAllowed=false。

## 本次执行结果

- 用户已确认 5 美元预算。通过现有启动器加载加密凭据，并经页锁定 MCP 执行精确计划；13 次请求全部一次成功，无重试。
- 全部结果为 received-unreviewed。控制器最大输出预留累计 3.0770968800000005 美元；按响应 usage 和峰时配置价格计算的费用合计 0.55535172 美元，未核对提供方账单。
- quality reviewId：`sha256:75f2ff3a03cb48ddb90237d6e16d9b95960000ea83ee37b4cd48b4f53362144f`。
- 首轮机械检查仅发现 `section-4/u162` 数字差异：原文“100 分”被译为“100%”。通过专用 translation-repair MCP 读取净化缺陷并只修改该英文单元，保留中文和原提供方响应。
- 返修后 revision：`sha256:17eb52bc831d6b4c862859dfe2e0df43f4692bf65a86cd287923d6aa9ee78acf1`；repairCount=1；defects=[]。来源、覆盖、结构、数字符号四项通过；术语、语义、整页表达等待独立审查，资源与正式浏览器门禁仍待完成。
- 两个待审提示：第六章生成者提出的源文疑点；第七章 `section-7/u270` 的“预训练”术语映射。未把提示当成已确认内容错误，也未擅自清除。
- MCP 导出待审候选 artifactHash：`sha256:18bb9704a540643b22d657a917db34661e231d6a19940e25ee79977378c1c8c8`。本地预览为 `http://127.0.0.1:4991/preview/en/concepts/supervised-learning/`，可切到同端口中文原文；标注 Review draft，noindex，无正式英文发布记录。
- `tests/app/english-pilot-preview.test.js` 已通过真实 MCP 候选的桌面/手机无脚本阅读、章节/SVG/表格/自测数量、正文完整嵌入、字号、全页溢出、地图链接/返回、私有快照不可访问检查。已查看两张首屏截图；不把这些基础检查等同于整页 SVG 裁切检查或完整发布浏览器验收。
- 本轮返修者身份为 `codex-supervised-learning-repair-20260907`，不能兼任后续独立语义审查。尚未提交任何语义审查证据。

## 图表细查与展示层修复

- 针对候选 `18bb9704…` 的桌面/手机图表截图发现真实问题：第二幅 SVG 的英文说明重叠，末尾被裁切；手机 SVG 文字过小；表格英文单词过度断行。
- 仅修改本地预览适配器和 CSS：SVG 的三个说明原文以响应式图例展示，原 SVG 字节仍保留；图表最小宽度保证字号，手机通过可聚焦的局部滚动区域查看，并提供提示。七张表使用正常列宽和局部滚动，不改变任何译文或表格内容。
- 预览正文加入了明确的 presentation-only 图例及滚动容器，已经不再声称渲染 HTML 与候选 HTML 完全相同；测试去除展示附加结构后逐字比较候选正文。控制器 candidate、revision 与机器审核状态未被这些排版修改改变。
- 增强浏览器测试已通过：可见 SVG 标签没有重叠/视口裁切、渲染高度至少 12 像素，图例三组标签齐全；所有七张表在手机局部滚动；图表和表格可用键盘滚动；页面无整体横向溢出；31 个 code 保护片段与中文原文逐字一致。
- 已查看改前/改后图表截图及各表格截图；中文预览回归通过。这里验证的是预览适配层，不等于生产 SPA、快速语言切换、回退和正式资源接受记录全部通过。后续译文 revision 变化后须重新导出并回归。
- 用户已明确允许启动独立语义审查子代理。该角色不参与返修；初审和返修后复验已分别在新的页锁定 MCP 会话中完成，正式结果见下一节。

## 独立语义审查与复验结果

- 初审完整覆盖 376 单元，提交 45 个有缺陷单元的定位证据。问题包括跨标签空格、句法拼接、施受关系、信号限定、模型表现的可靠性对象，以及获取方式与数量的区别。
- 主代理只经 translation-repair MCP 取得净化缺陷，第二轮修改这 45 个限定英文单元。未直接读取审查者私有证据或改中文，未调用新的付费 API。repairCount=2。
- 独立审查者在新 MCP 会话以 verification 调度完整重读新版本，重新提供 376 个单元的证据并成功提交一次；findings=[]。两个原始告警（RLHF 语境及 pre-trained 词形）经独立审查判断可接受。
- 当前 revision：`sha256:d0864383cd7f306ecb65b9db8f1e8fcbe93cba6b93d94c20250bede93b16c27e`。
- 当前 artifactHash：`sha256:5d05cac8c75165b1435c932fa7d0f3324081a430abff2c75c5930649a0a42bdd`；reviewId 保持不变。
- 控制器状态 awaiting-resource-browser-human-review；门禁 1–5、7、8 通过，6 为 pending-resource-review，9 为 pending-stage9-browser-test，publicationAllowed=false。没有把本地排版测试伪装成控制器的人类最终验收。
- 已通过 MCP 刷新预览候选和门禁依据，再运行英文预览回归；当前页面提示 final acceptance is pending。
- 剩余：正式展示链路的资源与浏览器验收（包括语言切换、回退、快速切换等），随后才能提交精确版本的人工最终验收。真实初学者学习效果测试仍未执行；翻译忠实性通过不等于这一教学评价已完成。

## 可重复准备与回归命令

```text
node tools/readiness/prepare-translation-pilot.js supervised-learning sha256:09e777f03a9f224ad6879ed351ca9d29494f017fb89144a58463e07a4772bc41
```

该命令只通过页锁定 MCP 获取生产材料，在本地预览目录写入副本，不调用 API、不提交审核。快照过期会拒绝完成准备；不得直接读取生产文件补齐材料。
