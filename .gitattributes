# Graph shard digests and the generated runtime artifact are byte-exact.
data/graph.js text eol=lf
data/graph-shadow/**/*.json text eol=lf

# Release manifest hashes require the exact checked-in artifact bytes on every OS.
site-release/** -text
