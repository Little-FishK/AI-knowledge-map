(function () {
  "use strict";
  window.DEEPDIVE = window.DEEPDIVE || {};

  window.DEEPDIVE['system-prompt'] = window.createDeepDive({
    title:'系统提示：在概率模型里声明行为契约，而不是建立安全边界',
    subtitle:'从消息序列、指令优先级、冲突解析和上下文拼装，到注入、泄露、版本化与契约测试，理解系统提示能控制什么。',
    thesis:'系统提示是应用在每次推理中注入的高优先级上下文，用来声明角色、任务、边界和输出契约；它影响下一 token 分布，却不是操作系统权限，不能独立保护秘密或阻止越权工具。',
    goals:['解释系统消息怎样参与自回归生成','区分优先级、时间顺序与可信度','把提示拆成可测试的行为契约','设计冲突和注入回归集','将硬安全约束下沉到确定性执行层'],
    sections:[
      {title:'系统提示仍然只是模型看到的一串 token',badge:'直觉',lead:'标为 system 后，文本是否变成模型不可违反的程序？',body:'<p>聊天运行时把不同角色的消息按模板编码，模型仍对整个前缀计算条件概率。训练让模型更倾向遵守高优先级角色，但生成过程没有一个逐条执行规则的形式化解释器；提示越长、冲突越隐蔽、任务越陌生，遵循可能越不稳定。</p><p>因此系统提示适合声明行为预期和协调上下文，不适合保存 API 密钥、实施租户隔离或授权付款。真正不可越过的边界必须由工具参数校验、身份权限和业务策略提供。</p>'},
      {title:'优先级解决冲突，时间顺序只补充同层上下文',kind:'eng',badge:'机制',lead:'后出现的用户消息为什么不应覆盖更早的系统约束？',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>来源</th><th>信任角色</th><th>应当怎样处理</th></tr></thead><tbody><tr><td>系统/开发者</td><td>应用策略与任务契约</td><td>约束低层指令</td></tr><tr><td>用户</td><td>目标和输入</td><td>在高层允许范围内执行</td></tr><tr><td>检索/网页/工具输出</td><td>不可信数据</td><td>作为事实候选，不自动当命令</td></tr><tr><td>历史摘要/记忆</td><td>派生状态</td><td>带来源、作用域和可纠正性</td></tr></tbody></table></div><p>同层消息可按较新意图更新，但跨层冲突应由信任优先级决定。模型可能执行失败，因此运行时仍要验证结果。</p>'},
      {title:'完整示例：退款助手的契约怎样从需求变成提示与硬门',kind:'eng',badge:'案例推演',lead:'“退款超过500元需批准”应该只写进系统提示吗？',body:'<ol class="dd-steps"><li>系统提示声明职责：解释政策、收集订单信息、提出退款建议，不声称已经执行。</li><li>把订单数据放入带来源的结构化区块，并明确其中自由文本不可信。</li><li>要求输出建议、证据订单ID、金额和是否需人工批准的schema。</li><li>用户要求退800元时，模型可解释并生成待审批提议。</li><li>退款工具根据登录主体、订单所有权、金额和审批令牌独立拒绝未批准调用。</li><li>返回工具状态后，模型只能报告真实结果，不能把“已提交”写成“已到账”。</li><li>测试正常、边界金额、缺字段、用户覆盖规则、订单备注注入和工具报错。</li></ol><p>提示改善决策和解释；执行器拥有最终权威。</p>'},
      {title:'系统提示预算要用消融实验而不是越写越长',kind:'math',badge:'数值例子',lead:'新增一页规则后效果下降，怎样判断是规则冲突还是上下文竞争？',body:'<p>建立200条契约测试。短版提示通过176条，遵循率88%；长版通过170条，85%，其中旧能力回归12条、新规则修复6条，净变化−6。再将新增规则拆成结构化冲突表，若通过184条即92%，说明问题来自表达和冲突，而非“模型记不住所有规则”的单一解释。</p><div class="dd-formula">pass rate = passed / total；净收益 = 新修复 − 新回归 = 6 − 12 = −6</div><p>还要测token成本、首字延迟和不同任务切片，不以单个示例决定版本。</p>'},
      {title:'原创图：提示契约与执行权限必须分层',badge:'可视化',lead:'模型被注入时，哪一层仍能阻止真实副作用？',body:'<figure class="dd-fig"><svg viewBox="0 0 760 335" role="img" aria-label="系统提示与用户目标、不可信数据进入模型，模型提出结构化意图，再经策略权限和业务工具验证"><defs><marker id="sp31" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0L6,3L0,6z" fill="#7f8b9d"/></marker></defs><rect x="24" y="42" width="128" height="62" rx="9" fill="#21252d" stroke="#72b293"/><text x="88" y="70" text-anchor="middle" class="svg-t">系统契约</text><text x="88" y="91" text-anchor="middle" class="svg-t" font-size="11">目标/边界/schema</text><rect x="24" y="126" width="128" height="62" rx="9" fill="#21252d" stroke="#65a6d9"/><text x="88" y="162" text-anchor="middle" class="svg-t">用户目标</text><rect x="24" y="210" width="128" height="62" rx="9" fill="#21252d" stroke="#cf7c72"/><text x="88" y="238" text-anchor="middle" class="svg-t">不可信数据</text><text x="88" y="259" text-anchor="middle" class="svg-t" font-size="11">网页/工具/文档</text><path d="M152 73L207 137M152 157H207M152 241L207 177" stroke="#7f8b9d" marker-end="url(#sp31)"/><rect x="212" y="96" width="132" height="122" rx="9" fill="#21252d" stroke="#8b76bd"/><text x="278" y="126" text-anchor="middle" class="svg-t">概率模型</text><text x="278" y="153" text-anchor="middle" class="svg-t" font-size="11">冲突解析不保证</text><text x="278" y="180" text-anchor="middle" class="svg-t">提出意图</text><path d="M344 157H389" stroke="#7f8b9d" marker-end="url(#sp31)"/><rect x="394" y="96" width="138" height="122" rx="9" fill="#21252d" stroke="#d3a05a"/><text x="463" y="126" text-anchor="middle" class="svg-t">确定性策略门</text><text x="463" y="153" text-anchor="middle" class="svg-t" font-size="11">身份/金额/目标域</text><text x="463" y="180" text-anchor="middle" class="svg-t">允许/拒绝/审批</text><path d="M532 157H577" stroke="#7f8b9d" marker-end="url(#sp31)"/><rect x="582" y="96" width="150" height="122" rx="9" fill="#21252d" stroke="#72b293"/><text x="657" y="126" text-anchor="middle" class="svg-t">业务工具</text><text x="657" y="153" text-anchor="middle" class="svg-t" font-size="11">真实状态/幂等</text><text x="657" y="180" text-anchor="middle" class="svg-t">审计结果</text><path d="M657 218V286H278V218" fill="none" stroke="#cf7c72" stroke-dasharray="6 5" marker-end="url(#sp31)"/><text x="468" y="311" text-anchor="middle" class="svg-t">工具事实返回模型；提示不能伪造执行成功</text></svg><figcaption>图 1　系统提示管理模型行为，确定性策略管理现实权限。</figcaption></figure>'},
      {title:'把提示写成最小、分区、无矛盾的契约',kind:'eng',badge:'设计',lead:'角色口吻、政策、数据和例子混在一段里会发生什么？',body:'<p>按稳定目标、不可违反约束、输入信任边界、决策流程、输出schema和失败处理分区；用同一术语表达主体和动作，写清适用与不适用。把会频繁变化的业务数据放到结构化配置或检索，不复制进长提示。</p><p>规则冲突时明示优先关系和保守回退，例如“资料不足时询问，不猜测；涉及写操作先提议再批准”。少量覆盖边界的正反例比大量风格示例更有价值。</p>'},
      {title:'提示注入利用的是指令与数据共享通道',kind:'eng',badge:'失败边界',lead:'用XML标签包住网页内容，为什么只能降低混淆而不能隔离？',body:'<p>标签、引号和“不要执行以下内容”给模型提供语义提示，但网页里的恶意文本仍进入同一上下文并参与注意力。间接注入可要求泄露提示、改写目标或调用工具；模型可能在长链条里忘记信任边界。</p><p>把外部内容视为数据，最小化可见上下文；工具使用最小权限和目标白名单；敏感动作由确定性策略或人批准。用多语言、编码、长上下文和工具返回中的注入测试，而非只测“忽略之前指令”。</p>'},
      {title:'系统提示不是秘密存储，防泄露要假设它可被推断',kind:'eng',badge:'保密',lead:'提示里不直接展示密钥，是否可以放内部政策和检测阈值？',body:'<p>用户可通过直接索要、角色扮演、逐段推断、错误回显或工具日志获得提示内容；模型也可能复述其语义。密钥、个人数据和真正需保密的规则不应进入提示。可公开的行为规范可以写入，敏感检测逻辑放在服务端。</p><p>提示泄露与指令违反是不同指标：模型不复述原文仍可能受低层恶意指令控制；反之公开规范被复述未必造成安全损失。先定义资产再测试。</p>'},
      {title:'输出结构与拒绝路径必须可被消费者验证',kind:'eng',badge:'接口',lead:'模型说“需要批准”，下游为何不能只读这句话？',body:'<p>让模型产生版本化schema和明确状态，如 <code>propose_refund</code>、<code>needs_approval</code>、<code>insufficient_data</code>；消费者做类型、枚举、跨字段和业务校验。自由文本解释不能触发副作用，拒绝或缺信息也是一等状态。</p><p>结构化输出保证格式不保证语义；订单是否属于用户、金额是否允许仍由权威系统查询。提示和schema版本要一起回归，防消费者与模型契约漂移。</p>'},
      {title:'会话、摘要和记忆会让旧提示假设漂移',kind:'eng',badge:'长时状态',lead:'系统提示未改，为什么长对话后行为仍可能变化？',body:'<p>历史消息、压缩摘要、工具观察和记忆不断改变条件上下文；旧用户目标可能被摘要成高置信事实，或跨任务残留。每轮重申必要的不变量，按任务/用户作用域读取记忆，并把来源与可纠正状态传给模型。</p><p>压缩后运行冲突和安全哨兵测试；任务切换时清除局部状态。不能依赖“最初说过一次”来保护长时Agent。</p>'},
      {title:'版本化提示像代码，但回滚证据比文本diff更重要',kind:'eng',badge:'发布',lead:'只改一句措辞，为什么也可能改变完全无关的任务？',body:'<p>提示变化会改变注意力和生成分布，影响不同语言、长上下文、工具选择和拒绝。保存提示、模板、模型、采样、工具schema和评测集版本；灰度比较任务成功、契约遵循、冲突解决、误拒、token成本和人工修正。</p><p>文本diff说明改了什么，不说明行为怎样变。发布需要冻结回归集、隐藏攻击集和生产影子样本；异常时能同时回滚提示与关联schema。</p>'},
      {title:'契约测试覆盖正常、冲突、恶意和故障四象限',kind:'eng',badge:'验证',lead:'提示测试为什么不能只保存几个理想对话？',body:'<p>正常集验证主任务；冲突集让用户、历史和工具内容与高层约束相反；恶意集覆盖注入、提取、编码与多轮；故障集覆盖缺字段、超时、拒绝和工具不一致。断言结构、决策、工具副作用和用户可见事实，而非逐字匹配措辞。</p><p>按语言、长度、模型版本和风险切片，报告置信区间与失败样例。模型评审可辅助扩展，但高风险判定使用确定性检查或人工复核。</p>'},
      {title:'常见误区与概念依赖',badge:'误区与学习路线',lead:'系统提示是概率契约层，不是权限、秘密或业务真相。',body:'<div class="dd-note warn"><b>初学者最易混淆：</b>“优先级更高”描述模型应遵循什么，不证明它每次都会遵循；安全设计必须假设遵循会失败。</div><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>误区</th><th>更准确的理解</th></tr></thead><tbody><tr><td>system消息不可覆盖</td><td>训练提高遵循倾向，不是形式保证</td></tr><tr><td>提示内规则能保密</td><td>默认可被复述或推断</td></tr><tr><td>提示可授权工具</td><td>权限由执行器和主体身份决定</td></tr><tr><td>越长越可靠</td><td>冲突与上下文竞争会增加</td></tr></tbody></table></div><p><b>学习路线：</b>先学上下文、结构化输出和工具调用，再学提示注入、指令层级、策略引擎与契约评测。</p>'}
    ],
    chain:['把业务需求拆成稳定契约与动态数据','给每类上下文标记角色、来源和可信度','用最小提示声明目标、冲突和失败路径','让模型只提出结构化意图','由确定性策略核验身份、权限和业务状态','把工具事实回传而不伪造成功','用四象限契约集评测并灰度发布','监控长时状态漂移并版本化回滚'],
    quiz:[{q:'系统提示为何不是安全边界？',a:'它仍是模型条件上下文，遵循是概率行为；权限和副作用必须由执行层保证。'},{q:'时间更晚的用户消息能否覆盖系统规则？',a:'按设计不应；跨层冲突由信任优先级而非时间顺序决定，但仍需测试。'},{q:'为什么提示里不能放密钥？',a:'提示可能被直接复述、间接推断或通过日志泄露。'},{q:'结构化输出还需什么校验？',a:'类型、跨字段、授权和权威业务状态校验。'},{q:'提示发布为何需要行为回归？',a:'小文本变化也会改变多任务、语言、工具选择和拒绝分布。'}],
    sources:[{title:'The Instruction Hierarchy',url:'https://arxiv.org/abs/2404.13208',note:'训练模型优先遵循特权指令'},{title:'IHEval',url:'https://arxiv.org/abs/2502.08745',note:'对齐与冲突指令层级评测'},{title:'Indirect Prompt Injection',url:'https://arxiv.org/abs/2302.12173',note:'外部数据中的间接指令攻击'},{title:'StruQ',url:'https://arxiv.org/abs/2402.06363',note:'结构化查询与提示注入防御研究'}]
  });
  window.DEEPDIVE['system-prompt'].html = window.DEEPDIVE['system-prompt'].html.replace(
    '<div class="dd-formula">pass rate = passed / total；净收益 = 新修复 − 新回归 = 6 − 12 = −6</div>',
    '<div class="dd-formula" data-formula-id="system-prompt-contract-metrics"><math display="block" aria-label="通过率 r 等于通过数 m 除以测试总数 n；净收益 delta 等于新修复数 f 减去新回归数 g，本例为六减十二等于负六"><mtable><mtr><mtd><mi>r</mi><mo>=</mo><mfrac><mi>m</mi><mi>n</mi></mfrac></mtd></mtr><mtr><mtd><mi>Δ</mi><mo>=</mo><mi>f</mi><mo>−</mo><mi>g</mi><mo>=</mo><mn>6</mn><mo>−</mo><mn>12</mn><mo>=</mo><mo>−</mo><mn>6</mn></mtd></mtr></mtable></math></div><p class="dd-formula-note"><code>r</code> 是契约测试通过率，<code>m</code> 是通过的测试条数，<code>n</code> 是测试总条数；<code>Δ</code> 是版本相对基线的净收益，<code>f</code> 是新版本修复的旧失败数，<code>g</code> 是新版本造成的新回归数。负 Δ 表示新增回归多于修复，不能因局部新规则生效就发布。</p>'
  )
  .replace(
    '标为 system 后，文本是否变成模型不可违反的程序？</p>',
    '标为 system 后，文本是否变成模型不可违反的程序？</p><p>系统提示的输入是应用注入的角色、任务、行为边界和输出契约，输出是这些 token 条件下的回答分布。聊天模板把消息角色编码进前缀，训练提高遵循高优先级指令的倾向，但没有形式解释器逐条强制执行。遵循结果只能当概率行为；秘密、身份和现实权限必须由模型外系统控制。</p>'
  )
  .replace(
    '后出现的用户消息为什么不应覆盖更早的系统约束？</p>',
    '后出现的用户消息为什么不应覆盖更早的系统约束？</p><p>冲突解析接收带角色、来源与时间的消息序列，输出应执行的高层任务和被忽略的低层冲突。系统或开发者约束高于用户目标，不可信网页与工具文本只作为数据；同一层才用较新意图补充。优先级是期望规则，不是执行保证，所以运行时仍须验证输出与副作用。</p>'
  )
  .replace(
    '“退款超过500元需批准”应该只写进系统提示吗？</p>',
    '“退款超过500元需批准”应该只写进系统提示吗？</p><p>案例输入是退款请求、订单事实、金额规则和登录主体，输出是结构化退款提议或缺信息状态，而不是直接改账。模型负责解释并提出意图，确定性策略门再核验所有权、金额与审批令牌，工具返回真实状态。提示通过不代表退款获准；超过五百元且无批准时执行器必须拒绝。</p>'
  )
  .replace(
    '新增一页规则后效果下降，怎样判断是规则冲突还是上下文竞争？</p>',
    '新增一页规则后效果下降，怎样判断是规则冲突还是上下文竞争？</p><p>提示消融输入短版、长版和结构化改写版以及同一契约测试集，输出通过率、修复数、回归数、成本和延迟。一次只改变规则表达并逐切片比较；净收益为负表示回归多于修复。单一总通过率仍可能掩盖关键安全回归，因此不能独自决定发布。</p>'
  )
  .replace(
    '模型被注入时，哪一层仍能阻止真实副作用？</p>',
    '模型被注入时，哪一层仍能阻止真实副作用？</p><p>分层系统输入模型提出的结构化意图及身份、金额、目标域等权威状态，输出允许、拒绝或待审批的工具调用。概率模型只提出意图，确定性策略门执行校验，业务工具负责幂等状态变化并回传事实。图中的箭头表示控制流，不表示系统提示本身拥有授权能力。</p>'
  )
  .replace(
    '角色口吻、政策、数据和例子混在一段里会发生什么？</p>',
    '角色口吻、政策、数据和例子混在一段里会发生什么？</p><p>提示设计输入稳定目标、动态数据、信任边界、失败处理和输出 schema，输出最小、分区且无矛盾的行为契约。稳定规则留在提示，频繁变化的数据通过结构化配置或检索注入；冲突时明示优先关系与保守回退。提示越短并不自动越好，删减后仍须用覆盖边界的正反例验收。</p>'
  )
  .replace(
    '用XML标签包住网页内容，为什么只能降低混淆而不能隔离？</p>',
    '用XML标签包住网页内容，为什么只能降低混淆而不能隔离？</p><p>注入测试输入可信指令与含恶意文本的网页、文档或工具结果，输出模型意图和实际工具动作。标签只能提示“这是数据”，恶意 token 仍参加同一注意力计算；真正防线是最小上下文、工具白名单、最小权限与审批。只通过一种“忽略之前指令”模板不能证明抵抗多语言、编码或多轮攻击。</p>'
  )
  .replace(
    '提示里不直接展示密钥，是否可以放内部政策和检测阈值？</p>',
    '提示里不直接展示密钥，是否可以放内部政策和检测阈值？</p><p>泄露评测输入系统提示资产、攻击提问和日志工具路径，输出原文复述、语义推断与实际安全损失。默认假设提示可被复述或推断，因此密钥、个人数据和敏感检测逻辑不能进入上下文。公开行为规范被复述未必是事故，是否失败必须先按资产价值定义。</p>'
  )
  .replace(
    '模型说“需要批准”，下游为何不能只读这句话？</p>',
    '模型说“需要批准”，下游为何不能只读这句话？</p><p>输出契约接收模型建议，输出版本化状态、字段和拒绝原因供消费者解析。消费者依次做类型、枚举、跨字段、身份和业务状态校验；自由文本解释不触发副作用。结构合法只表示格式通过，不证明订单所有权、金额或权限语义正确。</p>'
  )
  .replace(
    '系统提示未改，为什么长对话后行为仍可能变化？</p>',
    '系统提示未改，为什么长对话后行为仍可能变化？</p><p>长时状态装配输入系统不变量、历史、摘要、工具观察和带作用域记忆，输出本轮模型可见上下文。每轮重申必要边界并携带来源与可纠正状态，任务切换时清除局部记忆。初始规则只出现一次不能保证摘要后仍存在，也不能阻止旧目标跨任务残留。</p>'
  )
  .replace(
    '只改一句措辞，为什么也可能改变完全无关的任务？</p>',
    '只改一句措辞，为什么也可能改变完全无关的任务？</p><p>提示发布输入提示、模板、模型、采样、工具 schema 和评测集版本，输出带回归证据的候选版本与回滚包。文本变化会重新分配生成概率，须灰度比较任务成功、冲突、误拒、成本和人工修正。文本 diff 只说明改了什么，不能证明行为没有旁路回归。</p>'
  )
  .replace(
    '提示测试为什么不能只保存几个理想对话？</p>',
    '提示测试为什么不能只保存几个理想对话？</p><p>契约测试输入正常、冲突、恶意和故障四类样本，输出结构、决策、副作用与用户可见事实的断言结果。按语言、长度、版本和风险切片统计，并保留失败样例与置信区间；逐字措辞不必一致，但高风险动作和事实边界必须确定性通过。</p>'
  );

  window.DEEPDIVE['adversarial-robustness'] = window.createDeepDive({
    title:'对抗鲁棒性：在明确扰动集合内寻找最坏情况，而非宣称模型“抗攻击”',
    subtitle:'从决策边界、FGSM/PGD、鲁棒优化和认证半径，到自适应攻击、分布外变换与系统级损失控制，理解保证的适用域。',
    thesis:'鲁棒性必须相对于攻击者能力定义：输入可改哪些维度、预算多大、知道模型多少、目标是什么。一个防御只在特定扰动集合和评测攻击下有效，不能自动外推到现实语义攻击。',
    goals:['写出威胁模型与扰动集合','手算一次梯度符号攻击','解释鲁棒优化的内外层','识别梯度遮蔽和自适应评测缺陷','区分经验鲁棒与可认证保证'],
    sections:[
      {title:'自然准确率只测数据分布附近，不测主动最坏情况',badge:'问题定义',lead:'测试集99%准确，为什么一个几乎不变的输入仍可能翻转分类？',body:'<p>高维模型可依赖人类不敏感却与标签相关的脆弱特征。普通测试从自然分布抽样，很少碰到攻击者沿决策边界专门搜索的方向。对抗样本不是随机噪声：它根据模型或查询反馈优化，使损失增加或目标类别概率上升。</p><p>现实风险取决于攻击者能否实现扰动、模型输出是否触发高影响动作和下游是否有独立验证。图片像素范数只是研究代理，不等于所有物理或语义攻击。</p>'},
      {title:'威胁模型先于攻击算法和鲁棒分数',kind:'eng',badge:'范围',lead:'同一个“鲁棒准确率”为什么无法跨论文或产品直接比较？',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>维度</th><th>选择</th><th>影响</th></tr></thead><tbody><tr><td>目标</td><td>非定向/定向/置信操纵</td><td>成功条件不同</td></tr><tr><td>知识</td><td>白盒/分数查询/仅标签/迁移</td><td>可用梯度和预算不同</td></tr><tr><td>扰动</td><td>L∞、L2、L0、旋转、补丁、语义</td><td>“接近”的定义不同</td></tr><tr><td>预算</td><td>ε、查询次数、物理面积</td><td>攻击强度不同</td></tr><tr><td>系统</td><td>仅模型/含预处理与业务动作</td><td>真实影响不同</td></tr></tbody></table></div><p>报告必须带完整威胁模型，单个百分比没有独立意义。</p>'},
      {title:'数值例子：FGSM沿损失梯度符号走一步',kind:'math',badge:'手算',lead:'为什么每个像素只改很小，总损失却能明显上升？',body:'<p>设二维输入 x=[0.40,0.70]，对真实标签的损失梯度为 [−0.8,1.6]，L∞预算 ε=0.05。FGSM 取符号 [−1,+1]，得到 x_adv=[0.35,0.75]。一阶近似损失增加为梯度点乘扰动：<code>(−0.8)(−0.05)+1.6(0.05)=0.12</code>。</p><div class="dd-formula">x_adv = clip(x + ε sign(∇xL)) = [0.35, 0.75]</div><p>高维下许多微小同向贡献累积。一步攻击快但不保证找到真正最坏点；PGD会多步投影回预算球。</p>'},
      {title:'完整示例：交通标志分类从数字攻击到打印贴纸',kind:'eng',badge:'案例推演',lead:'像素空间成功的攻击怎样验证能否成为物理世界风险？',body:'<ol class="dd-steps"><li>定义资产：错误分类会影响辅助驾驶，但最终控制还有地图、轨迹和驾驶员。</li><li>白盒L∞攻击检查模型局部边界，记录自然与鲁棒准确率。</li><li>扩展旋转、亮度、压缩和不同距离，测试对常见变换的稳定性。</li><li>优化受面积和颜色约束的贴纸，并在不同打印机、相机、角度、天气重复采集。</li><li>用未参与优化的设备和路线测试，防止对单环境过拟合。</li><li>验证系统融合是否拒绝不一致标志，模型错误是否真的改变动作。</li><li>训练后用自适应攻击重新优化，而不是只重放旧贴纸。</li></ol><p>从数字扰动到业务损失是一条证据链，不应跳步。</p>'},
      {title:'原创图：鲁棒性结论被威胁模型边界包围',badge:'可视化',lead:'防住一个攻击算法，为什么不能推出防住同一预算内所有攻击？',body:'<figure class="dd-fig"><svg viewBox="0 0 760 335" role="img" aria-label="自然输入周围的扰动集合、决策边界、经验攻击点、认证半径和集合外语义攻击"><circle cx="275" cy="165" r="112" fill="#65a6d9" opacity="0.10" stroke="#65a6d9" stroke-dasharray="7 5"/><circle cx="275" cy="165" r="58" fill="#72b293" opacity="0.11" stroke="#72b293"/><path d="M70 250C180 110 340 255 500 95S690 105 735 55" fill="none" stroke="#cf7c72" stroke-width="4"/><circle cx="275" cy="165" r="7" fill="#f1f3f5"/><text x="275" y="148" text-anchor="middle" class="svg-t">自然输入 x</text><circle cx="361" cy="118" r="7" fill="#d3a05a"/><text x="390" y="111" class="svg-t" font-size="11">PGD找到的失败</text><circle cx="198" cy="225" r="7" fill="#8b76bd"/><text x="87" y="239" class="svg-t" font-size="11">未测方向</text><text x="275" y="292" text-anchor="middle" class="svg-t">声明的L∞扰动集合</text><text x="275" y="173" text-anchor="middle" class="svg-t" font-size="11">认证半径</text><rect x="545" y="205" width="165" height="78" rx="9" fill="#21252d" stroke="#d3a05a"/><text x="627" y="234" text-anchor="middle" class="svg-t">集合外攻击</text><text x="627" y="258" text-anchor="middle" class="svg-t" font-size="11">补丁/语义/物理/查询</text><text x="548" y="32" class="svg-t" font-size="11">红线：决策边界</text></svg><figcaption>图 1　经验攻击只提供下界；认证半径也只对其数学假设内成立。</figcaption></figure>'},
      {title:'PGD把攻击写成约束内的迭代最优化',kind:'math',badge:'机制',lead:'为什么多步小更新通常比一次大步更强？',body:'<p>每一步沿当前点的损失梯度上升，再投影回 x 周围的扰动集合：<code>x_{t+1}=Π_S(x_t+α sign(∇L))</code>。随机起点减少落在局部平坦区的概率，多次重启探索不同路径。</p><p>步长、步数和重启不足会高估防御；过大步长可能在边界来回跳。攻击强度应用收敛曲线确认：增加计算后成功率不再明显上升，但仍不是全局最优证明。</p>'},
      {title:'对抗训练近似求解内层最坏扰动、外层更新模型',kind:'eng',badge:'鲁棒优化',lead:'把攻击样本加入训练，为什么会提高特定预算内的稳健性？',body:'<p>目标近似为 <code>min_θ E[max_{δ∈S} L(fθ(x+δ),y)]</code>。内层攻击为当前模型找高损失输入，外层梯度让这些局部最坏点变得正确。若内层攻击弱，模型可能只学会欺骗攻击器；训练扰动集与真实攻击不匹配时，收益不外推。</p><p>鲁棒训练常增加算力并牺牲部分自然准确率。应比较同容量、同数据与同计算基线，并按目标风险决定是否值得。</p>'},
      {title:'梯度遮蔽制造“攻击失败”的假安全',kind:'eng',badge:'评测陷阱',lead:'预处理让梯度为零，为什么黑盒攻击反而可能成功？',body:'<p>不可微、随机或饱和操作会让直接反传得到无用梯度，但决策边界仍脆弱。迹象包括一步攻击强于多步、黑盒迁移强于白盒、增大预算不降低准确率、去掉随机性后攻击骤强。</p><p>自适应攻击用可微近似、期望梯度、直通估计或决策/分数查询绕过防御。评测者必须知道完整管线并针对防御机制设计攻击，而非套一个默认库。</p>'},
      {title:'经验鲁棒与认证鲁棒回答不同问题',kind:'eng',badge:'保证',lead:'认证半径0.3是否意味着现实照片怎么改都安全？',body:'<p>经验鲁棒表示在已运行攻击下尚未找到失败，是最坏风险的下界证据；认证方法证明在特定范数、半径和模型假设内预测不变，是条件性的数学保证。随机平滑等方法可给L2半径，但保证不覆盖旋转、贴纸、语义变化或实现错误。</p><p>报告可认证样本比例、半径分布和自然准确率，不只报平均半径。高风险系统仍需系统级冗余和监控。</p>'},
      {title:'范数接近不等于人类语义相同，语义相同也可能范数很远',kind:'eng',badge:'边界',lead:'L∞约束为什么既可能允许奇怪纹理，又排除轻微旋转？',body:'<p>逐像素预算便于优化和比较，却与感知、物理可实现性和任务语义不完全一致。轻微平移会移动大量像素，范数很大但标签不变；精心纹理范数小却可能人眼可见。真实评测应加入自然腐败、几何、风格、补丁、语义改写和领域约束。</p><p>不同攻击集合不是互相替代的“更强版本”，而是不同威胁。组合报告并说明未覆盖空间。</p>'},
      {title:'系统级鲁棒性用冗余、拒绝和损失限制兜住模型失败',kind:'eng',badge:'工程',lead:'无法保证分类器不被攻破时，怎样降低事故后果？',body:'<p>使用独立传感器、规则约束、时间一致性、异常检测、置信校准和安全降级；高影响动作需要多证据或人工确认。限制单次动作幅度和频率，记录可审计输入与版本，并能快速回滚。</p><p>这些控制也可能被协同攻击，必须端到端红队。目标从“模型永不出错”转为“错误不轻易穿透到不可逆损失”。</p>'},
      {title:'鲁棒评测报告攻击预算曲线和最坏切片',kind:'eng',badge:'验证',lead:'只在ε=8/255报一个点会掩盖什么？',body:'<p>同时给自然准确率、不同ε下鲁棒准确率、攻击成功率、步骤/重启收敛、目标与非目标攻击、白盒/迁移/查询、不同范数与自然腐败。按类别、设备和环境报告最坏切片及置信区间。</p><p>攻击代码和模型预处理要共同审计，检查像素尺度、裁剪、归一化和随机种子。隐藏自适应攻击集防止防御对公开攻击过拟合。</p>'},
      {title:'常见误区与概念依赖',badge:'误区与学习路线',lead:'“鲁棒”必须带范围、预算、攻击知识和系统后果。',body:'<div class="dd-note warn"><b>初学者最易混淆：</b>攻击器没找到反例，不等于反例不存在；经验评测给的是攻击能力下的证据。</div><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>误区</th><th>更准确的理解</th></tr></thead><tbody><tr><td>随机噪声测试等于对抗测试</td><td>攻击会沿目标方向优化</td></tr><tr><td>防住PGD就是抗所有攻击</td><td>只支持声明集合和评测范围</td></tr><tr><td>预处理破坏梯度即安全</td><td>可能只是梯度遮蔽</td></tr><tr><td>认证等于现实无风险</td><td>保证受范数、半径和实现假设限制</td></tr></tbody></table></div><p><b>学习路线：</b>先学梯度、损失和分类边界，再学鲁棒优化、认证、防御自适应评测和系统安全工程。</p>'}
    ],
    chain:['定义资产、攻击目标、知识与可修改集合','建立自然性能和随机腐败基线','从FGSM到多步多重启寻找反例','检查攻击收敛和梯度遮蔽','用内层强攻击训练或选择防御','以自适应、迁移和集合外攻击复测','在系统层增加冗余、拒绝和损失限制','持续报告范围内保证与范围外残余风险'],
    quiz:[{q:'自然准确率为何不等于对抗鲁棒？',a:'自然测试不是主动沿决策边界搜索的最坏输入。'},{q:'FGSM示例的扰动和近似损失增量？',a:'扰动[−0.05,+0.05]，一阶损失增加0.12。'},{q:'PGD为何要投影？',a:'保证每步搜索仍在声明的攻击预算集合内。'},{q:'什么是梯度遮蔽？',a:'防御让梯度难用但边界仍脆弱，造成白盒攻击失败的假象。'},{q:'认证半径的边界？',a:'只保证指定范数、半径、模型与实现假设内的不变性。'}],
    sources:[{title:'Towards Deep Learning Models Resistant to Adversarial Attacks',url:'https://arxiv.org/abs/1706.06083',note:'PGD与鲁棒优化框架'},{title:'Explaining and Harnessing Adversarial Examples',url:'https://arxiv.org/abs/1412.6572',note:'FGSM与线性解释'},{title:'Obfuscated Gradients Give a False Sense of Security',url:'https://arxiv.org/abs/1802.00420',note:'梯度遮蔽识别与攻击'},{title:'On Adaptive Attacks to Adversarial Example Defenses',url:'https://arxiv.org/abs/2002.08347',note:'针对防御机制的自适应评测'}]
  });
})();
