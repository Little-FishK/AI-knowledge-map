/* 理解原理页 —— RNN */
window.DEEPDIVE = window.DEEPDIVE || {};
window.DEEPDIVE['rnn'] = window.createDeepDive({
  title:'循环神经网络 RNN：把历史压进一个不断更新的状态',
  subtitle:'从共享递推、时间展开与 BPTT，到梯度连乘、LSTM 门控、教师强制和流式部署。',
  thesis:'RNN 在每个时间步复用同一组参数，以隐藏状态压缩此前序列。它天然适合流式输入、状态小且单步成本固定；代价是<b>时间步难并行、历史有损压缩、梯度沿时间反复连乘</b>，因此长距离信用分配和精确回看都困难。',
  goals:['手算一次 RNN 状态递推','从时间展开解释 BPTT','区分梯度爆炸与信息遗忘','理解 LSTM 门控和训练—推理错位'],
  sections:[
    {title:'同一个单元在时间上反复使用',kind:'math',badge:'核心递推',lead:'参数量为何不随序列长度增加？',body:'<div class="dd-formula">hₜ=φ(Wₕhₜ₋₁+Wₓxₜ+b)，　yₜ=g(Wᵧhₜ)</div><p><code>t</code> 是时间步，<code>xₜ</code> 是当前输入，<code>hₜ₋₁</code> 与 <code>hₜ</code> 是更新前后的隐藏状态；<code>Wₕ</code> 变换旧状态，<code>Wₓ</code> 变换当前输入，<code>b</code> 是偏置，<code>φ</code> 是状态激活；<code>Wᵧ</code> 和 <code>g</code> 把状态变成当前输出 <code>yₜ</code>。</p><p>所有时间步共享 Wₕ、Wₓ 和 b，所以序列变长只增加重复计算，不增加这组参数。hₜ 不是一份可检索的完整日志，而是模型为当前任务学到的历史摘要。共享参数让模型可处理任意长度，也让同一个变换及其误差反复作用。</p><div class="dd-note"><b>贯穿例子：</b>用一维 RNN 读取三天设备信号 x=[1,0,2]，φ 暂取恒等函数，Wₕ=0.5、Wₓ=1、b=0、h₀=0。</div>'},
    {title:'三步递推可以逐项手算',kind:'math',badge:'算例',lead:'最后状态到底混合了哪些历史？',body:'<p>沿用上一节的一维输入 x=[1,0,2]、Wₕ=0.5、Wₓ=1、b=0、h₀=0，并令激活为恒等函数。</p><div class="dd-formula">h₁=1；　h₂=0.5；　h₃=0.5×0.5+2=2.25</div><p>展开 h₃ 可得 2+0.5×0+0.5²×1。最近输入权重为 1，两步前输入权重为 0.25；若 Wₕ=0.5，早期信息按距离指数衰减。若 Wₕ=1.2，影响可能增长，但状态与梯度也更易爆炸。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>t</th><th>xₜ</th><th>计算</th><th>hₜ</th></tr></thead><tbody><tr><td>1</td><td>1</td><td>0.5×0+1</td><td>1</td></tr><tr><td>2</td><td>0</td><td>0.5×1+0</td><td>0.5</td></tr><tr><td>3</td><td>2</td><td>0.5×0.5+2</td><td>2.25</td></tr></tbody></table></div>'},
    {title:'时间展开把循环变成深度为 T 的计算图',kind:'math',badge:'BPTT',lead:'递归参数怎样收到来自未来损失的责任？',body:'<figure class="dd-fig"><svg viewBox="0 0 730 265" role="img" aria-label="三步 RNN 时间展开与反向传播路径"><defs><marker id="rnna" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 z" fill="#748093"/></marker><marker id="rnnb" markerWidth="8" markerHeight="8" refX="2" refY="3" orient="auto"><path d="M6,0 L0,3 L6,6 z" fill="#cf7d72"/></marker></defs><text x="82" y="38" class="svg-t">x₁=1</text><text x="302" y="38" class="svg-t">x₂=0</text><text x="522" y="38" class="svg-t">x₃=2</text><path d="M100,48 L100,92" stroke="#748093" marker-end="url(#rnna)"/><path d="M320,48 L320,92" stroke="#748093" marker-end="url(#rnna)"/><path d="M540,48 L540,92" stroke="#748093" marker-end="url(#rnna)"/><rect x="42" y="96" width="116" height="62" rx="8" fill="#21252d" stroke="#65a6d9"/><rect x="262" y="96" width="116" height="62" rx="8" fill="#21252d" stroke="#65a6d9"/><rect x="482" y="96" width="116" height="62" rx="8" fill="#21252d" stroke="#65a6d9"/><text x="100" y="132" text-anchor="middle" class="svg-t">h₁=1</text><text x="320" y="132" text-anchor="middle" class="svg-t">h₂=0.5</text><text x="540" y="132" text-anchor="middle" class="svg-t">h₃=2.25</text><path d="M158,127 L260,127" stroke="#748093" marker-end="url(#rnna)"/><path d="M378,127 L480,127" stroke="#748093" marker-end="url(#rnna)"/><path d="M540,158 L540,205" stroke="#748093" marker-end="url(#rnna)"/><rect x="478" y="207" width="124" height="42" rx="8" fill="#21252d" stroke="#d3a05a"/><text x="540" y="233" text-anchor="middle" class="svg-t">损失 L₃</text><path d="M520,207 C440,185 405,183 350,158" fill="none" stroke="#cf7d72" stroke-dasharray="6 4" marker-end="url(#rnnb)"/><path d="M490,216 C340,212 220,190 130,158" fill="none" stroke="#cf7d72" stroke-dasharray="6 4" marker-end="url(#rnnb)"/><text x="252" y="231" class="svg-t" font-size="11">梯度从未来沿共享状态路径返回</text></svg><figcaption>图 1　前向是顺序递推；BPTT 将三次使用同一参数的梯度相加。序列越长，需保存的激活和梯度路径越长。</figcaption></figure><p>反向传播穿过展开图并累加每次参数复用的贡献。截断 BPTT 每 K 步切断图，内存从依赖整段变为依赖 K，但超过 K 步的直接信用路径也被切断。</p>'},
    {title:'梯度问题来自雅可比反复相乘',kind:'math',badge:'长依赖',lead:'为什么 Wₕ=0.5 与 1.2 会走向两个极端？',body:'<div class="dd-formula">∂hₜ/∂hₖ = ∏<sub>i=k+1…t</sub> diag(φ′ᵢ)Wₕ</div><p><code>hₖ</code> 是较早第 k 步状态，<code>hₜ</code> 是较晚第 t 步状态；乘积遍历中间时间步 i，<code>diag(φ′ᵢ)</code> 是激活函数逐坐标导数组成的对角矩阵，<code>Wₕ</code> 是每步复用的状态权重。在一维线性例子中，跨 d 步梯度就是 Wₕᵈ：0.5¹⁰≈0.00098，早期责任几乎消失；1.2¹⁰≈6.19，容易爆炸。非线性饱和会进一步让 φ′ 接近零。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>现象</th><th>表现</th><th>常用缓解</th><th>不能解决</th></tr></thead><tbody><tr><td>梯度爆炸</td><td>loss/范数突增、NaN</td><td>梯度裁剪、初始化、归一化</td><td>长期记忆容量</td></tr><tr><td>梯度消失</td><td>早期 token 学不到责任</td><td>门控、残差、较短路径</td><td>被状态丢掉的原文</td></tr></tbody></table></div>'},
    {title:'LSTM 用近似加法通道控制记忆',kind:'math',badge:'门控',lead:'门控为何比普通 tanh RNN 更容易保留信息？',body:'<div class="dd-formula">cₜ=fₜ⊙cₜ₋₁+iₜ⊙c̃ₜ，　hₜ=oₜ⊙tanh(cₜ)</div><p><code>cₜ₋₁、cₜ</code> 是旧、新细胞状态，<code>c̃ₜ</code> 是候选内容；<code>fₜ</code> 是遗忘门，<code>iₜ</code> 是输入门，<code>oₜ</code> 是输出门，<code>⊙</code> 表示逐元素乘法，<code>hₜ</code> 是暴露给外部的隐藏状态。</p><p>遗忘门 f 决定旧记忆保留比例，输入门 i 决定写入多少候选，输出门 o 决定暴露多少状态。若 f≈1、i≈0，c 可沿近似恒等路径传递，梯度不必每步都穿过完整非线性。GRU 合并状态与部分门，参数更少。</p><p>门不是符号规则或硬存储槽；它们是数据驱动的连续向量。即使 f=0.99，跨 500 步保留比例也只有 0.99⁵⁰⁰≈0.0066。</p>'},
    {title:'教师强制制造训练—推理输入差异',kind:'eng',badge:'生成',lead:'训练每一步都看真答案，为什么上线会滚雪球？',body:'<p>训练序列生成器时，常把真实 yₜ₋₁ 输入下一步；推理时只能输入模型自己的预测。一处错误把状态带到训练中很少见的区域，后续错误继续累积，这叫 exposure bias。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>方法</th><th>作用</th><th>代价/边界</th></tr></thead><tbody><tr><td>计划采样</td><td>逐步混入模型预测</td><td>目标有偏、调度敏感</td></tr><tr><td>序列级损失</td><td>直接优化最终指标</td><td>梯度估计噪声大</td></tr><tr><td>beam/约束解码</td><td>推理时减少局部错误</td><td>不修复训练分布错位</td></tr></tbody></table></div>'},
    {title:'双向、堆叠与多对多只是读取方式变化',badge:'变体',lead:'分类、标注和生成各该取哪个输出？',body:'<p>many-to-one 取末状态做序列分类；many-to-many 为每步输出标签；编码器—解码器把输入压为状态再生成另一序列。双向 RNN 同时读取左右文，适合离线标注，却不能用于严格在线的未来未知场景。堆叠层提升容量，也拉长优化路径。</p><div class="dd-note warn"><b>双向不等于预测未来。</b>它只适用于推理时完整序列已可见的任务；实时传感器不能偷看尚未到达的数据。</div>'},
    {title:'RNN 与注意力的核心差别是访问历史的方式',kind:'eng',badge:'选型',lead:'小状态和全量 KV 历史分别换来了什么？',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>维度</th><th>RNN</th><th>自注意力/Transformer</th></tr></thead><tbody><tr><td>历史表示</td><td>固定状态压缩</td><td>保留各位置 KV</td></tr><tr><td>训练并行</td><td>时间依赖强</td><td>位置可并行</td></tr><tr><td>单步流式状态</td><td>固定大小</td><td>KV 随长度增长</td></tr><tr><td>精确回看</td><td>困难</td><td>可直接连接旧位置</td></tr><tr><td>典型优势</td><td>端侧、传感器、低延迟</td><td>大规模训练、长程交互</td></tr></tbody></table></div><p>实际速度取决于内核、批量和硬件。对每秒一个传感器值的在线异常检测，小 GRU 可能比大型 Transformer 更合适；对需要引用数千 token 之前原句的任务则相反。</p>'},
    {title:'评测要把长度、流式约束和状态恢复拆开',kind:'eng',badge:'工程边界',lead:'平均准确率为何不能证明 RNN 真正记住长依赖？',body:'<p>按依赖距离画性能曲线，并加入干扰项、状态重置、缺包和超长外推。部署时测每步延迟、状态大小、批处理吞吐、冷启动和断线恢复；若把隐藏状态跨用户复用，会造成上下文串扰与隐私泄漏。</p><div class="dd-note warn"><b>隐藏状态属于会话状态。</b>应绑定身份、设置生命周期、可重置并版本化；模型升级后旧状态未必兼容。</div>'},
    {title:'常见误区与学习路线',badge:'误区与依赖',lead:'把“循环”理解成计算图共享，而不是无限记忆。',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>误区</th><th>更准确的理解</th></tr></thead><tbody><tr><td>隐藏状态保存全部历史</td><td>它是固定容量的任务相关压缩</td></tr><tr><td>裁剪梯度能解决消失</td><td>它主要限制爆炸</td></tr><tr><td>LSTM 可以无限记忆</td><td>门控改善路径但仍会衰减和覆盖</td></tr><tr><td>双向 RNN 可用于实时预测</td><td>它需要完整未来上下文</td></tr><tr><td>Transformer 让 RNN 完全过时</td><td>流式小状态场景仍有优势</td></tr></tbody></table></div><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>层级</th><th>依赖与延伸</th></tr></thead><tbody><tr><td>先修</td><td>神经网络、反向传播、梯度消失</td></tr><tr><td><b>本页核心</b></td><td>递推状态、BPTT、门控、教师强制</td></tr><tr><td>对比</td><td>Transformer、状态空间模型、注意力</td></tr><tr><td>应用</td><td>语音、时间序列、流式推理</td></tr></tbody></table></div>'}
  ],
  chain:['序列逐步到达','共享递推更新隐藏状态','时间展开形成深计算图','BPTT 沿状态路径分配责任','门控选择保留与写入','以长度和流式约束验收'],
  quiz:[{q:'例子中 h₃ 为什么等于2.25？',a:'h₃=0.5h₂+x₃=0.5×0.5+2=2.25。'},{q:'Wₕ=0.5 时跨10步梯度约多少？',a:'0.5¹⁰≈0.00098，表现为梯度消失。'},{q:'截断 BPTT 的主要代价是什么？',a:'超过截断窗口的直接信用分配路径被切断。'},{q:'梯度裁剪主要缓解什么？',a:'限制梯度爆炸；不能恢复已经消失的长期信号。'},{q:'双向 RNN 何时不适用？',a:'严格在线、未来输入尚不可见的实时场景。'}],
  sources:[{title:'Learning Long-Term Dependencies with Gradient Descent is Difficult',url:'https://ieeexplore.ieee.org/document/279181',note:'长依赖梯度困难'},{title:'Long Short-Term Memory',url:'https://www.bioinf.jku.at/publications/older/2604.pdf',note:'LSTM 门控记忆'},{title:'Learning Phrase Representations using RNN Encoder–Decoder',url:'https://arxiv.org/abs/1406.1078',note:'GRU 与编码器—解码器'},{title:'Scheduled Sampling for Sequence Prediction with Recurrent Neural Networks',url:'https://arxiv.org/abs/1506.03099',note:'训练—推理错位'}]
});
