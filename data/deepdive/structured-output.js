/* 理解原理页 —— 结构化输出 */
window.DEEPDIVE = window.DEEPDIVE || {};
window.DEEPDIVE['structured-output'] = window.createDeepDive({
  title:'结构化输出：把概率文本接入确定性软件契约',
  subtitle:'从最小 schema、约束生成和分层校验，到版本迁移、有限修复与安全执行。',
  thesis:'结构化输出把模型回答转换为可解析、可校验、可版本化的数据契约。可靠链路不是“看起来像 JSON”，而是<b>生成时约束、完整性判断、schema 校验、业务/事实校验、执行时鉴权</b>；任何前一层通过都不能替代后一层。',
  goals:['区分语法/schema/语义/权限','设计最小可演进 schema','演算字段校验与修复','安全连接工具和数据库'],
  sections:[
    {title:'自然语言和程序接口的容错方式不同',badge:'直觉',lead:'多一句“好的”或少一个引号，为什么人能理解而程序会崩？',body:'<p>自然语言依靠语境容忍变体，程序需要明确边界、类型与状态。结构化输出声明字段、必填、枚举、长度和版本，使消费者能机械拒绝异常，而不是用脆弱正则猜模型意图。</p><p>提示“返回 JSON”只提高概率；JSON mode 通常保语法；JSON Schema 才进一步约束形状；约束解码在生成时阻止非法路径。支持范围依实现而异，仍需服务端复验。</p><div class="dd-note"><b>数据契约的作用：</b>让失败显式、局部、可计数。它不把概率模型变成可信数据库。</div>'},
    {title:'四道门必须逐层通过',kind:'eng',badge:'分层',lead:'一个对象能被 JSON.parse 解析，离执行退款还差多远？',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>层</th><th>检查</th><th>例子</th><th>失败处理</th></tr></thead><tbody><tr><td>完整语法</td><td>括号、引号、转义、结束态</td><td>不是半截 JSON</td><td>拒绝/重生</td></tr><tr><td>Schema</td><td>字段、类型、枚举、范围</td><td>action∈refund/ask</td><td>字段级错误</td></tr><tr><td>业务与事实</td><td>跨字段规则、查库、证据</td><td>amount≤订单可退额</td><td>补信息/转人工</td></tr><tr><td>权限与副作用</td><td>主体、策略、幂等、审批</td><td>当前客服能否批准</td><td>禁止或升级</td></tr></tbody></table></div><p>任何一层失败都不应让下游“尽量猜”。特别是模型给出的 user_id、金额和 action 都是不可信建议，执行层必须从认证上下文重新取得主体与权限。</p>'},
    {title:'最小 schema 比大而全更可靠',kind:'eng',badge:'设计',lead:'把所有可能字段都塞进一个嵌套对象，为什么会提高错误率和耦合？',body:'<p>只让模型产生需要语义判断的字段；时间戳、用户 ID、总价和派生状态由服务器填充。枚举代替自由动作名，明确 missing 与 null，限制字符串/数组长度，默认拒绝 additionalProperties，并给每个字段写业务含义与例子。</p><p>把互斥任务拆成 discriminated union：先用 <code>kind</code> 选择 refund_request 或 ask_clarification，再使用对应的小 schema。这样比一个包含几十个可选字段的对象更容易约束、校验和迁移。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>设计选择</th><th>收益</th><th>风险</th></tr></thead><tbody><tr><td>枚举动作</td><td>消费者无需猜拼写</td><td>新增值需兼容</td></tr><tr><td>必填字段</td><td>状态完整</td><td>未知值不要用假默认</td></tr><tr><td>长度/范围</td><td>限制资源与荒谬值</td><td>边界需与业务同步</td></tr><tr><td>禁止额外字段</td><td>发现 schema 漂移</td><td>前后版本需协商</td></tr></tbody></table></div>'},
    {title:'运行示例：合法对象如何在后两层失败',kind:'math',badge:'案例推演',lead:'模型输出 action=refund、amount=120，语法和 schema 都正确，为什么仍不能执行？',body:'<figure class="dd-fig"><svg viewBox="0 0 710 260" role="img" aria-label="退款结构化输出经过语法、schema、业务事实和权限四道验证门"><defs><marker id="so1" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 z" fill="#6b7484"/></marker></defs><rect x="16" y="85" width="135" height="88" rx="8" fill="#21252d" stroke="#6b8cbe"/><text x="83" y="109" text-anchor="middle" class="svg-t">模型对象</text><text x="83" y="131" text-anchor="middle" class="svg-t" font-size="11">action=refund</text><text x="83" y="150" text-anchor="middle" class="svg-t" font-size="11">amount=120</text><path d="M151,129 L188,129" stroke="#6b7484" marker-end="url(#so1)"/><rect x="190" y="85" width="112" height="88" rx="8" fill="#21252d" stroke="#4f9d78"/><text x="246" y="110" text-anchor="middle" class="svg-t">语法 ✓</text><text x="246" y="132" text-anchor="middle" class="svg-t" font-size="11">完整 JSON</text><text x="246" y="151" text-anchor="middle" class="svg-t" font-size="11">无截断</text><path d="M302,129 L339,129" stroke="#6b7484" marker-end="url(#so1)"/><rect x="341" y="85" width="112" height="88" rx="8" fill="#21252d" stroke="#4f9d78"/><text x="397" y="110" text-anchor="middle" class="svg-t">Schema ✓</text><text x="397" y="132" text-anchor="middle" class="svg-t" font-size="11">枚举合法</text><text x="397" y="151" text-anchor="middle" class="svg-t" font-size="11">0≤amount≤500</text><path d="M453,129 L490,129" stroke="#6b7484" marker-end="url(#so1)"/><rect x="492" y="57" width="196" height="70" rx="8" fill="#21252d" stroke="#c77b72"/><text x="590" y="82" text-anchor="middle" class="svg-t">业务/事实 ✕</text><text x="590" y="104" text-anchor="middle" class="svg-t" font-size="11">订单实付 ¥80，可退上限 ¥80</text><rect x="492" y="145" width="196" height="70" rx="8" fill="#21252d" stroke="#c77b72"/><text x="590" y="170" text-anchor="middle" class="svg-t">权限 ✕</text><text x="590" y="192" text-anchor="middle" class="svg-t" font-size="11">当前角色只能建议，不能批准</text><path d="M590,127 L590,143" stroke="#6b7484" marker-end="url(#so1)"/></svg><figcaption>图 1　格式保证只让错误更容易检测；数据库事实和当前主体权限仍决定能否执行。</figcaption></figure><div class="dd-formula">可退金额 = min(实付金额−已退款，政策上限) = min(80−0,100)=80</div><p>amount=120 在 schema 的通用 0–500 范围内，却违反订单不变量。正确恢复是返回 <code>amount_exceeds_remaining</code>，让模型改为解释或转人工；不是偷偷把 120 截成 80 后执行，因为这改变了原意且可能掩盖上游错误。</p>'},
    {title:'约束生成与生成后校验是互补的',kind:'math',badge:'机制',lead:'如果服务端最终都会校验，为什么还要生成时约束？',body:'<p>约束解码减少语法/schema 无效输出和无效重试；服务端校验防实现缺陷、版本错配和语义错误。受限采样把非法 token 置零并重新归一化：</p><div class="dd-formula">P′(t|s) ∝ P(t|s)·1[t∈合法集合 M(s)]</div><p>当模型原本大部分概率压在非法候选上，约束会迫使它选择低概率合法值，格式成功却可能内容变差。因此要同时看首次结构通过率、业务正确率和任务质量。若底层 API 不支持某些 schema 特性，应在启动时契约测试并生成后复验，而非默认相信声明。</p><div class="dd-note warn"><b>不要用自动修复掩盖分布问题。</b>　“修 JSON”模型可能改变字段含义；只有纯语法且可证明等价的修复才适合自动应用。</div>'},
    {title:'失败恢复要局部、有限且无副作用',kind:'eng',badge:'可靠性',lead:'校验失败后，把整个错误对象原样塞回模型无限重试会发生什么？',body:'<p>会增加延迟、成本、提示注入面和重复动作。返回机器生成的字段路径、错误代码与允许范围，不回显不可信大段文本；限制重试次数。第一次校验前绝不执行副作用，重试复用请求 ID 和幂等键。</p><ol class="dd-steps"><li>纯语法失败：约束重生或一次修复。</li><li>缺必填且可向用户询问：返回 ask_clarification。</li><li>事实/业务冲突：重新取权威数据，不让模型猜。</li><li>权限不足：禁止执行，必要时升级人工。</li><li>同类连续失败：降级更简单 schema 或确定性表单。</li></ol><p>记录首次通过率、每字段错误、修复后通过、最终任务失败、重试次数和额外 p95。</p>'},
    {title:'版本演进是一项分布式契约',kind:'eng',badge:'版本',lead:'生产者升级 schema 后，旧消费者为什么可能静默误读？',body:'<p>schema 需要版本号和兼容政策。新增可选字段通常向后兼容，但消费者若拒绝额外字段则不是；新增枚举值会让旧 switch 落入错误默认分支；删除/改类型是破坏性变化。采用 expand/contract：先让消费者接受新旧，再让生产者发送新，最后移除旧。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>变更</th><th>典型风险</th><th>迁移</th></tr></thead><tbody><tr><td>新增可选字段</td><td>严格消费者拒绝</td><td>先更新消费者</td></tr><tr><td>新增枚举值</td><td>未知值误执行</td><td>显式 unknown 分支</td></tr><tr><td>字段改名/类型</td><td>破坏读写</td><td>双写/双读后退役</td></tr><tr><td>收紧范围</td><td>旧数据不再合法</td><td>迁移数据并分版本校验</td></tr></tbody></table></div><p>提示、schema、约束编译器和消费者版本要一起记录；回放旧 trace 才能复现当时规则。</p>'},
    {title:'安全执行不信任模型提供的身份与工具名',kind:'eng',badge:'安全',lead:'schema 已把 action 限定为 refund，为什么提示注入仍可能造成伤害？',body:'<p>攻击者可诱导模型选择语法允许但不合适的动作或金额。工具注册表只暴露当前流程允许的能力；服务器从会话认证取得 user/tenant，不接受模型自报；参数做对象级授权、速率、金额、目的与审批校验。高风险动作先生成计划，由确定性策略门和人工确认后提交。</p><p>自由文本字段可能携带 SQL、HTML 或给下一个 agent 的注入，消费者必须按上下文转义并标记不可信。结构化不等于净化；它只是把攻击载荷放进了一个合法字符串。</p><div class="dd-note warn"><b>Confused deputy：</b>模型可以代表用户建议动作，但不能继承服务账号全部权限。每次工具调用都以最小权限和当前主体重新鉴权。</div>'},
    {title:'评测应沿整条契约定位失败',badge:'评测',lead:'“结构化输出成功率 99.9%”还缺哪些能决定上线的数字？',body:'<p>按层报告：完整语法率、schema 首次通过、死路、字段级错误、业务/事实通过、权限拦截、工具成功、重复副作用、最终任务质量、额外延迟和成本。用断流、Unicode、超长数组、未知枚举、旧消费者、新政策和恶意自由文本回归。</p><p>错误预算要基于最终后果。大量无害格式重试和一次越权退款不能被同一平均失败率对待；后者设独立硬门槛和即时回滚。</p><div class="dd-note"><b>诊断顺序：</b>先问输出是否完整，再问形状是否正确，再问事实/关系是否成立，最后问当前主体是否能安全执行。</div>'},
    {title:'常见误区与概念依赖',badge:'常见误区与学习路线',lead:'结构化输出的价值是建立可拒绝的边界，而不是让模型输出天然可信。',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>误区</th><th>更准确的理解</th></tr></thead><tbody><tr><td>JSON mode 等于 schema</td><td>前者常只保证语法，后者约束字段和类型</td></tr><tr><td>schema 通过等于内容正确</td><td>事实和跨字段不变量需外部验证</td></tr><tr><td>自动修复总比失败好</td><td>修复可能改语义，应有限且可证明</td></tr><tr><td>加字段总是兼容</td><td>严格消费者和枚举分支可能破坏</td></tr><tr><td>合法参数就有权执行</td><td>执行层仍需主体鉴权、策略和幂等</td></tr></tbody></table></div><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>层级</th><th>概念依赖与延伸学习</th></tr></thead><tbody><tr><td>先修</td><td>JSON/Schema、tokenization、API 契约</td></tr><tr><td><b>本页核心</b></td><td>最小 schema、分层验证、恢复、版本迁移</td></tr><tr><td>生成机制</td><td>约束解码、采样参数、流式输出</td></tr><tr><td>执行安全</td><td>工具调用、身份权限、工作流编排与应用评测</td></tr></tbody></table></div>'}
  ],
  chain:['定义最小版本化 schema','生成时约束并等待完整接受态','服务端重新解析与 schema 校验','验证事实和跨字段业务不变量','以当前主体鉴权后幂等执行','按失败层记录并有限恢复/回滚'],
  quiz:[{q:'JSON 语法通过与 schema 通过差什么？',a:'前者只保证可解析，后者还检查字段、类型、枚举和范围。'},{q:'amount=120 为什么示例中不能执行？',a:'虽在通用范围内，但订单剩余可退额只有 80，且当前角色无批准权限。'},{q:'为什么约束生成后仍要校验？',a:'防实现/版本错配，并处理业务、事实和权限等文法无法表达的条件。'},{q:'新增枚举为何可能破坏旧消费者？',a:'旧 switch 可能没有安全 unknown 分支。'},{q:'失败重试如何避免重复退款？',a:'校验全部通过前不执行，执行与重试使用同一幂等键。'}],
  sources:[{title:'JSON Schema Specification',url:'https://json-schema.org/specification',note:'JSON 数据契约与验证语义'},{title:'PICARD',url:'https://arxiv.org/abs/2109.05093',note:'生成过程中拒绝非法 token'},{title:'JSONSchemaBench',url:'https://arxiv.org/abs/2501.10868',note:'约束解码覆盖与效率评测'},{title:'OWASP API Security Top 10',url:'https://owasp.org/API-Security/',note:'对象授权、资源与接口安全边界'}]
});
