window.DEEPDIVE = window.DEEPDIVE || {};
window.DEEPDIVE["tokenization"] = {
  title: "Token 与分词：模型真正读写的离散语言",
  subtitle: "从 Unicode、预切分、BPE/Unigram、字节回退到特殊 token，理解同一句文本为何会变成不同长度、不同成本和不同能力边界。",
  thesis: "Tokenizer 是权重之前的一台<b>确定性编译器</b>：它把原始 Unicode 字符串变成 token ID，再由嵌入矩阵查表。词表顺序、规范化、特殊 token 或聊天模板一旦改变，ID 的含义就改变；因此 tokenizer 不是可随意更换的前处理插件，而是模型接口和权重的一部分。",
  html: `
<div class="dd-goals"><div class="dd-goals-h">读完这一页，你应该能自己回答：</div><ul><li>字符、字节、子词和 token 为什么不是同一件事？</li><li>BPE 怎样从字符序列逐步学出子词？</li><li>词表变大为何能缩短序列，却不一定让模型更强？</li><li>为什么换一个 tokenizer 会让原有嵌入权重失去意义？</li><li>怎样测试 Unicode、特殊 token、偏移量和流式停止边界？</li></ul></div>
<div class="dd-note key"><b>先抓住接口：</b>模型接收的不是“猫”“cat”或一个汉字，而是整数序列。可见文本相同，只要规范化或切分不同，就可能得到不同 ID；ID 相同，只要词表重排，也会查到完全不同的向量。</div>

<section class="dd-sec"><h2><span class="dd-n">1</span>一段文字要经过哪几道门<span class="dd-badge intuition">全景</span></h2><p class="dd-lead">从用户按键到嵌入向量，中间不是一次简单的“按词切开”。</p>
<figure class="dd-fig"><svg viewBox="0 0 760 250" role="img" aria-label="从 Unicode 文本到嵌入向量的分词流水线"><defs><marker id="tok-arrow" markerWidth="8" markerHeight="8" refX="7" refY="4" orient="auto"><path d="M0,0 L8,4 L0,8Z" fill="currentColor"/></marker></defs><g fill="none" stroke="currentColor" stroke-width="2" marker-end="url(#tok-arrow)"><path d="M130 112H180"/><path d="M290 112H340"/><path d="M450 112H500"/><path d="M610 112H660"/></g><g stroke="currentColor" stroke-width="2"><rect x="20" y="70" width="110" height="84" rx="12" fill="#8b5cf6" opacity=".22"/><rect x="180" y="70" width="110" height="84" rx="12" fill="#0ea5e9" opacity=".22"/><rect x="340" y="70" width="110" height="84" rx="12" fill="#10b981" opacity=".22"/><rect x="500" y="70" width="110" height="84" rx="12" fill="#f59e0b" opacity=".22"/><rect x="660" y="70" width="80" height="84" rx="12" fill="#ef4444" opacity=".22"/></g><g class="svg-t" text-anchor="middle"><text x="75" y="103">Unicode</text><text x="75" y="129">原始文本</text><text x="235" y="103">规范化</text><text x="235" y="129">预切分</text><text x="395" y="103">BPE /</text><text x="395" y="129">Unigram</text><text x="555" y="103">词表查 ID</text><text x="555" y="129">加特殊符号</text><text x="700" y="103">嵌入</text><text x="700" y="129">向量</text></g></svg><figcaption>每一步都必须版本化；“大致相同”的实现不够，因为一个边界差异会改变后续全部 ID。</figcaption></figure>
<p>规范化可能统一某些等价字符，预切分常利用空格或正则建立候选边界，子词算法再在边界内组合，最后查词表并按模板插入 BOS、EOS、角色或工具 token。不同模型可选择不同管线，不能从屏幕上的字符数推断 token 数。</p></section>

<section class="dd-sec"><h2><span class="dd-n">2</span>字符、字节、子词与 ID 分别是什么<span class="dd-badge intuition">消歧</span></h2><p class="dd-lead">“一个字就是一个 token”为什么只在偶然情况下成立？</p>
<table class="dd-table"><thead><tr><th>层级</th><th>例子</th><th>保证</th><th>不保证</th></tr></thead><tbody><tr><td>Unicode 码点</td><td><code>猫</code>、<code>é</code></td><td>抽象字符编号</td><td>一个视觉字形只含一个码点</td></tr><tr><td>UTF-8 字节</td><td><code>猫</code> 编为 3 字节</td><td>任意 Unicode 可编码</td><td>边界与人类字符一致</td></tr><tr><td>子词片段</td><td><code>token</code>+<code>ization</code></td><td>有限词表可组合开放文本</td><td>片段本身有完整语义</td></tr><tr><td>token ID</td><td><code>31415</code></td><td>可索引嵌入矩阵某一行</td><td>跨 tokenizer 仍表示同一片段</td></tr></tbody></table>
<p>组合音标、emoji 肤色修饰符和零宽连接符尤其容易让“可见字符数”失效。可靠系统应保留原文与字符偏移的映射，而不是把 token 边界冒充用户看到的字符边界。</p></section>

<section class="dd-sec"><h2><span class="dd-n">3</span>手算一次 BPE：高频相邻对怎样变成新符号<span class="dd-badge math">数值例子</span></h2><p class="dd-lead">BPE 学到的是合并规则，不是一张人工词典。</p><p>设玩具语料只含 <code>low</code> 5 次、<code>lower</code> 2 次。初始把每个词写成字符加词尾符 <code>&lt;/w&gt;</code>，并按词频统计相邻对：</p>
<table class="dd-table"><thead><tr><th>轮次</th><th>最高频相邻对</th><th>频次</th><th>合并后的表示</th></tr></thead><tbody><tr><td>0</td><td><code>l + o</code></td><td>7</td><td><code>lo w &lt;/w&gt;</code>；<code>lo w e r &lt;/w&gt;</code></td></tr><tr><td>1</td><td><code>lo + w</code></td><td>7</td><td><code>low &lt;/w&gt;</code>；<code>low e r &lt;/w&gt;</code></td></tr><tr><td>2</td><td><code>low + &lt;/w&gt;</code></td><td>5</td><td><code>low&lt;/w&gt;</code>；<code>low e r &lt;/w&gt;</code></td></tr></tbody></table>
<p>第三轮优先把完整的 <code>low</code> 合成一个词尾 token，因为它出现 5 次；<code>lower</code> 仍可由 <code>low</code>、<code>e</code>、<code>r</code> 等组合。真实训练会有预切分、并列频次规则和实现细节，但“反复加入最有压缩收益的相邻合并”这一机制不变。</p></section>

<section class="dd-sec"><h2><span class="dd-n">4</span>BPE、Unigram 与字节级方案在优化什么<span class="dd-badge math">方法</span></h2><p class="dd-lead">它们都产生子词，但搜索方向不同。</p>
<table class="dd-table"><thead><tr><th>方法</th><th>训练思路</th><th>编码特点</th><th>典型风险</th></tr></thead><tbody><tr><td>BPE</td><td>从小符号集向上合并</td><td>按固定合并序列得到确定切分</td><td>贪心规则未直接优化语言模型目标</td></tr><tr><td>Unigram</td><td>从大候选词表向下剪枝</td><td>按片段概率选择高似然切分，可采样多种切分</td><td>概率模型与剪枝实现更复杂</td></tr><tr><td>字节级子词</td><td>以 256 个字节为完备底座再合并</td><td>没有真正的未知字符</td><td>低覆盖语言或罕见串可能很长</td></tr></tbody></table>
<p>“无未知 token”只说明能编码，不说明编码高效。一个罕见字符即使能回退为多个字节 token，也会占用更多上下文位置，并让模型从更碎的证据中学习组合。</p></section>

<section class="dd-sec"><h2><span class="dd-n">5</span>词表大小是一笔参数—序列长度交换<span class="dd-badge math">权衡</span></h2><p class="dd-lead">词表翻倍，模型付出和节省的分别是什么？</p><div class="dd-formula">嵌入参数量 ≈ |V| × d；注意力配对数 ≈ L²</div><p>词表 <code>|V|</code> 变大，常见片段更可能由一个 token 表示，平均序列长度 <code>L</code> 可能缩短；但输入嵌入和输出分类矩阵随词表增长，罕见 token 获得的更新也更少。若输入输出权重共享，参数账会不同，但覆盖与稀疏训练的权衡仍在。</p>
<div class="dd-note warn"><b>不能只比较“每字符 token 数”。</b> 同时报告词表参数、实际语料长度分布、吞吐、下游质量和语言切片；压缩率更高不等于语义学习一定更好。</div></section>

<section class="dd-sec"><h2><span class="dd-n">6</span>分词效率为什么会影响多语言公平性<span class="dd-badge eng">覆盖</span></h2><p class="dd-lead">相同语义的信息，为何不同语言可能消耗不同上下文预算？</p><p>词表是从训练语料频率中学出的。高资源语言的常见词形更容易成为长 token，低资源语言可能被拆成字符或字节。可用“fertility（每词或每字符 token 数）”与压缩率按语言统计，但还要结合形态结构、书写系统和任务质量，不能把某个语言天然较多 token 直接解释为歧视。</p>
<table class="dd-table"><thead><tr><th>要测什么</th><th>为什么</th><th>避免的误判</th></tr></thead><tbody><tr><td>每语言长度分布</td><td>估计成本与截断概率</td><td>只看全局平均</td></tr><tr><td>数字、姓名、代码切片</td><td>检查复制与边界能力</td><td>只测自然段落</td></tr><tr><td>任务准确率随 token 长度</td><td>分离语言与长度因素</td><td>把所有差异归因于 tokenizer</td></tr></tbody></table></section>

<section class="dd-sec"><h2><span class="dd-n">7</span>特殊 token 和聊天模板为何属于模型协议<span class="dd-badge eng">接口</span></h2><p class="dd-lead">同一句用户问题，加上不同角色标记为什么会得到不同结果？</p><p>预训练或指令微调会让特定 ID 承担“序列开始”“用户开始”“助手开始”“工具结果”等控制含义。聊天模板决定这些 ID 的顺序、换行和结束边界。漏掉助手前缀、重复 BOS，或把 EOS 当普通文本，都会让模型进入训练中不熟悉的状态。</p><p>新增 token 还需要扩展嵌入和输出矩阵并训练新行；仅把字符串登记到词表，不会自动赋予概念。模板、tokenizer 文件、词表哈希与权重应作为一个发布单元。</p></section>

<section class="dd-sec"><h2><span class="dd-n">8</span>数字、代码和复制任务为什么对边界敏感<span class="dd-badge intuition">能力</span></h2><p class="dd-lead">分词不会决定模型全部能力，却会改变要学习的最短路径。</p><p>若长数字按不稳定长度片段切分，逐位运算和复制需要在片段内部恢复位结构；若缩进、换行或运算符边界被合并，代码模型要同时学习内容和隐藏边界。相反，把每个字符都拆开会显著增加长度。合理设计应通过真实任务验证，而不是凭“更像人类词语”判断。</p><p>生成时 token 也是解码单位：流式输出可能暂时只有不完整 UTF-8 字节，停止串也可能跨 token 边界。服务端应在字节解码和停止匹配上处理跨块状态。</p></section>

<section class="dd-sec"><h2><span class="dd-n">9</span>Unicode 和偏移量怎样制造静默故障<span class="dd-badge eng">安全</span></h2><p class="dd-lead">屏幕看起来相同的文本，底层序列可能为什么不同？</p><p>预组字符 <code>é</code> 与 <code>e</code> 加组合重音可以视觉相同却由不同码点组成；全角字符、不可见控制符和跨文字系统的形似字符也可能绕过字符串规则。规范化可减少部分差异，但也可能改变要求逐字保真的任务，因此必须明确采用何种 Unicode 形式。</p><p>标注系统常以字符、UTF-16 代码单元或 UTF-8 字节保存偏移；tokenizer 又输出自己的 offset mapping。混用单位会让实体高亮、训练标签和审计证据错位，而且通常不会报错。</p></section>

<section class="dd-sec"><h2><span class="dd-n">10</span>怎样做一次可复现的 tokenizer 验收<span class="dd-badge eng">检查表</span></h2><p class="dd-lead">能把普通英文编码再解码，不代表接口已经可靠。</p><ol class="dd-steps"><li><b>固定工件：</b>保存词表、合并规则/模型、规范化、预切分、特殊 token、模板和哈希。</li><li><b>做往返测试：</b>普通文本、空串、换行、组合字符、emoji、CJK、RTL、代码和随机字节。</li><li><b>做黄金 ID 测试：</b>固定若干字符串及其精确 ID，跨语言实现逐项比对。</li><li><b>验证 offset：</b>把 token 偏移重新映射到原文，检查组合字符和代理对。</li><li><b>验证模板：</b>打印完整聊天 ID，确认角色、BOS/EOS 和工具边界只出现预期次数。</li><li><b>统计分布：</b>按语言和任务报告 token 长度、截断率、未知/字节回退和延迟。</li><li><b>验证流式：</b>随机切分输出块，确认 UTF-8 解码和停止串检测结果不变。</li></ol></section>

<section class="dd-sec"><h2><span class="dd-n">11</span>把整条因果链连起来<span class="dd-badge intuition">综合</span></h2><ol class="dd-chain"><li>用户产生 Unicode 字符串，其中可能包含组合字符和不可见符号。</li><li>规范化与预切分确定候选边界。</li><li>BPE、Unigram 或字节级规则把文本覆盖成有限词表片段。</li><li>词表把片段映射到整数 ID，模板插入控制 token。</li><li>嵌入矩阵按 ID 查行，模型只对这条离散序列计算。</li><li>切分粒度改变序列长度、训练频率和要学习的组合路径。</li><li>解码与流式服务把 token 恢复为字节和可见文本。</li><li>任一工件或边界规则变化，都必须与权重共同验证和发布。</li></ol></section>

<section class="dd-sec"><h2><span class="dd-n">12</span>常见误解<span class="dd-badge intuition">消歧</span></h2><table class="dd-table"><thead><tr><th>误解</th><th>更准确的说法</th></tr></thead><tbody><tr><td>token 就是词</td><td>token 可是词、子词、字符、字节片段或控制符。</td></tr><tr><td>字符数等于 token 数</td><td>Unicode 组合、语料覆盖和合并规则都会改变比例。</td></tr><tr><td>字节回退表示所有语言同样高效</td><td>它只保证可编码，罕见串仍可能被拆得很长。</td></tr><tr><td>词表越大越好</td><td>序列可能变短，但参数、稀疏更新和迁移成本会上升。</td></tr><tr><td>只要文本相同就能替换 tokenizer</td><td>ID 与嵌入行绑定，模板和特殊 token 也参与训练协议。</td></tr></tbody></table></section>

<section class="dd-sec"><h2><span class="dd-n">13</span>检查你是否真的理解<span class="dd-badge intuition">自测</span></h2><ol class="dd-quiz"><li>在玩具 BPE 中，为什么 <code>l+o</code> 的频次是 7？</li><li>字节级方案为什么没有未知字符，却仍可能让某些语言更昂贵？</li><li>词表从 32k 增至 64k 时，至少哪两项资源会向相反方向变化？</li><li>为什么只修改特殊 token 的 ID 就可能破坏模型？</li><li>实体标注偏移正确但高亮错位，你会优先检查哪些单位？</li></ol><details class="dd-answers"><summary>参考答案</summary><ol><li><code>low</code> 出现 5 次、<code>lower</code> 出现 2 次，两者开头都有该相邻对。</li><li>任意文本能由字节覆盖，但低覆盖片段可能需要多个字节 token，增加长度与截断。</li><li>嵌入/输出矩阵参数通常增大，而平均序列长度可能减小；最终算力变化需实测。</li><li>该 ID 对应已训练嵌入和控制语义；重排后模型会读到另一行向量。</li><li>原文偏移究竟按码点、UTF-16 单元还是 UTF-8 字节，以及规范化前后是否混用。</li></ol></details></section>

<section class="dd-sec"><h2><span class="dd-n">14</span>概念依赖与延伸学习<span class="dd-badge eng">路线</span></h2><table class="dd-table"><thead><tr><th>方向</th><th>接下来读</th><th>关键问题</th></tr></thead><tbody><tr><td>ID 如何变向量</td><td><a href="#" data-node="embedding">嵌入</a></td><td>离散索引怎样通过训练获得几何关系？</td></tr><tr><td>顺序如何进入模型</td><td><a href="#" data-node="positional-encoding">位置编码</a></td><td>token 已切好后，模型怎样知道先后和距离？</td></tr><tr><td>长度为何昂贵</td><td><a href="#" data-node="self-attention">自注意力</a></td><td>序列长度怎样进入时间与显存复杂度？</td></tr><tr><td>协议怎样训练</td><td><a href="#" data-node="instruction-tuning">指令微调</a></td><td>角色和回答边界怎样成为监督信号？</td></tr><tr><td>上线怎样守住边界</td><td><a href="#" data-node="model-serving">模型服务</a></td><td>流式解码、截断和版本如何监控？</td></tr></tbody></table><div class="dd-note key"><b>过关标准</b>　给你一段多语言文本和一个模型包，你能画出从原始字节到 ID 的每一步，手算一次 BPE 合并，并列出更换词表、模板或规范化时必须重验的接口。</div></section>

<div class="dd-src"><b>资料来源与改编说明</b><ul><li><a href="https://arxiv.org/abs/1508.07909" target="_blank" rel="noopener">Neural Machine Translation of Rare Words with Subword Units</a>：BPE 子词方法。</li><li><a href="https://aclanthology.org/D18-2012/" target="_blank" rel="noopener">SentencePiece: A simple and language independent subword tokenizer and detokenizer</a>：原始文本上的 BPE/Unigram 实现。</li><li><a href="https://arxiv.org/abs/1909.03341" target="_blank" rel="noopener">Neural Machine Translation with Byte-Level Subwords</a>：字节级子词建模。</li><li><a href="https://www.unicode.org/reports/tr15/" target="_blank" rel="noopener">Unicode Standard Annex #15: Unicode Normalization Forms</a>：Unicode 规范化形式。</li></ul><p>流水线图、BPE 数值例子、工程检查表和对照表均为本项目原创组织。</p><div class="dd-src-date">访问日期：2026-07-22</div></div>`
};
