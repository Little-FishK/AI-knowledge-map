/* 人工复核后的章节六问合同。
 * 本文件必须晚于自动迁移层执行；合同中的证据均来自对应章节正文。
 */
(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["image-generation"];
  if (!page) return;

  // 本页正文已经逐章完成六问，不再显示迁移期自动追加的重复摘要。
  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const item = (answer, evidence) => ({ answer, evidence });
  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      {
        section: 1,
        definition: item(
          "图像生成是一种按照文字、参考图等条件创造图像候选的任务，不是图库检索。",
          "所谓图像生成，可以理解为“按条件创造图像候选”",
        ),
        problem: item(
          "它解决没有现成图片时，如何得到符合描述的新画面这一问题。",
          "它解决的是“没有现成图片，却希望得到符合描述的新画面”这一问题",
        ),
        inputOutput: item(
          "输入是文字及可选的参考图、草图或姿势，输出是一张或多张像素图像。",
          "参考图、草图或姿势",
        ),
        mechanism: item(
          "系统先编码输入条件，再采样新的内部表示，最后把该表示解码为可见图像。",
          "系统先把输入条件转换成内部表示，再根据训练中学到的图像规律采样一个新表示",
        ),
        interpretation: item(
          "结果只表示模型认为画面与条件相容，不能证明画面中的事件真实发生。",
          "生成结果表示“模型认为这种画面与条件相容”",
        ),
        boundary: item(
          "模型可能复现训练片段，生成新图不能证明绝对原创、事实正确或可无限制使用。",
          "模型仍可能记住并复现训练片段",
        ),
      },
      {
        section: 2,
        definition: item(
          "两阶段生成是一个把文字要求先变成条件表示、再形成画面的处理流程。",
          "两阶段生成描述了从“理解要求”到“形成画面”的完整过程",
        ),
        problem: item(
          "它用于解决文字含义与图像像素之间不能直接一一对应的问题。",
          "用于解决文字含义与图像像素之间不能直接一一对应的问题",
        ),
        inputOutput: item(
          "输入是提示词和可选控制条件，中间输出是条件表示，最终输出是图像候选。",
          "提示词和可选控制条件",
        ),
        mechanism: item(
          "系统先编码文字，再由生成引擎形成图像表示，然后解码并逐项验收。",
          "系统先把“柯基、帽子、海边、油画”编码成条件表示，再让生成引擎依照这些条件形成图像表示",
        ),
        interpretation: item(
          "整体相似只说明大方向可能对齐，不能证明对象数量与空间关系都正确。",
          "某个候选整体上很像提示词，只说明大方向可能对齐",
        ),
        boundary: item(
          "两阶段图示用于建立直觉，但不同模型不一定采用完全相同的固定部件。",
          "不同模型会把编码、生成和解码组合成不同结构",
        ),
      },
      {
        section: 3,
        definition: item(
          "扩散模型是一种通过反复去噪来生成数据的图像生成引擎。",
          "扩散模型指的是通过反复去噪来生成数据的引擎",
        ),
        problem: item(
          "它解决如何从随机起点得到既自然又符合文字条件的图像。",
          "它解决的是“怎样从随机起点得到既自然又符合文字要求的图像”",
        ),
        inputOutput: item(
          "输入是随机噪声、去噪步数和文字条件，每一步输出更有结构的状态，最终输出图片。",
          "随机噪声、当前去噪步数和文字条件",
        ),
        mechanism: item(
          "系统先预测符合目标的变化，再去掉部分噪声，然后重复处理直至形成画面。",
          "系统先预测当前噪声中哪些变化更像目标画面，再去掉一部分噪声",
        ),
        interpretation: item(
          "一次结果表示某个随机起点下的候选，并不是提示词的唯一标准答案。",
          "一次运行得到的是随机起点下的一个候选",
        ),
        boundary: item(
          "更多步骤不能保证计数、文字或事实正确，扩散也不是图像生成的唯一路线。",
          "增加去噪步数不一定持续提升质量",
        ),
      },
      {
        section: 4,
        definition: item(
          "可控生成是给文生图增加姿势、深度、线稿等结构约束的工作方式。",
          "可控生成所做的，是把结构约束加到文生图过程里",
        ),
        problem: item(
          "它用于解决提示词正确但位置、姿势或局部细节仍不可控的问题。",
          "用于解决“文字说得对，但位置、姿势或局部细节仍不可控”的问题",
        ),
        inputOutput: item(
          "输入是提示词与结构条件或遮罩，输出是同时服从语义与结构约束的图像。",
          "姿势骨架、深度图、线稿或局部遮罩",
        ),
        mechanism: item(
          "流程先拆分条件，再约束构图，然后生成候选，最后局部重绘错误区域。",
          "先把模糊愿望拆成可检查条件，再用结构图约束整体构图",
        ),
        interpretation: item(
          "若多个候选在同处失败，说明更像条件不清、约束冲突或模型覆盖不足。",
          "若四个候选都在同一位置失败，更像是条件表达不清、约束冲突或模型没有学好这种组合",
        ),
        boundary: item(
          "冲突条件无法同时完全服从，局部重绘也可能意外改变邻近区域。",
          "控制条件越多并不一定越好",
        ),
      },
      {
        section: 5,
        definition: item(
          "能力边界评测是一种按照失败类型系统检查模型的测试方法。",
          "能力边界评测记录模型在不同失败类型上的表现",
        ),
        problem: item(
          "它用于发现展示图无法揭示的、真实任务中可重复出现的能力短板。",
          "用于解决“展示图很好看，却不知道系统在真实任务中会在哪里失灵”的问题",
        ),
        inputOutput: item(
          "输入是分维度测试提示和采样次数，输出是各类错误的成功率、失败率与案例。",
          "覆盖文字、计数、空间关系、一致性和事实性的测试提示",
        ),
        mechanism: item(
          "评测先设计固定提示，再统一重复生成，然后按规则标错，最后比较维度和版本。",
          "先为每个高风险维度设计一组固定提示，再让同一模型按统一分辨率和采样预算重复生成",
        ),
        interpretation: item(
          "单张成功只说明该次采样成功，重复失败才说明存在稳定的能力短板。",
          "看到一张成功图，只说明该次采样成功",
        ),
        boundary: item(
          "清单不能永久代表所有模型；版本、语言、画幅或工具变化后必须重新测量。",
          "这份清单不能永久代表所有模型",
        ),
      },
      {
        section: 6,
        definition: item(
          "多维评估是一套把模糊的图像质量拆成多个独立问题的验收方法。",
          "多维评估比较的是“质量”的不同侧面，而不是寻找一个万能总分",
        ),
        problem: item(
          "它用于避免好看的画面掩盖贴题错误、身份漂移和安全风险。",
          "用于避免一张好看的图掩盖贴题错误、身份漂移或安全风险",
        ),
        inputOutput: item(
          "输入是固定提示、模型版本、种子、预算和评分规则，输出是分维度结果与失败案例。",
          "固定提示集、模型版本、随机种子策略、采样预算和人工评分规则",
        ),
        mechanism: item(
          "评估先固定测试条件，再重复生成，然后自动筛查并人工盲评，最后汇总风险。",
          "先固定提示、分辨率和采样次数，再对每条提示重复生成",
        ),
        interpretation: item(
          "某维度更高只表示该维度在当前测试条件下更好，不能推出整体更好。",
          "某个维度更高，只表示该维度在这组测试条件下更好",
        ),
        boundary: item(
          "自动指标不覆盖全部语义，人工评价也有偏差，安全失败不能被高分抵消。",
          "自动指标难覆盖构图偏好和高风险语义",
        ),
      },
      {
        section: 7,
        definition: item(
          "生成图像治理是一套覆盖生成前、生成中与发布后的风险控制流程。",
          "生成图像治理，可以理解为贯穿生成前、生成中和发布后的风险控制",
        ),
        problem: item(
          "它用于降低版权侵害、冒充真人、诈骗和虚假传播等风险。",
          "用于降低版权侵害、冒充真人、诈骗和虚假传播",
        ),
        inputOutput: item(
          "输入是请求、人物与素材、授权和用途，输出是允许、限制、拒绝和复核记录。",
          "用户请求、涉及的人物与素材、授权状态和使用场景",
        ),
        mechanism: item(
          "流程先核查身份授权，再限制高风险生成，然后记录来源并人工复核敏感内容。",
          "生成前先检查身份、授权与用途，生成时限制明显高风险请求",
        ),
        interpretation: item(
          "水印只表示某套工具留下来源线索，检测器的结果也只是概率判断。",
          "水印存在只能说明某套工具留下了来源线索",
        ),
        boundary: item(
          "水印和检测器都可能失效，技术检测不能替代法律判断与治理责任。",
          "压缩、截图或新模型都可能使它失效",
        ),
      },
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["reinforcement-learning"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");
  const bridges = [
    `<p>强化学习是让智能体通过连续交互学习决策策略的框架，用于处理动作会改变未来情境、没有逐步标准答案的问题。每一步输入当前状态，策略输出动作或动作概率，环境再返回奖励和下一状态；训练流程先收集这类转移，再估计长期结果，最后更新策略并重复交互。高回报表示策略在当前环境与奖励定义下表现较好，但状态遗漏、奖励错位或环境变化都可能让结论失效。</p>`,
    `<p>折扣回报是把当前以后的一串奖励合成单一决策目标的量，解决即时得分与长期后果冲突的问题。输入是未来奖励序列和折扣因子，输出从当前时刻开始的加权总和；计算时先按距离乘上 <code>γ</code> 的幂，再把各步奖励相加。回报较大表示按当前时间偏好更值得，但不同 γ、时间单位和终止规则下的数字不能直接比较，也不能证明奖励本身代表真实价值。</p>`,
    `<p>价值函数是特定策略下未来回报的期望估计，Bellman 递推则把长远价值拆成眼前奖励与下一状态价值。它们用于解决轨迹尚未结束时怎样评价当前状态或动作的问题：输入状态或状态—动作对，输出 V 或 Q；更新先观察一步转移，再用 <code>r+γV(s′)</code> 修正当前估计。价值较高表示在当前策略与环境下长期回报预期较高，不是事实标签；抽样稀少、分布外动作和函数近似都可能产生偏差。</p>`,
    `<p>探索—利用权衡是一种在当前收益与信息收集之间做选择的决策：利用选择当前估计最好的动作，探索则尝试可能改变未来选择的证据不足动作。它输入动作价值、不确定性和风险约束，输出本步动作；选择时先评估已有收益与证据，再由 ε-greedy 留出随机机会、熵奖励保持概率分散，或让 UCB 把收益与不确定性相加后比较。探索后估计排序改变，说明新证据纠正了旧判断；高风险或不可逆环境中不能直接在线试错，探索必须转移到模拟、离线或审批范围内。</p>`,
    `<p>价值法、策略梯度与 Actor–Critic 描述的是三种利用经验改进策略的计算路线。它们输入状态、动作、回报或价值估计，输出动作价值、策略概率和更新后的参数；价值法先估 Q 再选动作，策略梯度按优势调整概率，Actor–Critic 用 Critic 提供基线。回报上升且 KL、熵和独立任务指标稳定，表示当前更新在多项证据下有效；动作空间、估计偏差和奖励质量会限制方法，PPO 的小步约束也不等于安全保证。</p>`,
    `<p>奖励与环境共同定义了训练所能“看见”的世界，是强化学习目标的边界而非附属设置。输入是奖励规则、状态表示、转移和权限，输出是策略能够优化的经验分布；策略先在这些规则下行动，再寻找能提高回报的行为模式。训练回报上升只表示更擅长当前代理，若真实解决率、安全切片或副作用恶化，就应解释为目标错位；遗漏后果、模拟差距和不可逆动作都不能靠继续训练自动补回。</p>`,
    `<p>退款案例是一条完整的信用分配演算：输入订单状态、两个候选动作、两步奖励和折扣因子，输出各动作回报与首步选择。计算先列出即时奖励，再把后续奖励折扣回首个动作，最后比较 −6.8 与 6.2；先核验胜出表示延迟后果足以推翻即时满意。这个结论依赖状态包含权限和证据、奖励数值合理；关键信息缺失或后果漏记，都可能让 TD 更新更高效地学习错误策略。</p>`,
    `<p>安全强化学习工作流是把探索、离线估计、策略更新和上线闸门放进同一风险控制过程，用于降低危险探索、离线外推和策略突变的风险。它输入历史日志、模拟器、安全动作集、候选策略和监控指标，输出受约束策略及放行或回滚决定；先在离线或沙箱学习，再用保守估计和小流量验证，随后持续监控 KL、行为与真实指标。小步更新和离线高分只表示局部证据较好，不能证明长期安全；分布外动作、非平稳环境和奖励投机不能只靠算法分数处理，仍要求人工与硬约束。</p>`,
  ];
  let coreIndex = 0;
  page.html = page.html.replace(
    /<section class="dd-sec" data-section-role="core">[\s\S]*?<\/section>/g,
    (section) => section.replace("</section>", `${bridges[coreIndex++]}</section>`),
  );

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section, definition, problem, inputOutput, mechanism, interpretation, boundary,
  });
  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("强化学习是智能体通过连续交互学习决策策略的框架。", "强化学习是让智能体通过连续交互学习决策策略的框架"),
        item("它用于处理动作改变未来且没有逐步标准答案的决策问题。", "用于处理动作会改变未来情境、没有逐步标准答案的问题"),
        item("输入是状态，策略输出动作，环境输出奖励与下一状态。", "每一步输入当前状态，策略输出动作或动作概率，环境再返回奖励和下一状态"),
        item("训练流程先收集转移，再估计长期结果，最后更新策略并重复交互。", "训练流程先收集这类转移，再估计长期结果，最后更新策略"),
        item("高回报表示策略在当前环境和奖励定义下表现较好。", "高回报表示策略在当前环境与奖励定义下表现较好"),
        item("状态遗漏、奖励错位和环境变化都可能使结论失效。", "都可能让结论失效")),
      contract(2,
        item("折扣回报是把当前以后多步奖励合成一个目标的量。", "折扣回报是把当前以后的一串奖励合成单一决策目标的量"),
        item("它解决即时奖励与长期后果发生冲突时怎样比较动作的问题。", "解决即时得分与长期后果冲突的问题"),
        item("输入是奖励序列和折扣因子，输出是加权总回报。", "输入是未来奖励序列和折扣因子，输出从当前时刻开始的加权总和"),
        item("先按时间距离乘折扣幂，再把各步奖励相加。", "先按距离乘上 γ 的幂，再把各步奖励相加"),
        item("回报较大表示按当前时间偏好更值得选择。", "回报较大表示按当前时间偏好更值得"),
        item("不同折扣、时间单位与终止规则的数值不能直接比较。", "不能直接比较，也不能证明奖励本身代表真实价值")),
      contract(3,
        item("价值函数描述特定策略下未来回报的期望估计。", "价值函数是特定策略下未来回报的期望估计"),
        item("它用于解决轨迹未结束时怎样评价当前状态或动作的问题。", "用于解决轨迹尚未结束时怎样评价当前状态或动作的问题"),
        item("输入是状态或状态动作对，输出是 V 或 Q 价值。", "输入状态或状态—动作对，输出 V 或 Q"),
        item("先观察一步转移，再用奖励加折扣后继价值更新当前估计。", "先观察一步转移，再用 r+γV(s′) 修正当前估计"),
        item("高价值表示当前策略环境下的长期回报预期较高。", "价值较高表示在当前策略与环境下长期回报预期较高"),
        item("价值不是事实，稀疏样本、分布外动作与近似都会造成偏差。", "不是事实标签；抽样稀少、分布外动作和函数近似")),
      contract(4,
        item("探索—利用权衡是一种在收益获取与信息收集之间做选择的决策。", "探索—利用权衡是一种在当前收益与信息收集之间做选择的决策"),
        item("它解决当前估计最优可能因样本不足而长期锁死的问题。", "可能改变未来选择的证据不足动作"),
        item("输入是价值、不确定性和风险约束，输出是本步动作。", "输入动作价值、不确定性和风险约束，输出本步动作"),
        item("选择时先评估收益与证据，再用随机机会、策略熵或不确定性奖励决定动作。", "选择时先评估已有收益与证据，再由 ε-greedy"),
        item("估计排序改变表示新证据修正了旧判断。", "估计排序改变，说明新证据纠正了旧判断"),
        item("高风险不可逆场景不能直接在线探索，必须使用受限环境。", "不能直接在线试错")),
      contract(5,
        item("价值法、策略梯度和 Actor–Critic 是改进策略的三类计算路线。", "描述的是三种利用经验改进策略的计算路线"),
        item("它们用于把经验中的长期回报转化为策略改进。", "利用经验改进策略"),
        item("输入是状态、动作、回报或价值，输出价值、概率及参数。", "输入状态、动作、回报或价值估计，输出动作价值、策略概率和更新后的参数"),
        item("价值法先估 Q，策略梯度按优势更新，Actor–Critic 提供价值基线。", "价值法先估 Q 再选动作，策略梯度按优势调整概率"),
        item("回报与独立指标共同改善表示当前策略更新较为有效。", "表示当前更新在多项证据下有效"),
        item("动作空间、估计偏差和奖励质量会限制算法，PPO 不保证安全。", "PPO 的小步约束也不等于安全保证")),
      contract(6,
        item("奖励与环境共同描述训练目标和智能体可见后果的边界。", "奖励与环境共同定义了训练所能“看见”的世界"),
        item("本节用于识别代理奖励与现实目标错位的问题。", "强化学习目标的边界"),
        item("输入是奖励、状态、转移与权限，输出是可优化经验分布。", "输入是奖励规则、状态表示、转移和权限，输出是策略能够优化的经验分布"),
        item("策略先按规则行动，再寻找能够提高回报的行为模式。", "策略先在这些规则下行动，再寻找能提高回报的行为模式"),
        item("回报升而真实指标降表示目标已经错位。", "若真实解决率、安全切片或副作用恶化，就应解释为目标错位"),
        item("遗漏后果、模拟差距和不可逆动作不能靠训练自动补回。", "都不能靠继续训练自动补回")),
      contract(7,
        item("退款案例是把延迟奖励归因到首个动作的信用分配演算。", "退款案例是一条完整的信用分配演算"),
        item("它解决即时满意与后续越权损失应怎样共同决定动作的问题。", "延迟后果足以推翻即时满意"),
        item("输入是状态、动作、两步奖励和折扣，输出是回报与选择。", "输入订单状态、两个候选动作、两步奖励和折扣因子，输出各动作回报与首步选择"),
        item("先列即时奖励，再折扣后续奖励，最后比较两个回报。", "先列出即时奖励，再把后续奖励折扣回首个动作，最后比较"),
        item("先核验胜出表示长期后果推翻了短期收益。", "先核验胜出表示延迟后果足以推翻即时满意"),
        item("状态缺失或奖励漏记可能让 TD 更快学到错误策略。", "都可能让 TD 更新更高效地学习错误策略")),
      contract(8,
        item("安全强化学习工作流是结合离线学习、受控验证和上线闸门的过程。", "安全强化学习工作流是把探索、离线估计、策略更新和上线闸门放进同一风险控制过程"),
        item("它用于降低危险探索、离线外推和策略突变风险。", "用于降低危险探索、离线外推和策略突变的风险"),
        item("输入是日志、模拟器、安全集、策略与指标，输出策略和放行决定。", "输入历史日志、模拟器、安全动作集、候选策略和监控指标，输出受约束策略及放行或回滚决定"),
        item("先离线或沙箱学习，再保守估计与小流量验证，随后持续监控。", "先在离线或沙箱学习，再用保守估计和小流量验证，随后"),
        item("小步更新与离线高分只表示当前局部证据较好。", "只表示局部证据较好"),
        item("分布外动作、非平稳环境和奖励投机不能只靠算法分数处理。", "不能只靠算法分数处理")),
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["unsupervised-learning"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");
  const bridges = [
    `<p>无监督学习可以概括为：在没有人工逐条答案时，用研究者选定的目标寻找候选结构。它接收无标签样本，输出分组、低维表示、密度模型或生成模型；流程先选择距离、重建或概率等假设，再优化对应目标，最后用稳定性和用途验证结果。目标值变好只表示更符合这套假设，不能证明算法发现了唯一真实结构。</p>`,
    `<p>这四类任务描述无标签数据可以产生的四种不同结果：聚类、降维、密度估计和生成建模；这种划分用于回答“我们究竟想从无标签数据得到什么”。共同输入是样本集合，输出分别是簇或归属概率、低维坐标、位置密度以及可生成的新样本；计算目标时，先让聚类优化组内相似、降维保留选定信息，再让密度与生成建模拟合分布。结果应按原问题解释，不能用漂亮的二维图替代分组价值，也不能把高密度直接理解为高质量。</p>`,
    `<p>这个数值例子展示的是尺度怎样偷偷改变相似度：输入为用户的购买次数和金额，输出是原始、标准化或业务加权后的距离。计算先逐维求差，再平方、求和并开平方；标准化则先把每维差异换成相对自身波动的单位。距离主要由金额决定，只说明它的数值范围较大；无论原始尺度还是 z-score 都带有权重假设，不能自动代替业务判断。</p>`,
    `<p>这幅图是一项表示敏感性检查，用于识别候选结构是否依赖特征尺度。它以同一批点和两套缩放方式为输入，输出两种几何布局；先改变各维对距离的贡献，再观察分组或邻居怎样随之变化。结构明显改变表示算法正在执行不同几何假设，而不是某一幅图必然错误；二维投影和少量点只提供线索，仍需外部用途与重复实验。</p>`,
    `<p>降维是把多维特征转换成少量新坐标的表示方法：输入每个样本的多维特征，输出少量新坐标和可选的近似重建。计算时，PCA 先中心化数据，再寻找保留总体方差最大的正交方向并投影；t-SNE 与 UMAP 则更重视原空间近邻在图上仍靠近。PCA 坐标表示样本沿投影方向的位置，而非线性图中的岛主要是邻域线索；任何方法都会丢信息，也都不能仅凭二维间距宣布天然类别。</p>`,
    `<p>密度估计把“哪里常见”变成可查询的分布模型，用于异常复核、生成或缺失值补全。输入是无标签样本，输出是给新位置计算密度或抽取样本的模型；训练先为样本计算密度，再汇总对数似然并调整参数。较低密度表示记录在训练分布下少见，不表示错误、欺诈或低价值；连续密度还依赖单位、模型族和分布稳定性，跨口径数值不能直接比较。</p>`,
    `<p>自监督与传统无监督的区别在于监督信号怎样产生。输入是原始样本，经过遮盖、裁剪或配对后形成自动题目，输出是被隐藏内容或一致表示；训练先从数据构造目标，再像普通预测任务一样计算损失。较低预测损失表示模型更会完成这道自动题，不能保证表示适合所有下游任务；是否把自监督归入无监督属于分类口径，说明信号来源比争论名称更重要。</p>`,
    `<p>无标签评测链是由内部指标、稳定性和外部或下游验证组成的证据组合，解决“没有标准标签时如何比较候选聚类”的问题。它输入数据、距离、分组及多次重跑结果，输出三层评测证据；先算轮廓系数检查当前几何，再扰动数据与参数检查复现，最后验证专家语义或任务收益。高轮廓系数只表示组内近、组间远，稳定只表示不易随扰动消失，两者都不能单独证明分组值得使用。</p>`,
    `<p>伪结构是采集来源或处理流程造成、却被误读为目标语义的候选结构。排查时输入簇归属、设备、时间、地区和缺失模式等元数据，输出混杂变量预测力与分层对照结果；先用探针判断来源能否预测簇，再做重采样或反事实替换。来源变量解释力很强表示当前结构可能在复现采集流程，但探针相关也不等于已经找到全部因果机制；高影响决策还必须接受公平、合法与人工复核。</p>`,
    `<p>版本化工作流把一次探索变成可以复现、比较和撤回的部署对象。它接收冻结的数据、特征、目标、随机种子和验证证据，输出带版本的模型、簇映射及监控规则；先记录口径并比较候选方案，再以稳定性和外部价值选择，重训后重新匹配簇。编号变化、分裂或合并表示结构版本发生变化，不能把旧簇号当永久身份；数据漂移或用途改变后，原验证结论也必须重新建立。</p>`,
  ];
  let coreIndex = 0;
  page.html = page.html.replace(
    /<section class="dd-sec" data-section-role="core">[\s\S]*?<\/section>/g,
    (section) => section.replace("</section>", `${bridges[coreIndex++]}</section>`),
  );

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section, definition, problem, inputOutput, mechanism, interpretation, boundary,
  });
  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("无监督学习是在没有人工逐条答案时按选定目标寻找候选结构的方法。", "无监督学习可以概括为"),
        item("它用于解决大量无标签样本怎样形成可分析结构的问题。", "在没有人工逐条答案时"),
        item("输入是无标签样本，输出是分组、表示、密度或生成模型。", "接收无标签样本，输出分组、低维表示、密度模型或生成模型"),
        item("先选择结构假设，再优化目标，最后验证稳定性和用途。", "先选择距离、重建或概率等假设，再优化对应目标，最后"),
        item("目标变好表示结果更符合当前结构假设。", "目标值变好只表示更符合这套假设"),
        item("它不能证明算法发现了唯一真实结构。", "不能证明算法发现了唯一真实结构")),
      contract(2,
        item("这四类任务描述无标签数据能够产生的不同结果。", "这四类任务描述无标签数据可以产生的四种不同结果"),
        item("这一区分用于回答想从无标签数据得到何种结果。", "用于回答“我们究竟想从无标签数据得到什么”"),
        item("输入都是样本，输出分别为簇、低维坐标、密度和新样本。", "共同输入是样本集合，输出分别是簇或归属概率、低维坐标"),
        item("计算目标时先分别优化组内相似或信息保留，再由密度和生成方法拟合分布。", "计算目标时，先让聚类优化组内相似"),
        item("每种输出都应回到原任务用途解释。", "结果应按原问题解释"),
        item("二维图和高密度都不能直接替代质量或价值判断。", "不能用漂亮的二维图替代分组价值")),
      contract(3,
        item("特征尺度是在距离中改变各维贡献大小的隐含权重。", "尺度怎样偷偷改变相似度"),
        item("本例用于解释金额数值为何可能淹没次数差异。", "展示的是尺度怎样偷偷改变相似度"),
        item("输入是次数和金额，输出是不同处理下的距离。", "输入为用户的购买次数和金额，输出是原始、标准化或业务加权后的距离"),
        item("先逐维求差，再平方求和并开方；标准化先换成相对波动单位。", "先逐维求差，再平方、求和并开平方"),
        item("金额主导距离只表示其数值范围较大。", "只说明它的数值范围较大"),
        item("原始尺度与标准化都含权重假设，不能代替业务判断。", "不能自动代替业务判断")),
      contract(4,
        item("表示敏感性检查是比较同一数据在不同特征尺度下结构变化的方法。", "这幅图是一项表示敏感性检查"),
        item("它用于识别候选结构是否依赖特征尺度。", "用于识别候选结构是否依赖特征尺度"),
        item("输入是同一批点和多套缩放，输出是相应几何布局。", "以同一批点和两套缩放方式为输入，输出两种几何布局"),
        item("先改变各维距离贡献，再观察分组与邻居变化。", "先改变各维对距离的贡献，再观察"),
        item("结构改变表示算法执行了不同几何假设。", "表示算法正在执行不同几何假设"),
        item("二维少量点只提供线索，不能代替外部和重复验证。", "二维投影和少量点只提供线索")),
      contract(5,
        item("降维是把多维特征转换成较少新坐标的表示方法。", "降维是把多维特征转换成少量新坐标的表示方法"),
        item("它解决高维数据难以压缩、观察或交给后续模型的问题。", "输出少量新坐标"),
        item("输入是多维特征，输出是少量坐标和可选重建。", "输入每个样本的多维特征，输出少量新坐标和可选的近似重建"),
        item("计算时 PCA 先中心化再投影到最大方差方向，其他方法重点保持近邻。", "计算时，PCA 先中心化数据，再寻找"),
        item("PCA 坐标表示投影位置，非线性图中的岛主要表示邻域线索。", "PCA 坐标表示样本沿投影方向的位置"),
        item("降维会丢信息，二维距离不能证明天然类别。", "都不能仅凭二维间距宣布天然类别")),
      contract(6,
        item("密度估计是学习数据在空间中集中程度的分布建模任务。", "密度估计把“哪里常见”变成可查询的分布模型"),
        item("它用于异常复核、样本生成和缺失值补全。", "用于异常复核、生成或缺失值补全"),
        item("输入是无标签样本，输出是可查询密度或抽样的模型。", "输入是无标签样本，输出是给新位置计算密度或抽取样本的模型"),
        item("先算样本密度，再汇总对数似然并调整参数。", "先为样本计算密度，再汇总对数似然并调整参数"),
        item("低密度表示样本在训练分布下较少见。", "较低密度表示记录在训练分布下少见"),
        item("少见不等于错误或低价值，密度还依赖单位、模型和分布。", "不表示错误、欺诈或低价值")),
      contract(7,
        item("自监督学习是从原始样本自动构造预测目标的训练方式。", "自监督与传统无监督的区别在于监督信号怎样产生"),
        item("它解决缺少人工标签时仍需要明确训练答案的问题。", "经过遮盖、裁剪或配对后形成自动题目"),
        item("输入是变换后的样本，输出是隐藏内容或一致表示。", "输入是原始样本，经过遮盖、裁剪或配对后形成自动题目，输出"),
        item("先从数据构造目标，再按预测任务计算损失。", "先从数据构造目标，再像普通预测任务一样计算损失"),
        item("低预测损失表示模型更会完成当前自动任务。", "较低预测损失表示模型更会完成这道自动题"),
        item("它不能保证表示适合所有下游任务，分类归属也有口径差异。", "不能保证表示适合所有下游任务")),
      contract(8,
        item("无标签评测链是由内部指标、稳定性与外部或下游证据组成的组合。", "无标签评测链是由内部指标、稳定性和外部或下游验证组成的证据组合"),
        item("它解决没有标准标签时怎样比较候选聚类的问题。", "解决“没有标准标签时如何比较候选聚类”的问题"),
        item("输入是数据、距离、分组和重跑结果，输出是三层评测证据。", "它输入数据、距离、分组及多次重跑结果，输出三层评测证据"),
        item("先算内部几何，再检查扰动复现，最后验证语义和任务收益。", "先算轮廓系数检查当前几何，再扰动数据与参数检查复现，最后"),
        item("高轮廓系数表示当前距离下组内近、组间远。", "高轮廓系数只表示组内近、组间远"),
        item("内部高分和稳定性都不能单独证明分组有实际价值。", "都不能单独证明分组值得使用")),
      contract(9,
        item("伪结构是采集或处理因素造成、却被误读为目标语义的结构。", "伪结构是采集来源或处理流程造成"),
        item("排查用于避免把设备、批次或缺失模式误当目标群体。", "被误读为目标语义"),
        item("输入是簇与来源元数据，输出是探针和分层对照证据。", "输入簇归属、设备、时间、地区和缺失模式等元数据，输出"),
        item("先用探针预测簇，再重采样或做反事实替换。", "先用探针判断来源能否预测簇，再做重采样或反事实替换"),
        item("来源解释力强表示结构可能主要复现采集流程。", "表示当前结构可能在复现采集流程"),
        item("探针相关不能证明完整因果，高影响用途仍需治理。", "不等于已经找到全部因果机制")),
      contract(10,
        item("版本化工作流是把结构发现结果变成可复现部署对象的方法。", "版本化工作流把一次探索变成可以复现、比较和撤回的部署对象"),
        item("它解决重训后结果含义和簇身份怎样可靠延续的问题。", "可以复现、比较和撤回"),
        item("输入是冻结配置与证据，输出是版本模型、簇映射和监控规则。", "接收冻结的数据、特征、目标、随机种子和验证证据，输出"),
        item("先记录并比较方案，再选择版本，重训后重新匹配簇。", "先记录口径并比较候选方案，再以稳定性和外部价值选择"),
        item("编号变化、分裂或合并表示结构版本已经变化。", "表示结构版本发生变化"),
        item("旧簇号不能作为永久身份，漂移后必须重新验证。", "不能把旧簇号当永久身份")),
    ],
    termReviews: [
      ...(Array.isArray(page.quality?.termReviews)
        ? page.quality.termReviews.filter((review) => Number(review.section) !== 5)
        : []),
      {
        section: 5,
        reviewedAt: "2026-07-27",
        terms: [
          {
            name: "协方差矩阵",
            meaning: "概括多个特征怎样共同变化的矩阵",
            purpose: "帮助 PCA 找到保留总体变化的方向",
            definitionEvidence: "概括各特征怎样一起变化",
            purposeEvidence: "各新坐标还保留多少变化",
          },
          {
            name: "局部邻域",
            meaning: "一个点附近最相似的一组点",
            purpose: "检查降维后原来的近邻是否仍靠近",
            definitionEvidence: "一个点周围最相近的若干点",
            purposeEvidence: "把这些近邻放到二维图中仍然靠近",
          },
          {
            name: "高维",
            meaning: "一个样本包含很多特征坐标",
            purpose: "说明近邻最初是在多特征空间中计算",
            definitionEvidence: "原数据拥有很多特征",
            purposeEvidence: "原始高维空间里有哪些近邻",
          },
          {
            name: "特征向量",
            meaning: "按顺序记录一个样本多个特征的数值列表",
            purpose: "作为投影与距离计算的统一输入",
            definitionEvidence: "表示某位用户的特征向量",
            purposeEvidence: "用户在这条方向上的一维坐标",
          },
          {
            name: "正交",
            meaning: "投影方向彼此垂直且不重复",
            purpose: "避免多个新坐标反复记录同一方向",
            definitionEvidence: "各方向长度为 1，并且彼此垂直",
            purposeEvidence: "避免重复记录同一个方向",
          },
          {
            name: "t-SNE",
            meaning: "重点保持局部近邻的非线性降维方法",
            purpose: "探索局部邻居和候选小群体",
            definitionEvidence: "t-SNE 和 UMAP 在做另一件事",
            purposeEvidence: "适合观察局部邻居和候选小群体",
          },
          {
            name: "UMAP",
            meaning: "强调局部邻接并兼顾部分较大尺度结构的降维方法",
            purpose: "探索邻域与连续变化",
            definitionEvidence: "局部邻接关系，并尝试保留部分较大尺度结构",
            purposeEvidence: "适合探索邻域和连续变化",
          },
        ],
      },
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["loss-function"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const bridges = [
    `<p>可以把这一节的闭环读成四步：把一次预测与目标交给损失函数，得到单样本损失；再按批次或任务汇总，得到优化器要降低的标量；优化器依据这个标量的局部变化调整参数；最后回到分样本、分群体和真实任务指标解释结果。因而“损失更低”只表示在当前数据、公式和权重下更符合所写规则，不能单独证明模型更准确、更公平或更有用。</p>`,
    `<p>这套记号的输入是训练样本、当前模型参数以及可选的正则强度，输出是一个供参数更新使用的总体目标值；这样做用于解决多个样本和复杂度约束怎样合成同一目标的问题。实际计算先逐样本得到损失，再在批次或训练集上平均，最后才加正则项；读数下降说明当前定义下的平均代价变小。它只能近似未知数据上的风险，样本偏差、错误标签和正则权重失当都可能让这个近似失真。</p>`,
    `<p>回归损失描述的是“误差怎样转成训练压力”，用于解决不同大小的回归误差应受到多强惩罚的问题。输入是预测值与连续目标，输出是每个样本的非负代价；计算时先让 MSE、MAE 和 Huber 分别采用平方、绝对值和分段规则，再把单样本代价汇总成训练目标。较低数值只在同一数据与同一公式下表示拟合更好；它不能判断离群点是否真实、业务是否对高估和低估同价，也不能自动决定 Huber 阈值。</p>`,
    `<p>因此交叉熵接收的是模型对各类别的概率分布和真实类别，输出是一个非负损失；计算时先读取真实类别对应的概率，再取负对数，最后跨样本平均。数值越小，表示模型平均把更多概率放在真实类别上，但不能保证概率已经校准，也不能保证少数类召回、事实正确或实际决策成本更优；标签噪声、类别权重和分布变化都会改变它的含义。</p>`,
    `<p>把两类目标分工后，流程就清楚了：训练阶段输入可频繁取得的样本与目标，输出可求导的代理损失并用它更新参数；验证与上线阶段再输入独立样本、用户反馈和安全记录，输出真实任务指标。代理下降而真实指标不升，意味着两者发生了错位，而不是继续压低代理就会自然修复；延迟反馈、混杂因素和用户行为变化可能让这条代理关系失效。</p>`,
    `<p>这里的权重、掩码和正则项，是用来改写各训练信号相对重要性的控制量。从计算接口看，这一节输入的是单样本损失以及这些控制量，输出是重新组合后的训练目标，用来解决类别失衡、无效位置和复杂度约束怎样进入同一标量的问题。程序先删去掩码标记为无效的位置，再对保留项加权、归一化并加入正则；数值下降表示按这套重新定义的“重要性”计算出的代价变小。权重过大、归一化口径改变或掩码误删，都可能让少量样本支配训练，所以必须同时查看各分项和自然分布上的指标。</p>`,
    `<p>单步参数更新是把损失的局部斜率转换成一次参数移动的过程。这个微型实验解决的是“一个损失数怎样真的推动参数”这一问题：输入为样本 <code>x=2,y=6</code>、初始参数 <code>w=2</code>、半平方损失与学习率，输出为更新后的 <code>w=2.4</code> 和新损失 <code>0.72</code>。计算依次经过预测、误差、损失、梯度和更新；新损失更低表示这一步沿当前样本的局部下降方向移动。它只是单样本、单参数、单步演示，不能据此判断多步训练稳定或未见数据表现。</p>`,
    `<p>损失曲线是一种诊断视图，而不是质量证书：它以训练过程中的各项损失、验证指标和切片统计为输入，输出对拟合、失配或目标冲突的线索。解释时要先确认曲线口径一致，再比较训练与验证趋势，随后下钻到群体和子目标；平均值下降但关键切片恶化，表示整体结果仍然不合格。曲线噪声、数据泄漏和指标错位会限制诊断，不能只凭一条平滑下降线宣布成功。</p>`,
    `<p>这套选择流程的产物不是一个“永远正确的公式”，而是一份可审计的训练与验证方案，用于解决新任务中怎样选择代理并证明它仍服务真实目标的问题。它接收决策代价、输出语义、数据分布和部署约束；执行时先澄清代价和输出语义，再选择代理并完成手算与微型过拟合，最后独立验证并上线监控，输出损失定义、权重口径和验收指标。只有代理与真实指标在关键切片上共同改善时，结果才表示方案暂时有效；任务、用户或分布改变后，原方案不能直接沿用，必须重新验证。</p>`,
  ];

  let coreIndex = 0;
  page.html = page.html.replace(
    /<section class="dd-sec" data-section-role="core">[\s\S]*?<\/section>/g,
    (section) => {
      const bridge = bridges[coreIndex++];
      return bridge ? section.replace("</section>", `${bridge}</section>`) : section;
    },
  );

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section,
    definition,
    problem,
    inputOutput,
    mechanism,
    interpretation,
    boundary,
  });

  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("损失函数把预测与目标之间的差异变成可比较、可优化的标量代价。", "把一次预测与目标交给损失函数，得到单样本损失"),
        item("它解决自然语言式的“答得不好”无法直接指导大量参数更新的问题。", "得到优化器要降低的标量"),
        item("输入是预测、目标及汇总规则，输出是单样本损失和供优化使用的总体标量。", "再按批次或任务汇总"),
        item("先计算单样本损失，再汇总为标量，随后依据局部变化调整参数。", "优化器依据这个标量的局部变化调整参数"),
        item("损失下降表示在当前数据、公式和权重下更符合所写规则。", "只表示在当前数据、公式和权重下更符合所写规则"),
        item("它不能单独证明模型更准确、更公平或更有用。", "不能单独证明模型更准确、更公平或更有用")),
      contract(2,
        item("经验风险是有限训练样本上单样本损失的平均，总目标还可包含正则项。", "在批次或训练集上平均，最后才加正则项"),
        item("它用于解决怎样把多个样本和复杂度约束合成同一训练目标的问题。", "用于解决多个样本和复杂度约束怎样合成同一目标的问题"),
        item("输入是训练样本、当前模型参数和可选正则强度，输出是总体目标值。", "输入是训练样本、当前模型参数以及可选的正则强度"),
        item("逐样本计算损失，跨样本平均，再加入正则项。", "先逐样本得到损失，再在批次或训练集上平均"),
        item("读数下降表示当前定义下的平均代价变小。", "读数下降说明当前定义下的平均代价变小"),
        item("有限样本风险会受样本偏差、错误标签和正则强度影响。", "样本偏差、错误标签和正则权重失当都可能让这个近似失真")),
      contract(3,
        item("回归损失描述连续预测误差怎样转换成训练代价。", "回归损失描述的是"),
        item("它用于解决不同大小的回归误差应受到多强惩罚的问题。", "用于解决不同大小的回归误差应受到多强惩罚的问题"),
        item("输入是预测值与连续目标，输出是每个样本的非负代价。", "输入是预测值与连续目标，输出是每个样本的非负代价"),
        item("计算时先按平方、绝对值或分段规则得到单样本代价，再汇总训练。", "计算时先让 MSE、MAE 和 Huber 分别采用平方、绝对值和分段规则"),
        item("同一口径下更低的值表示预测更贴近目标。", "较低数值只在同一数据与同一公式下表示拟合更好"),
        item("公式不能判断离群点真伪、非对称业务代价，也不能自动决定 Huber 阈值。", "它不能判断离群点是否真实")),
      contract(4,
        item("分类交叉熵是根据真实类别所获概率计算的负对数损失。", "交叉熵接收的是模型对各类别的概率分布和真实类别"),
        item("它解决只判对错无法区分犹豫错误与自信错误的问题。", "模型平均把更多概率放在真实类别上"),
        item("输入是类别概率分布与真实类别，输出是非负分类损失。", "接收的是模型对各类别的概率分布和真实类别，输出是一个非负损失"),
        item("计算时先读取真实类概率，再取负对数，最后跨样本平均。", "计算时先读取真实类别对应的概率"),
        item("数值越小表示平均给真实类别的概率越高。", "数值越小，表示模型平均把更多概率放在真实类别上"),
        item("低交叉熵不能保证校准、少数类召回、事实正确或决策成本最优。", "不能保证概率已经校准")),
      contract(5,
        item("可微代理是可频繁计算并提供局部斜率、暂时代替真实目标的训练量。", "输出可求导的代理损失"),
        item("它解决真实目标延迟、稀疏或不可导，不能逐步训练的问题。", "训练阶段输入可频繁取得的样本与目标"),
        item("训练输入样本与目标并输出代理损失；验证输入反馈与记录并输出真实指标。", "验证与上线阶段再输入独立样本、用户反馈和安全记录"),
        item("先优化代理更新参数，再用独立验证和上线指标检查真实目标。", "用它更新参数；验证与上线阶段再输入"),
        item("代理下降但真实指标不升，表示两者发生目标错位。", "意味着两者发生了错位"),
        item("延迟反馈、混杂因素与用户行为变化可能让代理关系失效。", "可能让这条代理关系失效")),
      contract(6,
        item("权重、掩码和正则项是改写各训练信号相对重要性的控制量。", "是用来改写各训练信号相对重要性的控制量"),
        item("它们用于解决类别失衡、无效位置与复杂度约束怎样进入同一目标的问题。", "用来解决类别失衡、无效位置和复杂度约束怎样进入同一标量的问题"),
        item("输入是单样本损失和各控制量，输出是加权、归一并正则化后的训练目标。", "这一节输入的是单样本损失以及这些控制量，输出是重新组合后的训练目标"),
        item("先按掩码筛选，再加权归一，最后加入正则。", "先删去掩码标记为无效的位置，再对保留项加权、归一化并加入正则"),
        item("数值下降表示按重新定义的重要性计算出的代价变小，而非自然频率风险变小。", "数值下降表示按这套重新定义的“重要性”计算出的代价变小"),
        item("权重、归一化或掩码失当可能让少量样本错误支配训练。", "都可能让少量样本支配训练")),
      contract(7,
        item("单步参数更新是把损失对参数的局部斜率转换成一次参数移动的过程。", "单步参数更新是把损失的局部斜率转换成一次参数移动的过程"),
        item("它解决损失数值如何具体影响待学习参数的问题。", "损失数怎样真的推动参数"),
        item("输入是样本、初始参数、损失与学习率，输出是更新参数和新损失。", "输出为更新后的 w=2.4 和新损失 0.72"),
        item("依次计算预测、误差、损失、梯度，再按学习率更新。", "依次经过预测、误差、损失、梯度和更新"),
        item("新损失更低表示这一局部步骤沿当前样本的下降方向移动。", "表示这一步沿当前样本的局部下降方向移动"),
        item("单样本、单参数、单步结果不能证明长期稳定和泛化。", "不能据此判断多步训练稳定或未见数据表现")),
      contract(8,
        item("损失曲线是记录训练过程中损失与相关指标变化的诊断视图。", "损失曲线是一种诊断视图"),
        item("它用于发现过拟合、切片退化、子目标冲突和指标错位。", "对拟合、失配或目标冲突的线索"),
        item("输入是损失、验证指标与切片统计，输出是诊断线索。", "以训练过程中的各项损失、验证指标和切片统计为输入"),
        item("先核对口径，再比较训练验证趋势，然后下钻群体和子目标。", "先确认曲线口径一致，再比较训练与验证趋势，随后下钻"),
        item("平均损失下降而关键切片恶化，表示整体结果仍然不合格。", "平均值下降但关键切片恶化，表示整体结果仍然不合格"),
        item("噪声、数据泄漏和指标错位限制曲线能证明的结论。", "曲线噪声、数据泄漏和指标错位会限制诊断")),
      contract(9,
        item("损失选择流程是一份从决策代价到验证与监控的可审计方案。", "一份可审计的训练与验证方案"),
        item("它用于解决新任务中怎样选择代理并证明其服务真实目标的问题。", "用于解决新任务中怎样选择代理并证明它仍服务真实目标的问题"),
        item("输入是决策代价、输出语义、数据分布和部署约束，输出是损失、权重和验收指标。", "接收决策代价、输出语义、数据分布和部署约束"),
        item("执行时先澄清代价与输出语义，再选择代理和完成小实验，最后独立验证并监控。", "执行时先澄清代价和输出语义，再选择代理并完成手算与微型过拟合"),
        item("代理与真实指标在关键切片共同改善，才表示方案暂时有效。", "结果才表示方案暂时有效"),
        item("任务、用户或分布改变后，原方案不能直接沿用，必须重新验证。", "原方案不能直接沿用，必须重新验证")),
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["information-theory"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");
  const part = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section, definition, problem, inputOutput, mechanism, interpretation, boundary,
  });
  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        part("本节描述后续计算共同使用的概率对象和阅读规则。", "这一组约定描述了后续计算共同使用的"),
        part("这些约定用于避免混淆真实频率、模型预测和实际事件。", "用于避免把真实频率 P、模型预测 Q 和已经发生的事件 x 混为一谈"),
        part("输入是事件集合、P 与 Q，输出是概率量及按 P 加权的期望。", "输入是事件集合、P 与 Q，输出是可代入后续公式的概率和按 P 加权的期望"),
        part("计算先确认事件由 P 产生，再读取 Q 的概率，然后按 P 求平均。", "计算时先确认事件由 P 产生，再读取 Q 给该事件的概率，然后按 P 求长期平均"),
        part("结果应解释为给定分布下的理想长期平均量。", "结果只能解释为给定分布下的理想平均量"),
        part("现实 P 通常未知，样本频率可能含有抽样偏差。", "现实中的 P 通常未知，样本频率也可能有抽样偏差")),
      contract(2,
        part("自信息指的是一个实际结果发生后消除的不确定性。", "某个实际结果发生后消除的不确定性"),
        part("它解决如何量化单次结果意外程度的问题。", "解决“单次结果到底有多意外”的量化问题"),
        part("输入是事件及其真实概率，输出是以 bit 表示的自信息。", "输入是事件 x 及其真实概率 P(x)，输出是以 bit 表示的"),
        part("计算先取概率的二进制对数，再加负号得到非负信息量。", "先对概率取以 2 为底的对数，再加负号"),
        part("数值越大表示该结果在真实分布下越罕见。", "数值越大表示该结果在 P 下越罕见"),
        part("高自信息不能推出事件更重要、更有用或更正确。", "不能推出事件更重要、更有用或更正确")),
      contract(3,
        part("熵描述一个真实分布在重复试验中的平均不确定性。", "一个真实分布在重复试验中的平均不确定性"),
        part("它用于把不同事件的单次意外程度汇总为分布级指标。", "用于把不同事件的单次意外程度汇成一个分布级量"),
        part("输入是完整真实分布 P，输出是平均 bit 每次。", "输入是完整分布 P，输出是平均 bit/次"),
        part("计算先求各事件自信息，再按真实概率加权相加。", "计算先求每个事件的自信息，再按 P(x) 加权相加"),
        part("熵更高表示长期平均而言该分布更难预测。", "熵更高表示长期平均更难预测"),
        part("熵不能说明每个事件都罕见，也不能直接评价答案质量。", "却不表示每个事件都罕见，也不能直接评价答案质量")),
      contract(4,
        part("交叉熵描述数据按 P 出现而预测由 Q 给出时的平均对数损失。", "数据按 P 出现、预测却由 Q 给出时的平均对数损失"),
        part("它解决如何用一个数比较模型概率与真实事件的问题。", "它解决如何用一个数比较模型概率与真实事件的问题"),
        part("输入是 P 和 Q，输出是以 bit 每事件表示的交叉熵。", "输入是 P 和 Q，输出是 bit/事件的 H(P,Q)"),
        part("流程先由 P 决定频率，再读取 Q，随后计算负对数并加权平均。", "流程先让 P 决定事件频率，再读取 Q(x)，然后计算负对数并按 P 平均"),
        part("数值较低表示 Q 平均给真实事件分配了更高概率。", "数值较低表示 Q 给真实事件的平均概率较高"),
        part("不同分布、底数或预测单位下的交叉熵不能直接横向比较。", "不同数据分布、对数底数或预测单位下的数值不能直接横比")),
      contract(5,
        part("KL 散度描述使用 Q 代替 P 后增加的平均编码代价。", "使用 Q 代替 P 后增加的平均编码代价"),
        part("它用于区分数据自身不确定性与模型概率放错的额外损失。", "用于分离“数据本身的不确定性”和“模型概率放错位置的额外损失”"),
        part("输入是有方向的 P 与 Q，输出是对应方向的 KL 散度。", "输入是有方向的 P 与 Q，输出是 D"),
        part("计算先求交叉熵，再减去真实分布 P 自身的熵。", "计算先求交叉熵，再减去 P 自身的熵"),
        part("KL 为零表示 Q 在 P 会发生的事件上完全匹配。", "零表示 Q 在 P 会发生的事件上完全匹配"),
        part("KL 方向不可交换，未知 P、样本估计和零概率都会限制解释。", "方向不可交换，且样本估计、零概率和未知 P 都限制实际解释")),
      contract(6,
        part("本节比较的是几何坐标差与概率编码代价这两类不同问题。", "几何坐标差与概率编码代价是两种不同问题"),
        part("这种比较用于防止把有方向的 KL 散度误当作普通距离。", "用于防止把 KL 当作普通距离"),
        part("输入都是 P、Q；输出分别是对称几何长度和有方向对数代价。", "输入都是概率向量 P、Q；欧氏距离输出对称的几何长度"),
        part("欧氏距离先逐坐标求差平方再开方，KL 则按 P 加权对数概率比。", "前者先逐坐标求差、平方并求和，再开方"),
        part("两者较小都能表示某种接近，但单位和具体含义不同。", "数值较小都可表示某种接近，但单位和含义不同"),
        part("两个数不能互换或相加，也不能证明某种度量永远更好。", "不能互换、相加或据此宣称一种度量永远更好")),
      contract(7,
        part("困惑度表示把平均对数损失还原后的熵等效分支数。", "平均对数损失还原成等效分支数"),
        part("它用于把抽象的 bit 每 token 换成更容易理解的尺度。", "用于让抽象的 bit/token 更容易比较"),
        part("输入是注明底数的平均 token 损失，输出是 PPL。", "输入是注明对数底数的平均 token 损失，输出是 PPL"),
        part("计算先确认损失单位，再用与底数对应的指数函数还原。", "先确认单位，再用对应指数函数还原"),
        part("更低 PPL 表示模型平均给真实 token 分配了更高概率。", "更低的 PPL 表示模型平均给真实 token 更高概率"),
        part("困惑度不代表真实候选数、事实性或推理能力，跨分词器也不可直比。", "但不代表候选数、事实性或推理能力")),
      contract(8,
        part("完整手算展示同一组 P、Q 如何产生熵、交叉熵、KL 和困惑度。", "同一组 P、Q 怎样依次产生熵、交叉熵、KL 和困惑度"),
        part("它用于核对五个信息量之间的角色和计算链是否连贯。", "用来核对五个量的角色是否连贯"),
        part("输入是三种天气的 P 与 Q，输出是四个带单位的汇总量。", "输入是三种天气的 P 与 Q，输出是四个带单位的汇总量"),
        part("计算先由 P 求熵，再求交叉熵，然后相减并指数化。", "计算先由 P 求熵，再用 P 加权 Q 的码长，然后相减得到 KL"),
        part("结果说明 Q 低估雪的概率造成了主要额外编码代价。", "结果说明 Q 低估雪造成主要额外代价"),
        part("三事件例子只验证算术链，不能代表连续分布和真实语言模型。", "不能代表连续分布、有限样本估计或真实语言模型")),
      contract(9,
        part("经验交叉熵描述有限文本样本上的平均负对数概率。", "有限文本样本上的平均负对数概率"),
        part("它用于在真实分布 P 未知时提供可计算的训练信号。", "用于在未知 P 时提供可计算的训练信号"),
        part("输入是 token 序列和逐步概率，输出是序列或批次平均损失。", "输入是 token 序列及模型逐步概率，输出是序列或批次平均损失"),
        part("计算先累加真实 token 负对数概率，再除以有效 token 数并跨样本平均。", "先累加真实 token 的负对数概率，再除以有效 token 数并跨样本平均"),
        part("训练值下降表示模型更贴合当前训练样本。", "训练值下降表示更贴合当前样本"),
        part("训练和验证损失都有抽样与分布误差，不能替代事实、安全和任务评测。", "但二者都有抽样和分布误差，也不能替代事实、安全与任务评测")),
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["supervised-learning"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");
  const item = (answer, evidence) => ({ answer, evidence });
  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      {
        section: 1,
        definition: item(
          "监督学习描述利用配对的输入和目标样本来学习预测规则的训练范式。",
          "利用“输入—目标”样本学习预测规则",
        ),
        problem: item(
          "它用于解决怎样根据过去样本对未见输入作出可检验预测的问题。",
          "面对一封训练时没见过的新邮件，怎样依据过去样本给出可检验的判断",
        ),
        inputOutput: item(
          "输入是邮件及其标签，输出是能够对新邮件产生类别或数值预测的模型。",
          "邮件内容与对应标签",
        ),
        mechanism: item(
          "训练先产生预测，再与标签比较，然后根据误差调整规则并反复执行。",
          "先让模型预测训练样本，再把预测与标签比较",
        ),
        interpretation: item(
          "垃圾概率零点八表示当前模型更偏向垃圾类别，不表示标签或结论必然正确。",
          "表示它在当前规则下更偏向垃圾类别",
        ),
        boundary: item(
          "标签可能错误，换到另一邮件来源时模型也可能不再可靠。",
          "不能证明标签绝对正确",
        ),
      },
      {
        section: 2,
        definition: item(
          "分类与回归描述监督学习输出的两种基本形态：离散类别与连续数值。",
          "监督学习输出的两种基本形态",
        ),
        problem: item(
          "这种区分用于避免把类别、类别概率和连续数值混作同一种预测目标。",
          "用来避免把类别、概率和连续数值混用",
        ),
        inputOutput: item(
          "输入是样本特征和训练目标；分类输出类别或概率，回归输出连续数值。",
          "分类输出类别或各类别概率",
        ),
        mechanism: item(
          "两类任务都先预测，再用匹配的损失比较目标，最后调整模型。",
          "训练时都先产生预测，再用与任务匹配的损失比较预测和目标",
        ),
        interpretation: item(
          "分类结果说明样本更符合哪个类别，回归结果说明估计数值的大小。",
          "分类结果说明样本更符合哪个预先定义的类别",
        ),
        boundary: item(
          "任务类型不能只按答案表面是数字还是文字决定，还取决于预测对象和错误代价。",
          "不能只凭“看起来像数字还是文字”机械决定任务类型",
        ),
      },
      {
        section: 3,
        definition: item(
          "参数学习指的是从候选参数中寻找训练样本平均损失较小的一组参数。",
          "从候选参数中寻找平均损失较小的一组",
        ),
        problem: item(
          "它解决如何把预测与标签的不一致转化成可优化目标的问题。",
          "让模型在训练样本上的平均错误尽量小",
        ),
        inputOutput: item(
          "输入是训练样本、当前参数和损失规则，输出是更新参数及其预测函数。",
          "训练样本、当前参数和损失规则",
        ),
        mechanism: item(
          "训练先计算预测，再计算样本损失，然后汇总平均值并更新参数。",
          "先用当前参数计算预测，再计算每个样本的损失",
        ),
        interpretation: item(
          "平均损失下降表示当前样本上更贴近标签，不表示所有样本都正确或已经泛化。",
          "平均损失下降表示模型在当前样本和损失定义下更贴近标签",
        ),
        boundary: item(
          "二分类小批次只解释概率到损失，不能替代梯度推导和独立测试。",
          "不能替代后续的梯度推导或测试集验收",
        ),
      },
      {
        section: 4,
        definition: item(
          "泛化评估描述模型在未参与训练与选择的数据上是否依然有效。",
          "模型在未参与训练和选择的数据上是否仍然有效",
        ),
        problem: item(
          "它解决训练集成绩无法代表模型真实使用效果的问题。",
          "它解决训练成绩无法代表真实使用效果的问题",
        ),
        inputOutput: item(
          "输入是冻结模型、独立样本及目标，输出是损失、精确率、召回率和切片证据。",
          "冻结的模型、独立样本及其目标",
        ),
        mechanism: item(
          "流程先训练参数，再用验证集选方案，最后在方案冻结后使用测试集。",
          "先用训练集拟合参数，再用验证集选择方案",
        ),
        interpretation: item(
          "训练误差下降而验证误差上升，说明模型更像在记忆训练细节。",
          "说明模型更像在记忆训练细节",
        ),
        boundary: item(
          "一次高分仍依赖样本代表性、时间稳定性和无泄漏前提，不能证明未来都可靠。",
          "一次测试高分仍受样本代表性、时间变化和数据泄漏影响",
        ),
      },
      {
        section: 5,
        definition: item(
          "标注瓶颈记录可靠目标信号在成本、质量和覆盖范围上的限制。",
          "可靠目标信号在成本、质量和覆盖上的限制",
        ),
        problem: item(
          "它用于判断一个监督数据方案能否支撑真实部署，而不只看标签数量。",
          "用来判断一个监督方案能否支撑真实部署",
        ),
        inputOutput: item(
          "输入是标注来源、抽样范围、一致性和部署场景，输出是噪声与覆盖缺口。",
          "标注来源、抽样范围、一致性结果和部署场景",
        ),
        mechanism: item(
          "先抽样复核一致性，再比较人群、时间和来源覆盖，随后检查目标是否一致。",
          "先抽样复核标注一致性，再比较不同人群、时间和来源的覆盖",
        ),
        interpretation: item(
          "高一致率表示标注规则较稳定，但不表示目标本身没有偏差。",
          "高一致率表示标注规则较稳定",
        ),
        boundary: item(
          "自动日志便宜但可能不可信，专家复核准确却可能昂贵缓慢。",
          "自动日志便宜也不代表可信",
        ),
      },
      {
        section: 6,
        definition: item(
          "四种范式比较描述训练信号的来源以及学习过程最终产出的对象。",
          "训练信号从哪里来、学习后产出什么",
        ),
        problem: item(
          "它用于在缺少人工标签时选择与数据和反馈条件相符的学习范式。",
          "用于在没有人工标签时选择合适范式",
        ),
        inputOutput: item(
          "输入是可用数据、反馈和交互条件，输出是学习路线及对应模型、表示或策略。",
          "任务可获得的数据、反馈方式和交互条件",
        ),
        mechanism: item(
          "判断先看外部目标，再看能否自造目标，最后看是否需要行动获得延迟奖励。",
          "先看是否有外部目标值，再看数据能否自行构造目标",
        ),
        interpretation: item(
          "范式名称表示主要监督来源，不表示一个实际系统只能使用其中一种。",
          "分类结果说明主要监督来源",
        ),
        boundary: item(
          "自监督和强化学习系统都可能组合监督训练，传统范式边界并非绝对互斥。",
          "边界允许组合",
        ),
      },
      {
        section: 7,
        definition: item(
          "大模型时代的监督学习可以理解为用高质量目标信号约束通用模型行为。",
          "用高质量目标信号约束通用模型的具体行为",
        ),
        problem: item(
          "它解决预训练模型能力广泛却未必按照具体任务要求作答的问题。",
          "它解决的是预训练能力很广却未必按任务要求作答的问题",
        ),
        inputOutput: item(
          "输入是指令与理想回答或领域样本与标签，输出是监督微调模型或任务预测器。",
          "指令与理想回答、领域样本与标签",
        ),
        mechanism: item(
          "应用先定义行为，再收集输入目标对，然后最小化损失并在独立切片验收。",
          "先定义希望模型表现出的行为，再收集有代表性的输入—目标对",
        ),
        interpretation: item(
          "下游分数提高说明模型更适合当前目标和分布，不能说明知识全面增加。",
          "下游分数提高说明模型更适合当前目标和分布",
        ),
        boundary: item(
          "示范狭窄、标签有偏或部署任务变化时，监督适配仍可能失效。",
          "示范数据狭窄、标签带偏或部署任务变化时",
        ),
      },
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["gradient-descent"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const bridges = [
    `<p>这套“局部导航”用于解决看不到整张损失地形时下一步往哪里走的问题。它读取当前位置的参数与损失函数，输出由各参数偏导数组成的梯度；计算时先固定其他参数求每一维斜率，再把这些斜率排成向量。某个分量绝对值较大，表示损失在该坐标附近更敏感，却不能据此断言这个参数更重要，也不能推知远处最低点的位置。</p>`,
    `<p>负梯度可以理解为“固定欧氏步长时，让局部一阶预测下降最多的方向”。给定当前位置的梯度和允许迈出的长度，它输出一条单位方向；先用点积估计每个候选位移造成的损失变化，再选择与梯度完全反向的位移。点积最负表示一阶近似下降最多，但这个结论依赖步子足够小和普通欧氏距离，换坐标尺度、走得过远或遇到强烈弯曲时都要重新判断。</p>`,
    `<p>更新式把抽象方向变成训练程序真正执行的动作，用于解决梯度方向怎样落实到模型参数的问题：输入当前参数、当前或小批估计的梯度以及学习率，输出下一步参数。程序先计算梯度，再用学习率缩放，最后从旧参数中减去这段改变量；损失从 2 降到 0.72 表示这一次局部移动有效。单步下降不能证明后续每步稳定，也不能证明模型已学会真实规律或能泛化到新样本。</p>`,
    `<p>稳定上限回答的是“同一方向最多能走多远而不把误差越推越大”。它以局部曲率和学习率为输入，输出误差是收缩、震荡还是发散的判断；在这个二次例子中，计算时先得到误差倍率 <code>1−4η</code>，再检查其绝对值是否小于 1。倍率为负表示跨过最优点，绝对值大于等于 1 则表示误差不再收缩。真实网络的曲率会随位置变化，因此 <code>0&lt;η&lt;2/a</code> 是局部二次模型的解释，不是所有训练过程通用的固定许可证。</p>`,
    `<p>狭长谷地描述的不是算法，而是一种各方向曲率差异很大的局部地形，用来解释“梯度不为零、损失也在降，训练却很慢”的现象。输入可以看成当前损失曲面和起点，输出是普通梯度下降形成的参数轨迹；计算更新时，梯度先被陡方向主导，学习率因而必须变小，所以平缓方向进展缓慢。之字形越明显通常说明尺度不均越严重，但图形会受参数坐标和缩放影响，不能只凭二维示意图诊断真实高维网络。这里的高维是指参数坐标数量很多；提到它是为了提醒读者，二维等高线只能帮助理解局部机制。</p>`,
    `<p>小批量训练用有噪声的梯度估计换取更频繁、可并行的反馈。它输入随机抽取的一组样本和当前参数，输出该批次的平均梯度并立即更新；换一批样本后重复这一过程，长期平均才接近全量梯度。批次之间抖动大表示估计方差较高，不必然代表训练失败；批量大小仍受显存、吞吐、设备同步和任务泛化共同约束，改变批量后不能沿用原学习率结论。</p>`,
    `<p>这一节比较的是三种“拿到梯度以后怎样走”的更新规则，用于缓解批次噪声、往返摆动和参数尺度差异。它们共同输入当前梯度与超参数，输出新参数；计算更新时，SGD 先直接使用当前梯度，动量再累积近期方向，Adam 还依据平方梯度历史缩放各坐标步长。更平滑或更快的训练曲线只说明当前配方下优化更顺，不代表 Adam 必然泛化最好，也不能修复错误标签、错误损失或断开的计算链。</p>`,
    `<p>预热、衰减与裁剪是三种用途不同的步长保护机制：输入分别是当前训练步、学习率日程或梯度长度，输出是本步实际学习率或受限梯度。预热先逐步放大学习率，衰减在后期缩小它，裁剪只在梯度越过阈值时按比例缩短。训练初期不再猛冲、后期震荡减弱和偶发尖峰被截住，分别表示对应机制正在起作用；若裁剪几乎步步触发或损失仍持续发散，就不能证明根因已经解决。</p>`,
    `<p>这套诊断流程是把“损失不降”拆成可验证故障假设的方法。它接收小样本实验、预测与标签、梯度范数、更新比率和验证曲线，输出对数据、计算链、步长、优化器或目标错位的定位线索；检查顺序先证明模型能记住小样本，再核对损失输入和梯度，随后检查真实更新，最后才比较优化器与独立验证。某一项异常只缩小排查范围，并不自动证明唯一根因；日志口径错误、随机波动和多个故障同时存在时仍需受控对照实验。</p>`,
  ];

  let sectionIndex = 0;
  page.html = page.html.replace(/<section class="dd-sec">[\s\S]*?<\/section>/g, (section) => {
    sectionIndex += 1;
    if (sectionIndex > 9) return section;
    const marked = section.replace('<section class="dd-sec">', '<section class="dd-sec" data-section-role="core">');
    return marked.replace("</section>", `${bridges[sectionIndex - 1]}</section>`);
  });

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section,
    definition,
    problem,
    inputOutput,
    mechanism,
    interpretation,
    boundary,
  });

  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("梯度是损失对全部参数的局部偏导数组成的向量。", "由各参数偏导数组成的梯度"),
        item("它用于解决看不到完整损失地形时下一步应向哪里移动的问题。", "用于解决看不到整张损失地形时下一步往哪里走的问题"),
        item("输入是当前位置参数与损失函数，输出是梯度向量。", "读取当前位置的参数与损失函数，输出由各参数偏导数组成的梯度"),
        item("计算时先逐维固定其他参数求斜率，再把结果排成向量。", "先固定其他参数求每一维斜率，再把这些斜率排成向量"),
        item("分量绝对值较大表示损失在该坐标附近更加敏感。", "表示损失在该坐标附近更敏感"),
        item("局部梯度不能证明参数更重要，也不能指出远处最低点。", "不能据此断言这个参数更重要，也不能推知远处最低点的位置")),
      contract(2,
        item("负梯度是固定欧氏步长时使局部一阶预测下降最多的方向。", "负梯度可以理解为"),
        item("它解决已知局部梯度后怎样选择最陡下降方向的问题。", "让局部一阶预测下降最多的方向"),
        item("输入是当前梯度与允许步长，输出是一条下降单位方向。", "给定当前位置的梯度和允许迈出的长度，它输出一条单位方向"),
        item("先用点积估计候选位移，再选择与梯度完全反向的位移。", "先用点积估计每个候选位移造成的损失变化，再选择"),
        item("点积最负表示一阶近似预期下降最多。", "点积最负表示一阶近似下降最多"),
        item("结论依赖小步、欧氏距离和局部近似，换尺度或走远后可能失效。", "依赖步子足够小和普通欧氏距离")),
      contract(3,
        item("梯度更新式是把局部下降方向转成一次参数移动的规则。", "更新式把抽象方向变成训练程序真正执行的动作"),
        item("它用于解决梯度方向怎样真正落实到模型参数的问题。", "用于解决梯度方向怎样落实到模型参数的问题"),
        item("输入是参数、梯度和学习率，输出是下一步参数。", "输入当前参数、当前或小批估计的梯度以及学习率，输出下一步参数"),
        item("先算梯度，再按学习率缩放，最后从旧参数中减去改变量。", "先计算梯度，再用学习率缩放，最后从旧参数中减去"),
        item("损失从 2 降至 0.72 表示本次局部更新有效。", "损失从 2 降到 0.72 表示这一次局部移动有效"),
        item("单步下降不能证明长期稳定、掌握规律或泛化。", "不能证明后续每步稳定，也不能证明模型已学会真实规律")),
      contract(4,
        item("学习率稳定上限描述局部更新仍能使误差收缩的最大步长范围。", "稳定上限回答的是"),
        item("它用于判断正确方向在多大步长下会震荡或发散。", "同一方向最多能走多远而不把误差越推越大"),
        item("输入是局部曲率与学习率，输出是收缩、震荡或发散判断。", "以局部曲率和学习率为输入，输出误差是收缩、震荡还是发散的判断"),
        item("计算时先得到误差倍率，再检查其绝对值是否小于一。", "计算时先得到误差倍率"),
        item("倍率为负表示越过最优点，绝对值不小于一表示误差不收缩。", "倍率为负表示跨过最优点"),
        item("二次函数的稳定区间不能当成真实网络全程通用许可。", "不是所有训练过程通用的固定许可证")),
      contract(5,
        item("狭长谷地是不同参数方向曲率相差很大的局部损失地形。", "狭长谷地描述的不是算法，而是一种各方向曲率差异很大的局部地形"),
        item("它用于解释梯度存在且损失下降时训练仍可能缓慢的问题。", "用来解释“梯度不为零、损失也在降，训练却很慢”的现象"),
        item("输入可看作损失曲面与起点，输出是梯度下降参数轨迹。", "输入可以看成当前损失曲面和起点，输出是普通梯度下降形成的参数轨迹"),
        item("计算更新时梯度先被陡方向主导，因此学习率受限且平缓方向推进缓慢。", "计算更新时，梯度先被陡方向主导"),
        item("明显之字形通常表示不同方向的尺度不均较严重。", "之字形越明显通常说明尺度不均越严重"),
        item("二维图受坐标缩放影响，不能单独诊断真实高维网络。", "不能只凭二维示意图诊断真实高维网络")),
      contract(6,
        item("mini-batch 是用随机小组样本平均梯度进行一次更新的训练方式。", "小批量训练用有噪声的梯度估计换取更频繁、可并行的反馈"),
        item("它解决全量梯度单次反馈慢、内存与并行成本高的问题。", "换取更频繁、可并行的反馈"),
        item("输入是随机样本组与当前参数，输出是批次平均梯度和一次更新。", "输入随机抽取的一组样本和当前参数，输出该批次的平均梯度并立即更新"),
        item("每批求平均后立即更新，换批重复，长期平均接近全量梯度。", "换一批样本后重复这一过程，长期平均才接近全量梯度"),
        item("批次间抖动大表示梯度估计方差高，不必然是训练失败。", "抖动大表示估计方差较高，不必然代表训练失败"),
        item("批量受显存、吞吐、同步与泛化限制，改变后需重调学习率。", "不能沿用原学习率结论")),
      contract(7,
        item("优化器是拿到梯度后决定怎样更新参数的规则。", "三种“拿到梯度以后怎样走”的更新规则"),
        item("这些规则用于缓解批次噪声、来回摆动和参数尺度差异。", "用于缓解批次噪声、往返摆动和参数尺度差异"),
        item("输入是当前梯度和超参数，输出是更新后的参数。", "共同输入当前梯度与超参数，输出新参数"),
        item("计算更新时 SGD 先用当前梯度，动量累积方向，Adam 再按平方梯度缩放坐标。", "计算更新时，SGD 先直接使用当前梯度"),
        item("曲线更快更平滑只表示当前配方下的优化过程更顺。", "只说明当前配方下优化更顺"),
        item("Adam 不保证泛化最好，也不能修复标签、损失和计算链错误。", "不能修复错误标签、错误损失或断开的计算链")),
      contract(8,
        item("预热、衰减和裁剪是分别控制早期、后期与异常梯度的步长保护机制。", "预热、衰减与裁剪是三种用途不同的步长保护机制"),
        item("它们用于防止训练初期猛冲、后期震荡和偶发巨大更新。", "预热先逐步放大学习率，衰减在后期缩小它"),
        item("输入是训练步、学习率日程或梯度长度，输出是实际学习率或受限梯度。", "输入分别是当前训练步、学习率日程或梯度长度，输出是本步实际学习率或受限梯度"),
        item("预热逐步放大，衰减后期缩小，裁剪仅在超过阈值时缩短梯度。", "预热先逐步放大学习率，衰减在后期缩小它，裁剪只在梯度越过阈值时"),
        item("初期稳定、后期少震荡和尖峰被截住，分别表示对应机制正在起作用。", "分别表示对应机制正在起作用"),
        item("频繁裁剪或持续发散时不能证明训练根因已经解决。", "不能证明根因已经解决")),
      contract(9,
        item("训练停滞诊断是把损失异常拆成一组可验证故障假设的方法。", "这套诊断流程是把“损失不降”拆成可验证故障假设的方法"),
        item("它用于定位数据、计算链、步长、优化器或目标究竟在哪一环失效。", "对数据、计算链、步长、优化器或目标错位的定位线索"),
        item("输入是实验与训练日志，输出是故障定位线索。", "接收小样本实验、预测与标签、梯度范数、更新比率和验证曲线，输出"),
        item("先测小样本记忆，再查损失和梯度，随后查更新，最后比较优化器与验证。", "先证明模型能记住小样本，再核对损失输入和梯度，随后检查真实更新"),
        item("单项异常只表示排查范围缩小，不代表唯一根因已被证明。", "某一项异常只缩小排查范围，并不自动证明唯一根因"),
        item("日志错误、随机波动和多故障并存时仍需受控对照实验。", "仍需受控对照实验")),
    ],
    termReviews: [
      ...(Array.isArray(page.quality?.termReviews) ? page.quality.termReviews : []),
      {
        section: 5,
        reviewedAt: "2026-07-27",
        terms: [{
          name: "高维",
          meaning: "参数或特征坐标的数量很多",
          purpose: "提醒二维图示不能完整代表真实模型地形",
          definitionEvidence: "高维是指参数坐标数量很多",
          purposeEvidence: "二维等高线只能帮助理解局部机制",
        }],
      },
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["backprop"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const bridges = [
    `<p>有限差分是一种用微小扰动数值近似梯度的方法，用于解决不手写导数也能估计斜率的问题。它输入某个参数值和步长 ε，输出该参数上损失的近似斜率；计算时先在参数两侧各做一次前向，再用两个损失之差除以步长得到差商。差商越接近真实导数，说明步长越合适，但每个参数都要额外前向，参数一多就不能承担训练规模，且步长过大或过小都会失真。</p>`,
    `<p>计算图是一种把复杂函数记录成运算节点与依赖边的有向无环结构，用于解决不必一次推导整条巨大公式的问题。它输入前向执行时的运算关系和必要中间值，输出一张能按链式法则拼接梯度的图；构建时先在前向记录每步运算，再让加法、乘法、矩阵乘和激活各自只实现局部导数。图中一个节点表示一次局部运算，不能理解为对整条公式的整体求导，且只有前向保存了必要中间值，反向才可用。</p>`,
    `<p>上游梯度乘局部导数描述反向经过一个节点时如何把敏感度继续传下去，用于解决梯度怎样逐节点组合的问题。它输入下游送来的上游梯度和该节点的局部导数，输出对本节点输入的梯度；计算时先取上游梯度，再乘以本地的局部导数。乘积表示输入变化一点最终损失会变多少，不能理解为发明了新的微积分，且该式只在局部导数存在且被正确实现时才成立。</p>`,
    `<p>这段手算描述如何沿计算图逐节点把损失种子传回每个变量，用于解决这个梯度到底怎样一步步出现的问题。它输入前向得到的各中间值和损失种子，输出每个变量上的梯度；计算时先以损失种子 1 开始，再依次经过平方、减法和乘法逐节点缩放。得到负梯度表示把该参数稍微增大损失就会下降，不能理解为它必须增大这么多，且这些数值只对当前取定的一组取值成立。</p>`,
    `<p>分叉累加描述一个变量同时参与多条路径时梯度如何合并，用于解决只沿一条路径反传会漏掉贡献的问题。它输入该变量各条下游路径分别送回的梯度，输出这些贡献相加后的总梯度；计算时先分别求每条路径的偏导，再在该变量节点把它们相加。两条路径相加表示总导数包含全部下游影响，不能只保留其中一条，且图内分叉求和与跨批次梯度累积不是同一件事。</p>`,
    `<p>向量—雅可比积是一种不显式构造整块雅可比就得到梯度的方法，用于解决大张量层雅可比过大无法存下的问题。它输入下游传来的梯度向量和该层的局部映射，输出对该层输入的梯度；计算时先拿到下游向量，再用该层专门的规则直接算出向量与雅可比的乘积。训练从标量损失反传表示反向模式尤其适合参数极多而输出很少的情形，但换成少输入多输出时前向模式反而更省。</p>`,
    `<p>激活缓存描述训练为何比纯推理占用更多内存，用于解决梯度检查点该如何权衡的问题。它输入前向产生的激活、参数梯度和优化器状态，输出反向可用的中间值和总的显存占用；机制上先在前向保存必要激活，再在反向读取它们用于计算梯度。显存远高于推理表示大量激活被保留待用，检查点可以只存一部分并在反向时重算，但重算要求过程可重复，且不当处理随机数或原地操作会出错。</p>`,
    `<p>这一节描述哪些代码操作会悄悄切断或污染计算图，用于解决公式明明正确梯度却仍然出错的问题。它输入代码里的实际张量操作和梯度检查信号，输出对故障类型的定位；排查时先看图是否被切断，再依次检查原地修改、梯度未清零、精度溢出和错误广播。某一项命中只表示缩小了排查范围，不能证明唯一根因，且多个故障可能同时存在。</p>`,
    `<p>梯度检查是一种用有限差分为自动梯度提供独立证据的方法，用于解决怎样确认反向实现没写错的问题。它输入同一个前向在参数两侧的两次损失，输出数值梯度并与解析梯度比较相对误差；执行时先取很小的网络和步长，再用中心差分算出数值梯度并与反向结果比对。相对误差很小表示实现大概率正确，但折点、随机层未固定或步长不当都可能让检查误报，因此它只是抽查而不是证明。</p>`,
  ];

  let sectionIndex = 0;
  page.html = page.html.replace(/<section class="dd-sec">[\s\S]*?<\/section>/g, (section) => {
    sectionIndex += 1;
    if (sectionIndex > 9) return section;
    const marked = section.replace('<section class="dd-sec">', '<section class="dd-sec" data-section-role="core">');
    return marked.replace("</section>", `${bridges[sectionIndex - 1]}</section>`);
  });

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section,
    definition,
    problem,
    inputOutput,
    mechanism,
    interpretation,
    boundary,
  });

  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("有限差分是用微小扰动数值近似梯度的数值方法。", "有限差分是一种用微小扰动数值近似梯度的方法"),
        item("它用于解决不手写导数也能估计梯度这一问题。", "用于解决不手写导数也能估计斜率的问题"),
        item("输入是参数值和步长，输出是损失的近似斜率。", "它输入某个参数值和步长"),
        item("先在参数两侧各做一次前向，再用损失差除以步长得到差商。", "计算时先在参数两侧各做一次前向，再用两个损失之差除以步长得到差商"),
        item("差商越接近真实导数，越说明步长选得合适。", "差商越接近真实导数，说明步长越合适"),
        item("但参数一多就不能承担训练，步长过大或过小都会失真。", "参数一多就不能承担训练规模")),
      contract(2,
        item("计算图描述复杂函数如何拆成局部运算节点与依赖边。", "计算图是一种把复杂函数记录成运算节点与依赖边的有向无环结构"),
        item("它用于解决不必一次推导整条巨大公式的问题。", "用于解决不必一次推导整条巨大公式的问题"),
        item("输入是前向的运算关系与中间值，输出是可拼接梯度的图。", "它输入前向执行时的运算关系和必要中间值，输出一张能按链式法则拼接梯度的图"),
        item("先在前向记录每步运算，再让各运算只实现局部导数。", "构建时先在前向记录每步运算，再让加法、乘法、矩阵乘和激活各自只实现局部导数"),
        item("节点只表示一次局部运算，不能当作整体求导。", "图中一个节点表示一次局部运算，不能理解为对整条公式的整体求导"),
        item("但只有前向保存了中间值，反向才能使用。", "只有前向保存了必要中间值，反向才可用")),
      contract(3,
        item("它描述反向如何把上游敏感度逐节点传下去。", "上游梯度乘局部导数描述反向经过一个节点时如何把敏感度继续传下去"),
        item("它用于解决梯度怎样逐节点组合起来的问题。", "用于解决梯度怎样逐节点组合的问题"),
        item("输入是上游梯度与局部导数，输出是对本节点输入的梯度。", "它输入下游送来的上游梯度和该节点的局部导数，输出对本节点输入的梯度"),
        item("先取上游梯度，再乘以本地的局部导数。", "计算时先取上游梯度，再乘以本地的局部导数"),
        item("乘积表示输入变一点最终损失会变多少。", "乘积表示输入变化一点最终损失会变多少"),
        item("但该式仅在局部导数存在且实现正确时才成立。", "该式只在局部导数存在且被正确实现时才成立")),
      contract(4,
        item("这段手算描述如何沿计算图把损失种子逐节点传回变量。", "这段手算描述如何沿计算图逐节点把损失种子传回每个变量"),
        item("它用于解决这个梯度到底怎样一步步出现的问题。", "用于解决这个梯度到底怎样一步步出现的问题"),
        item("输入是各中间值与损失种子，输出是每个变量上的梯度。", "它输入前向得到的各中间值和损失种子，输出每个变量上的梯度"),
        item("先以损失种子 1 开始，再依次经过平方、减法、乘法缩放。", "计算时先以损失种子 1 开始，再依次经过平方、减法和乘法逐节点缩放"),
        item("得到负梯度表示把该参数增大一点损失会下降。", "得到负梯度表示把该参数稍微增大损失就会下降"),
        item("但这些数值仅对当前取定的一组取值成立。", "这些数值只对当前取定的一组取值成立")),
      contract(5,
        item("它描述一个变量参与多条路径时梯度如何合并累加。", "分叉累加描述一个变量同时参与多条路径时梯度如何合并"),
        item("它用于解决只沿一条路径反传会漏掉贡献的问题。", "用于解决只沿一条路径反传会漏掉贡献的问题"),
        item("输入是各下游路径送回的梯度，输出是相加后的总梯度。", "它输入该变量各条下游路径分别送回的梯度，输出这些贡献相加后的总梯度"),
        item("先分别求每条路径的偏导，再在变量节点相加。", "计算时先分别求每条路径的偏导，再在该变量节点把它们相加"),
        item("相加表示总导数包含全部下游影响，不能只留一条。", "两条路径相加表示总导数包含全部下游影响，不能只保留其中一条"),
        item("但图内分叉求和不是跨批次梯度累积。", "图内分叉求和与跨批次梯度累积不是同一件事")),
      contract(6,
        item("它是不显式构造整块雅可比就能得到梯度的方法。", "向量—雅可比积是一种不显式构造整块雅可比就得到梯度的方法"),
        item("它用于解决大张量层雅可比过大无法存下的问题。", "用于解决大张量层雅可比过大无法存下的问题"),
        item("输入是下游梯度向量与局部映射，输出是对该层输入的梯度。", "它输入下游传来的梯度向量和该层的局部映射，输出对该层输入的梯度"),
        item("先拿到下游向量，再用专门规则算出向量与雅可比乘积。", "计算时先拿到下游向量，再用该层专门的规则直接算出向量与雅可比的乘积"),
        item("从标量损失反传表示反向模式适合多参数少输出。", "训练从标量损失反传表示反向模式尤其适合参数极多而输出很少的情形"),
        item("但少输入多输出时前向模式反而更省。", "但换成少输入多输出时前向模式反而更省")),
      contract(7,
        item("它描述训练为何比纯推理占用更多内存。", "激活缓存描述训练为何比纯推理占用更多内存"),
        item("它用于解决梯度检查点如何权衡内存与算力的问题。", "用于解决梯度检查点该如何权衡的问题"),
        item("输入是激活、梯度与优化器状态，输出是中间值与显存占用。", "它输入前向产生的激活、参数梯度和优化器状态，输出反向可用的中间值和总的显存占用"),
        item("先在前向保存必要激活，再在反向读取用于计算梯度。", "机制上先在前向保存必要激活，再在反向读取它们用于计算梯度"),
        item("显存远高于推理表示大量激活被保留待用。", "显存远高于推理表示大量激活被保留待用"),
        item("但重算换内存要求过程可重复，随机数处理不当会出错。", "不当处理随机数或原地操作会出错")),
      contract(8,
        item("它描述哪些代码操作会切断或污染计算图。", "这一节描述哪些代码操作会悄悄切断或污染计算图"),
        item("它用于解决公式正确梯度却仍然出错的问题。", "用于解决公式明明正确梯度却仍然出错的问题"),
        item("输入是实际张量操作与梯度信号，输出是故障类型定位。", "它输入代码里的实际张量操作和梯度检查信号，输出对故障类型的定位"),
        item("先看图是否被切断，再依次查原地修改、未清零、溢出等。", "排查时先看图是否被切断，再依次检查原地修改、梯度未清零、精度溢出和错误广播"),
        item("命中一项只表示缩小范围，不能证明唯一根因。", "某一项命中只表示缩小了排查范围，不能证明唯一根因"),
        item("但命中一项不是唯一根因，多个故障可能同时存在。", "多个故障可能同时存在")),
      contract(9,
        item("它是用有限差分为自动梯度提供独立证据的方法。", "梯度检查是一种用有限差分为自动梯度提供独立证据的方法"),
        item("它用于解决怎样确认反向实现没写错的问题。", "用于解决怎样确认反向实现没写错的问题"),
        item("输入是参数两侧的两次损失，输出是数值梯度与相对误差。", "它输入同一个前向在参数两侧的两次损失，输出数值梯度并与解析梯度比较相对误差"),
        item("先取很小网络和步长，再用中心差分算数值梯度并比对。", "执行时先取很小的网络和步长，再用中心差分算出数值梯度并与反向结果比对"),
        item("相对误差很小表示实现大概率正确。", "相对误差很小表示实现大概率正确"),
        item("但折点、随机未固定或步长不当都可能让检查误报。", "折点、随机层未固定或步长不当都可能让检查误报")),
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["attention"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section, definition, problem, inputOutput, mechanism, interpretation, boundary,
  });

  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("注意力机制是让模型自己决定每个词该看句中哪些词的机制。", "注意力机制可以理解为让模型在处理每个词时自己决定该看句中哪些词的机制"),
        item("它用于解决怎样不靠固定规则为每个词找到相关词的问题。", "它要解决的正是怎样不靠固定规则"),
        item("输入是一串词的表示，输出是每个词该看向谁的判断。", "它输入一串词的表示"),
        item("先让每个词根据内容与别的词逐一比较，再输出该看向谁。", "先让每个词根据内容与别的词逐一比较，再输出每个词应重点看向谁的判断"),
        item("某个词被判断为重点看，表示它与当前内容更相关而非最近。", "表示它在当前内容下与该词更相关"),
        item("但门控 RNN 只能缓解、不能消除长路径与串行约束。", "门控 RNN 能缓解但不能消除这两个结构约束")),
      contract(2,
        item("它是把该看谁拆成查询、键、值三个角色来计算的机制。", "可以理解为把该看谁拆成查询、键、值三个角色的计算方式"),
        item("它用于解决模型该重点看句中哪些词的问题。", "用于解决模型该重点看句中哪些词的问题"),
        item("输入是每个词的查询、键、值向量，输出是汇总全场的新表示。", "它输入每个词的查询、键、值向量，输出该词汇总全场后的新表示"),
        item("先用查询与所有键点积算匹配，再 softmax 加权求和所有值。", "答案是一套三步的匹配-加权流程"),
        item("权重高只表示该词在这个头里更匹配，不是唯一原因。", "权重高只表示该词在这个头里更匹配"),
        item("但注意力权重不是解释概率，也不能证明是唯一原因。", "但这不是解释概率")),
      contract(3,
        item("它描述缩放点积注意力如何把分数变成一次加权混合。", "这段手算描述缩放点积注意力如何把分数变成一次加权混合"),
        item("它用于解决权重到底怎样落到具体数值的问题。", "用于解决权重到底怎样落到具体数值的问题"),
        item("输入是查询、键、值向量，输出是一个混合向量。", "它输入查询、键、值向量"),
        item("先做点积与缩放，再经 softmax 得到权重并加权求和。", "先做点积与缩放，再经 softmax 得到权重并加权求和"),
        item("权重更大表示这把键更匹配，但只是一次混合系数。", "权重更大表示这把键更匹配"),
        item("但注意力权重不是解释概率，只是一次混合系数。", "但它不是解释概率")),
      contract(4,
        item("自注意力是查询、键、值都来自同一句话的注意力。", "自注意力可以理解为查询、键、值都来自同一句话的注意力"),
        item("它用于解决句子内部各词如何互相参照的问题。", "用于解决句子内部各词如何互相参照的问题"),
        item("输入是同一句话的词表示，输出是每个词的新表示。", "它输入同一句话的词表示"),
        item("先让每个词与同句所有词比较，再据权重更新表示。", "先让每个词与同句所有词比较，再据权重更新"),
        item("某词主要看向另一词，表示两者在句内更相关。", "表示两者在句内更相关"),
        item("但这只是同一句内部的关系，不涉及外部序列。", "但这只是同一句内部的关系")),
      contract(5,
        item("它是用一层内直接连接替代 RNN 逐步传递的机制。", "可以理解为它用一层内的直接连接替代了 RNN 的逐步传递"),
        item("它用于解决长距离依赖难学和无法并行两个瓶颈。", "用于解决长距离依赖难学和无法并行两个瓶颈"),
        item("输入是整段序列所有 token 的表示，输出是每个位置的新表示。", "它接收整段序列中所有 token 的表示，并同时输出每个位置融合可见上下文后的新表示"),
        item("计算时每个位置先与所有可见位置直接匹配，再并行算完。", "计算时每个位置先与所有可见位置直接匹配，再并行地一次算完"),
        item("路径更短表示远距信息更容易学到。", "路径更短表示远距信息更容易学到"),
        item("但更短的路径不保证权重一定落在正确位置。", "但更短的路径不保证权重一定落在正确位置")),
      contract(6,
        item("多头是在同一层并行跑多组注意力、各看一种相关的机制。", "多头注意力可以理解为在同一层并行跑多组注意力、各看一种相关方式的机制"),
        item("它用于解决单组注意力只能按一种方式衡量相关的问题。", "用于解决单组注意力只能按一种方式衡量相关的问题"),
        item("输入是同一批词表示，输出是综合多视角的新表示。", "它输入同一批词表示"),
        item("先在各子空间分别做注意力，再把多组结果拼接投影。", "先在各子空间分别做注意力，再把多组结果拼接投影"),
        item("某些头呈现语法或指代，表示分工可能在训练中形成。", "表示分工可能在训练中形成"),
        item("但这种分工是学出来的，而非预先指定或必然出现。", "但这种分工是学出来的而非预先指定")),
      contract(7,
        item("平方级开销指每个词都要与所有词两两匹配带来的成本。", "平方级开销可以理解为每个词都要与所有词两两匹配带来的成本"),
        item("它用于解决为什么长上下文又慢又贵这一问题。", "用于解决为什么长上下文又慢又贵这一问题"),
        item("输入是序列全部位置的表示，输出是每个位置的新表示。", "它输入序列全部位置的表示"),
        item("先让每个位置与所有位置两两算分，再归一化与加权。", "先让每个位置与所有位置两两算分，再对整张分数矩阵做归一化与加权"),
        item("开销随长度平方增长，表示长度翻倍成本翻四倍。", "开销随长度平方增长表示长度翻倍成本翻四倍"),
        item("但实际总成本还包含投影与前馈，优化只改变常数。", "但实际总成本还包含投影与前馈")),
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["embedding"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section, definition, problem, inputOutput, mechanism, interpretation, boundary,
  });

  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("嵌入是把文本映射成连续向量的表示方法。", "嵌入是一种把文本映射成连续向量的表示方法"),
        item("它用于解决机器无法直接比较文字意思、只能算数字的问题。", "嵌入用于解决机器无法直接比较文字意思、只能算数字这一问题"),
        item("输入是一段文本，输出是一个几百到几千维的向量。", "它输入一段文本，先经嵌入模型编码，再输出一个几百到几千维的向量"),
        item("先经嵌入模型编码，再输出一个高维向量。", "先经嵌入模型编码，再输出一个几百到几千维的向量"),
        item("两段文本向量距离越近，表示它们语义越相近。", "两段文本向量距离越近，表示它们语义越相近"),
        item("但这种距离只在同一嵌入模型空间里可比，换模型不成立。", "但这种距离只在同一嵌入模型的空间里可比")),
      contract(2,
        item("距离等于语义是训练目标塑造出来的几何性质。", "距离等于语义可以理解为训练目标塑造出来的几何性质"),
        item("它用于解决凭什么向量远近能对应意思远近的疑问。", "用于解决凭什么向量远近能对应意思远近这一疑问"),
        item("输入是大量相近或无关的样本对，输出是语义化的向量空间。", "它输入大量相近或无关的样本对"),
        item("先据配对调整参数，再让相近向量靠拢、无关向量推开。", "先据配对调整参数，再让相近向量靠拢、无关向量推开"),
        item("相近样本被拉近，表示模型学到了它们的关联。", "相近样本被拉近表示模型学到了它们的关联"),
        item("但这只是训练配对逼出的副产品，不是设计的规则。", "但这只是训练配对逼出的副产品，不是设计的规则")),
      contract(3,
        item("余弦相似度是用夹角余弦衡量两个向量方向接近程度的指标。", "余弦相似度可以理解为用夹角余弦衡量两个向量方向接近程度的指标"),
        item("它用于解决怎样把两段话是否同义变成可比数值的问题。", "用于解决怎样把两段话是否同义变成一个可比数值的问题"),
        item("输入是两个嵌入向量，输出是一个相似度分数。", "它输入两个嵌入向量"),
        item("先算点积，再除以两者长度得到相似度。", "先算点积再除以两者长度"),
        item("余弦值越大，表示两者方向越接近、语义越相近。", "余弦值越大越表示两者方向越接近"),
        item("但它不能跨模型比较，也不能当作正确概率。", "不能跨模型比较")),
      contract(4,
        item("这次手算描述余弦相似度如何把方向差异变成可排序分数。", "这次手算描述余弦相似度如何把方向差异变成一个可排序的分数"),
        item("它用于解决更同向怎样落成可复算排名的问题。", "用于解决更同向怎样落成可复算排名的问题"),
        item("输入是查询与候选向量，输出是每个候选的相似度。", "它输入查询与候选向量"),
        item("先做点积，再按长度归一得到余弦。", "先做点积、再按长度归一"),
        item("分数越高表示方向越接近，但只是模型空间内的相似。", "分数越高表示方向越接近"),
        item("但数值高只是模型空间内的相似，不代表业务上正确。", "但数值高只是模型空间内的相似，不代表业务上正确")),
      contract(5,
        item("这里比较的是两个同名却不同的嵌入概念。", "这里比较的是两个同名却不同的嵌入"),
        item("它用于解决做检索时该用哪一个嵌入的选型问题。", "用于解决该用哪一个的选型问题"),
        item("输入同样是文本，输出却是 token 向量或整段文本向量。", "输入同样是文本，但输入嵌入层先给每个 token 一个向量、再送进 Transformer，而嵌入模型直接输出整段文本的一个向量"),
        item("输入嵌入层先给每个 token 一个向量，再送进 Transformer。", "输入嵌入层先给每个 token 一个向量、再送进 Transformer"),
        item("用途不同，表示两者不能互相替代。", "用途不同表示不能互相替代"),
        item("但两者都叫嵌入，混用会导致选型和实现出错。", "但两者都叫嵌入，混用会导致选型和实现出错")),
      contract(6,
        item("语义搜索是按意思而非字面找最近邻的检索方式。", "语义搜索可以理解为按意思而非字面找最近邻的检索方式"),
        item("它用于解决关键词搜索漏掉没有共同词文档的问题。", "用于解决关键词搜索漏掉没有共同词的相关文档的问题"),
        item("输入是查询和文档库，输出是离查询最近的文档。", "它输入查询和文档库"),
        item("先把查询和文档都嵌入成向量，再输出最近的文档。", "先把两者都嵌入成向量，再输出离查询最近的文档"),
        item("某文档被召回，表示它与查询意思相近。", "某文档被召回表示它与查询意思相近"),
        item("但召回质量依赖嵌入好坏，平均召回好也不保证每条对。", "但召回质量依赖嵌入好坏")),
      contract(7,
        item("这两个坑描述换模型和否定两种典型失败。", "这两个坑描述换模型和否定两种失败"),
        item("认识它们用于避免嵌入落地时最常见的两类翻车。", "认识这两个坑用于避免嵌入在工程落地时最常见的两类翻车"),
        item("共同输入是待检索文本和嵌入模型，输出是检索可靠性。", "它们的共同输入是待检索文本和所用嵌入模型，输出是检索结果的可靠性"),
        item("第一个坑源于换模型后向量先落到不同空间，再被混用。", "第一个坑源于换模型后向量先落到不同空间、再被拿来混用"),
        item("某次召回对不对，表示的是当前模型空间内的相似。", "某次检索召回对不对，表示的是当前模型空间内的相似"),
        item("但这不是保证，必须用真实检索样本验证。", "但这不是保证，必须用真实检索样本验证")),
    ],
  };
})();

(() => {
  const page = window.DEEPDIVE && window.DEEPDIVE["context-window"];
  if (!page) return;

  page.html = page.html.replace(/<p class="dd-contract-summary">[\s\S]*?<\/p>/g, "");

  const item = (answer, evidence) => ({ answer, evidence });
  const contract = (section, definition, problem, inputOutput, mechanism, interpretation, boundary) => ({
    section, definition, problem, inputOutput, mechanism, interpretation, boundary,
  });

  page.quality = {
    ...page.quality,
    contractVersion: 2,
    sectionContracts: [
      contract(1,
        item("上下文窗口是一次调用中模型能直接看到的内容预算。", "上下文窗口可以理解为一次调用中模型能直接看到的内容预算"),
        item("它用于解决一次调用里哪些信息能直接影响生成的容量问题。", "用于解决一次调用里哪些信息能直接影响下一步生成的容量问题"),
        item("输入是规则、历史、证据、工具结果和输出预留，输出是一个布局。", "它输入系统规则、历史、检索证据、工具结果和输出预留"),
        item("先按分词器计数，再按产品策略接受、裁剪或压缩。", "先按分词器计数，再按产品策略接受、裁剪或压缩"),
        item("窗口大小只表示容量上限，不保证每处信息都被用好。", "窗口大小只表示容量上限，不表示每个位置的信息都能被同样准确地使用"),
        item("但原始信息一旦滑出窗口，本次生成就用不到它。", "原始信息一旦滑出窗口，本次生成就不能再直接用到它")),
      contract(2,
        item("无状态指模型每次调用都不自动保留上一次的内容。", "无状态可以理解为模型每次调用都不自动保留上一次的内容"),
        item("它用于解决模型为何看似记得对话却会失忆的问题。", "用于解决它为何看似记得对话却会突然失忆的问题"),
        item("输入是本次提交的消息与取回内容，输出是据此生成的回答。", "它输入本次显式提交的消息与取回内容"),
        item("先把历史、摘要或检索结果重新装进输入，再据此生成。", "先把相关历史、摘要或检索结果重新装进输入，再据此生成"),
        item("回答能提到旧信息，说明该信息本轮仍在窗口里。", "回答能提到旧信息，说明该信息本轮仍在窗口里"),
        item("但若未保留也未检索回来，模型本次并非真的记得。", "但若既未保留也未检索回来，模型本次就不是真的记得")),
      contract(3,
        item("平方级开销指每个位置都要与所有位置两两算注意力的成本。", "平方级开销可以理解为每个位置都要与所有位置两两算注意力带来的成本"),
        item("它用于解决窗口为什么不能无限放大的问题。", "用于解决窗口为什么不能无限放大的问题"),
        item("输入是序列长度，输出是随长度平方增长的计算与显存。", "它输入序列长度"),
        item("先让每个位置与全部位置两两算分，再对分数表归一化。", "先让每个位置与全部位置两两算分，再对这张分数表做归一化"),
        item("长度扩大十倍位置对约扩大一百倍，说明代价超线性。", "长度扩大十倍位置对约扩大一百倍，说明代价是超线性的"),
        item("但这只是标准全局注意力的主要瓶颈，不是唯一约束。", "但这解释的是标准全局注意力的主要瓶颈，不是所有实现的唯一约束")),
      contract(4,
        item("这一节比较的是同一声称长度背后的四种不同限制。", "这一节比较的是同一个声称长度背后的四种不同限制"),
        item("它用于解决同样支持长度却有的慢、崩或答错的问题。", "用于解决为什么同样支持长度却有的慢、有的并发就崩、有的答错的问题"),
        item("输入是长度、并发、硬件、证据位置与结果，输出是瓶颈判断。", "诊断输入是请求长度、并发、硬件、证据位置和任务结果"),
        item("先分别核算计算、容量、训练长度和装配，再定位瓶颈。", "先分别核算计算与读写、缓存容量、训练长度和装配，再定位真正的瓶颈"),
        item("接口返回成功只表示输入被接收，不表示能有效利用长度。", "接口返回成功只表示输入被接收，不表示模型能有效利用整段长度"),
        item("但四类瓶颈不同，不能用一类指标替代另一类。", "因此不能用一类指标替代另一类")),
      contract(5,
        item("有效上下文指模型在当前任务里真正能取用并用对的信息。", "有效上下文可以理解为模型在当前任务里真正能取用并用对的信息"),
        item("它用于解决把资料塞满窗口为什么不等于用好的问题。", "用于解决把资料塞满窗口为什么不等于用好的问题"),
        item("输入是候选材料及其顺序，输出是带证据的答案。", "它输入候选材料及其顺序"),
        item("先由注意力从大量内容中挑线索，再据此作答。", "先由注意力从大量内容中挑线索，再据此作答"),
        item("材料位置改变结果，说明位置和噪声都在起作用。", "会明显改变结果，说明位置和噪声都在起作用"),
        item("但一次答对并不表示整段窗口都被可靠使用。", "但一次答对并不表示整段窗口都被可靠使用")),
      contract(6,
        item("窗口治理指在超预算时决定保留、检索、压缩和排序的方法。", "窗口治理可以理解为在内容超预算时决定保留、检索、压缩和排序的一套方法"),
        item("它用于解决内容装不下或长对话失忆时该怎么办的问题。", "用于解决内容装不下或长对话失忆时该怎么办的问题"),
        item("输入是超预算材料、目标和失败代价，输出是窗口布局。", "它输入超预算材料、任务目标和失败代价"),
        item("先保护规则与当前任务，再检索、压缩并留出输出余量。", "先保护不可丢的规则与当前任务，再检索高价值证据、压缩可恢复历史并留出输出余量"),
        item("答案质量、证据覆盖和成本一起变好，才表示治理有效。", "答案质量、证据覆盖和成本一起变好，才表示治理有效"),
        item("但摘要可能漏细节、检索可能漏召回，需回查与回退。", "但摘要可能漏细节、检索可能漏召回")),
    ],
  };
})();
