/* 理解原理页 —— PEFT / LoRA */
window.DEEPDIVE = window.DEEPDIVE || {};
window.DEEPDIVE['peft-lora'] = window.createDeepDive({
  title:'PEFT / LoRA：冻结主干，只学习低秩权重增量',
  subtitle:'从 ΔW=BA、rank 与缩放，到可训练参数账、QLoRA、目标模块、合并版本和多适配器干扰。',
  thesis:'LoRA 假设任务适配所需的权重变化可由低秩矩阵近似，用两个小矩阵表示 ΔW，在冻结基座时减少梯度、优化器状态和检查点。它不会按同等比例减少<b>前向计算、训练激活或行为风险</b>，rank 与注入层决定表达上限。',
  goals:['推导 LoRA 参数量和前向','区分训练状态与总显存','理解 rank/alpha/目标模块','安全合并、切换与评测适配器'],
  sections:[
    {title:'任务适配为什么可能是低秩变化',badge:'直觉',lead:'一个 d×k 大矩阵为何不必让每个元素都独立变化？',body:'<p>先把词说清：线性层的基座权重 <code>W</code> 把 k 维输入变成 d 维输出；微调想学的是它的改变量 <code>ΔW</code>。矩阵的“秩”可先理解为它包含多少个彼此独立的变化方向。若任务只需少数方向，ΔW 就可以由两个更窄的矩阵相乘得到，而不必独立存下 d×k 个改变量。</p><p>预训练已学到通用表示，新任务可能只需沿少数方向调整注意力或特征。若 ΔW 的有效秩很低，可用 B(d×r)A(r×k) 表示，r 远小于 d、k。输入仍是原层的 k 维向量，输出仍是 d 维向量，只是额外叠加一条低秩分支。</p><p>这是经验假设，不是所有任务定理。复杂领域迁移、多任务冲突或从未学过的能力可能需要更高 rank、更多目标层或全量微调。</p><div class="dd-note"><b>低秩的是增量，不是基座。</b>　推理仍需完整 W；LoRA 只以小分支改变其输出。</div>'},
    {title:'前向公式与缩放',kind:'math',badge:'机制',lead:'LoRA 分支怎样与原线性层相加？',body:'<div class="dd-formula" data-formula-id="peft-lora-forward" data-display="mathml"><math display="block" aria-label="输出 h 等于基座权重 W 乘输入 x，加 alpha 除以秩 r 再乘 B A x；A 是 r 乘 k，B 是 d 乘 r"><mrow><mi>h</mi><mo>=</mo><mi>W</mi><mi>x</mi><mo>+</mo><mfrac><mi>α</mi><mi>r</mi></mfrac><mi>B</mi><mi>A</mi><mi>x</mi><mo>,</mo><mspace width="0.8em"/><mi>A</mi><mo>∈</mo><msup><mi>ℝ</mi><mrow><mi>r</mi><mo>×</mo><mi>k</mi></mrow></msup><mo>,</mo><mspace width="0.5em"/><mi>B</mi><mo>∈</mo><msup><mi>ℝ</mi><mrow><mi>d</mi><mo>×</mo><mi>r</mi></mrow></msup></mrow></math></div><p class="dd-formula-note"><code>x</code> 是 k 维输入，<code>W</code> 是冻结的 d×k 基座权重，<code>h</code> 是 d 维输出。<code>A</code> 先把 k 维输入压到 r 维，<code>B</code> 再把 r 维展开到 d 维；<code>ℝ</code> 表示矩阵元素是实数。<code>r</code> 是秩上限，<code>α</code> 是缩放系数，比例 <code>α/r</code> 控制低秩分支相对基座分支的强度。</p><p>常将 A 随机初始化、B 初始化为零，使训练开始时 ΔW=0，不改变基座行为。α/r 控制更新尺度；rank 增大若 α 不调，增量尺度会变化。dropout 是训练时随机丢弃部分分支信号的正则化手段，可只作用 LoRA 分支。</p><p>反向不更新 W，但梯度要穿过 W 传播到早层适配器和 A/B；所以基座计算与激活并未消失。若目标指标提高而通用能力下降，说明低秩分支改变了任务行为但产生回归，不能只按训练损失选择配置。</p><div class="dd-note warn"><b>rank 不是“压缩比旋钮”而已。</b>　它改变可表达增量子空间，必须和 α、层、数据一起消融。</div>'},
    {title:'运行示例：4096×4096 投影省多少可训练参数',kind:'math',badge:'逐步演算',lead:'rank=8 时，16.8M 参数的矩阵需要训练多少 LoRA 参数？',body:'<figure class="dd-fig"><svg viewBox="0 0 700 250" role="img" aria-label="4096乘4096基座矩阵通过4096乘8和8乘4096低秩矩阵产生增量"><rect x="20" y="65" width="170" height="120" rx="9" fill="#21252d" stroke="#6b8cbe"/><text x="105" y="92" text-anchor="middle" class="svg-t">冻结 W</text><text x="105" y="120" text-anchor="middle" class="svg-t">4096×4096</text><text x="105" y="148" text-anchor="middle" class="svg-t" font-size="11">16,777,216 参数</text><text x="105" y="170" text-anchor="middle" class="svg-t" font-size="11">仍参与前向</text><text x="240" y="128" class="svg-t">+</text><rect x="285" y="86" width="105" height="78" rx="8" fill="#21252d" stroke="#d3a05a"/><text x="337" y="112" text-anchor="middle" class="svg-t">A: 8×4096</text><text x="337" y="139" text-anchor="middle" class="svg-t" font-size="11">32,768</text><text x="420" y="128" class="svg-t">→</text><rect x="455" y="86" width="105" height="78" rx="8" fill="#21252d" stroke="#4f9d78"/><text x="507" y="112" text-anchor="middle" class="svg-t">B: 4096×8</text><text x="507" y="139" text-anchor="middle" class="svg-t" font-size="11">32,768</text><text x="590" y="128" class="svg-t">=</text><rect x="615" y="75" width="70" height="100" rx="8" fill="#21252d" stroke="#4f9d78"/><text x="650" y="104" text-anchor="middle" class="svg-t">ΔW</text><text x="650" y="129" text-anchor="middle" class="svg-t" font-size="11">秩≤8</text><text x="650" y="151" text-anchor="middle" class="svg-t" font-size="11">65,536</text><text x="350" y="218" text-anchor="middle" class="svg-t">可训练比例 65,536 / 16,777,216 ≈ 0.391%</text></svg><figcaption>图 1　两个低秩矩阵只训练约 0.39% 参数，但完整基座矩阵仍要参与计算。</figcaption></figure><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>rank</th><th>A+B 参数</th><th>相对 W</th><th>表达能力/代价</th></tr></thead><tbody><tr><td>4</td><td>32,768</td><td>.195%</td><td>最省，可能欠拟合</td></tr><tr><td>8</td><td>65,536</td><td>.391%</td><td>示例起点</td></tr><tr><td>32</td><td>262,144</td><td>1.563%</td><td>容量高、状态更多</td></tr></tbody></table></div>'},
    {title:'省的是哪些内存，哪些仍然存在',kind:'eng',badge:'资源',lead:'只训练 0.39% 参数，训练显存为什么不会缩到 0.39%？',body:'<p>不为冻结 W 保存梯度与 Adam 一、二阶状态，适配器检查点也小；但基座权重、前向激活、反向所需中间值、临时工作区仍在。激活常随 batch、序列和层数主导。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>资源</th><th>全量微调</th><th>LoRA</th></tr></thead><tbody><tr><td>基座权重</td><td>需要</td><td>需要且冻结</td></tr><tr><td>基座梯度</td><td>需要</td><td>通常不存</td></tr><tr><td>基座优化器状态</td><td>需要</td><td>不需要</td></tr><tr><td>激活</td><td>需要</td><td>仍需要，可检查点</td></tr><tr><td>适配器状态</td><td>无</td><td>小但需梯度/优化器</td></tr></tbody></table></div><p>训练速度也不会按参数比例加速，因为 W x 的大矩阵乘仍占主要 FLOPs。</p>'},
    {title:'目标模块决定任务能改哪里',kind:'eng',badge:'调参',lead:'只对 Q/V 投影加 LoRA 与覆盖所有线性层，有何结构差异？',body:'<p>Transformer 注意力中的 Q（查询）、K（键）、V（值）和 O（输出）投影，分别参与“找谁、怎样匹配、取什么信息、怎样送回主干”；MLP 是每层随后进行特征变换的前馈网络，gate/up/down 是常见实现里的门控、升维和降维投影。这里的<b>降维</b>只表示把向量的坐标数量从较多变成较少，用于让 MLP 输出回到主干维度；它不是在这一节把样本特征压成二维图。对这些模块加 LoRA，就是选择允许任务数据改动哪些计算环节。</p><p>目标层少更省、更易控，可能容量不足；覆盖更多层提升适配能力也增加过拟合与部署开销。配置输入是候选模块、rank 和相同训练预算，输出是各方案的任务、通用、安全和未见分布指标。</p><p>做受控消融：固定数据与总训练 token，比较 QV、全部注意力、注意力+MLP；rank 可按层不同。不要只看训练 loss，需目标任务、通用能力、安全与未见分布。若加更多层后训练损失下降而留出收益停滞，应解释为容量没有转化为泛化，而不是继续无条件扩层。</p><div class="dd-note warn"><b>模块名依架构而异。</b>　配置匹配不到目标层可能静默训练零参数；启动时列出可训练参数和层路径并断言数量。</div>'},
    {title:'QLoRA 把冻结基座量化存储',kind:'eng',badge:'量化',lead:'基座是 4-bit，梯度怎样通过它到达 LoRA？',body:'<p>QLoRA 是“量化后的冻结基座 + 可训练 LoRA”的组合。量化把基座权重用更少比特近似存储；4-bit 表示每个量化编码只有 16 种取值，而计算时会把编码还原成较高精度近似值。基座参数不更新，但这次矩阵运算仍把梯度传给 A/B。</p><p>NF4 是针对近似正态分布权重设计的 4-bit 编码；double quantization 连量化尺度本身也再压缩；分页优化器在内存紧张时分批迁移优化器状态。它们分别处理权重表示、量化元数据和峰值内存，并不是三种精度相同的名字。</p><p>QLoRA 组合了量化误差与低秩适配上限。输入是量化基座、LoRA 配置和任务数据，输出是适配器及其量化训练配方。训练完成后合并到高精基座再重新量化，与保持分离推理不是同一产物；若两种产物指标不同，应归因到合并和再量化路径并分别评测。</p><div class="dd-note math"><b>资源边界：</b>训练内存下降不等于训练 FLOPs 同比例下降；反量化与基座前向、反向仍然发生。</div>'},
    {title:'合并与动态切换有不同工程边界',kind:'eng',badge:'部署',lead:'将 ΔW 写回 W 后，为什么文件更简单却更难回滚？',body:'<p>合并 <code>W←W+(α/r)BA</code> 可消除分支额外矩阵乘，适合固定单适配器；分离保存便于按租户/任务切换和回滚，但调度需将相同适配器批在一起，频繁加载会抖动。</p><p>适配器必须绑定基座哈希、tokenizer、目标模块、rank、α、dtype 和训练数据版本。错基座即使形状相同也会产生未验证行为。合并顺序与量化顺序影响结果，产物记录完整配方。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>形态</th><th>优势</th><th>风险</th></tr></thead><tbody><tr><td>分离</td><td>小文件、可切换/回滚</td><td>额外算子、批调度、错配</td></tr><tr><td>合并</td><td>单权重、推理简单</td><td>新大文件、失去独立开关</td></tr><tr><td>多适配器组合</td><td>尝试能力组合</td><td>尺度/方向干扰，无保证</td></tr></tbody></table></div>'},
    {title:'小参数比例不代表小行为变化',kind:'eng',badge:'失败边界',lead:'只改 0.1% 参数，为什么能让模型学会新格式，也能破坏安全拒答？',body:'<p>神经网络行为由参数方向与放大链决定，不按改动数量线性变化。少量低秩更新作用于多层和所有输入，能显著重排 logits；窄数据还会过拟合措辞、遗忘通用能力或形成触发器。</p><p>训练数据需许可、去重和防投毒；第三方适配器视为可执行模型制品，扫描来源、签名和行为。对目标、通用、长上下文、多语言、安全、工具和校准做回归；逐层/逐适配器消融定位问题。</p><div class="dd-note warn"><b>适配器供应链：</b>小文件易传播，也易绕过完整模型审查；加载权限应等同模型代码/权重。</div>'},
    {title:'何时选 LoRA，何时选其他方案',badge:'决策',lead:'任务只需要新知识、固定格式或深层能力改变时，方案相同吗？',body:'<p>知识频繁更新优先 RAG/工具；少量格式和行为可先提示/约束；多租户任务适配、有限算力适合 LoRA；需要广泛能力重塑且数据/资源充分可考虑全量微调；端侧体积目标还需蒸馏/量化。</p><p>比较同预算基线：提示、检索、LoRA、全量微调和更强模型。计算训练、存储、适配器路由、延迟、评测与维护的每成功任务成本，而非只看 GPU 能否跑。</p><div class="dd-note"><b>停止条件：</b>rank/层数增加后真实留出增益平台而通用/安全退化上升，应停止扩适配器容量。</div>'},
    {title:'常见误区与概念依赖',badge:'常见误区与学习路线',lead:'LoRA 是低秩参数化的训练方法，不是“零成本微调”或“低风险补丁”。',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>误区</th><th>更准确的理解</th></tr></thead><tbody><tr><td>训练参数少，全部显存同比少</td><td>基座和激活仍在，主要省梯度/优化器</td></tr><tr><td>rank 越大越好</td><td>容量、过拟合、状态和尺度需平衡</td></tr><tr><td>QLoRA 训练 4-bit 梯度</td><td>低位基座冻结，计算时反量化，训练 LoRA</td></tr><tr><td>适配器可配任何同形基座</td><td>必须绑定精确基座与模块语义</td></tr><tr><td>改 0.1% 参数风险也 0.1%</td><td>小增量可全局改变 logits 和安全行为</td></tr></tbody></table></div><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>层级</th><th>概念依赖与延伸学习</th></tr></thead><tbody><tr><td>先修</td><td>矩阵秩、微调、反向传播与优化器状态</td></tr><tr><td><b>本页核心</b></td><td>低秩增量、rank/alpha、目标模块、合并/切换</td></tr><tr><td>组合</td><td>量化/QLoRA、模型合并、蒸馏</td></tr><tr><td>生产</td><td>模型路由、数据治理、评测与供应链安全</td></tr></tbody></table></div>'}
  ],
  chain:['确定任务是否需参数适配','选择目标层/rank/alpha','冻结基座并训练 A/B','消融容量与数据覆盖','分离或按配方合并部署','回归目标/通用/安全并记录血缘'],
  quiz:[{q:'4096×4096、r=8 的 LoRA 参数多少？',a:'8×4096+4096×8=65,536，约原矩阵 0.391%。'},{q:'LoRA 主要省哪些状态？',a:'冻结基座的梯度、优化器状态和任务检查点。'},{q:'QLoRA 的基座如何参与计算？',a:'低位存储、前向反量化，冻结不更新但梯度传到 LoRA。'},{q:'合并后要记录什么？',a:'基座哈希、适配器、目标层、rank、alpha、dtype、顺序和量化配方。'},{q:'为何仍需安全回归？',a:'少量低秩更新可全局改变输出和拒答行为。'}],
  sources:[{title:'LoRA',url:'https://arxiv.org/abs/2106.09685',note:'低秩权重适配'},{title:'QLoRA',url:'https://arxiv.org/abs/2305.14314',note:'量化基座上的高效微调'},{title:'Parameter-Efficient Transfer Learning for NLP',url:'https://arxiv.org/abs/1902.00751',note:'Adapter 参数高效迁移'},{title:'AdaLoRA',url:'https://arxiv.org/abs/2303.10512',note:'自适应秩分配'}]
});
