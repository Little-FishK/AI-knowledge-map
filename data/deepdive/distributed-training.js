/* 理解原理页 —— 分布式训练 */
window.DEEPDIVE = window.DEEPDIVE || {};
window.DEEPDIVE['distributed-training'] = window.createDeepDive({
  title:'分布式训练：沿数据、张量、流水线和状态四个轴拆分',
  subtitle:'从全局批量与 all-reduce，到张量并行、pipeline bubble、ZeRO/FSDP、3D 并行和故障检查点。',
  thesis:'分布式训练不是“多加 GPU”，而是决定参数、梯度、优化器状态、激活和样本分别放在哪里、何时通信。扩展效率受<b>计算/通信比、峰值内存、全局批量语义、拓扑和故障恢复</b>共同限制。',
  goals:['计算训练内存与全局批量','区分 DP/TP/PP/ZeRO','手算流水线气泡和扩展效率','设计数值一致与恢复门禁'],
  sections:[
    {title:'权重能放下，不代表训练能放下',badge:'直觉',lead:'一个 7B FP16 模型约 14GB，为何 24GB GPU 仍不能全量 Adam 微调？',body:'<p><code>7B</code> 表示约 70 亿个参数，FP16 每个权重通常占 2 bytes，所以只存推理权重约需 14GB。全量训练还需要更高精度主权重、每个参数的梯度，以及 Adam 为每个参数维护的两组历史统计；“激活”则是前向各层为反向传播暂存的中间结果。混合精度粗略每参数可能需要 12–20 bytes 状态，7B 远超单卡；长序列激活也可主导。</p><p>内存账的输入是参数量、数值精度、优化器、batch、序列长度和临时缓冲，输出是各类状态的峰值及每卡能否容纳。分片策略分别解决不同对象：数据并行复制模型，ZeRO/FSDP 分片状态，张量并行拆单层，流水线并行拆层，激活检查点用重复计算换较少存储。</p><p>“权重能加载”只证明推理主体可放下，不证明反向传播能运行。内存估算还会受实现、碎片和通信缓冲影响，必须用实测峰值留出安全余量。</p><div class="dd-note"><b>先做内存账：</b>权重、梯度、优化器、激活、通信缓冲和碎片逐项估算，再选并行轴。</div>'},
    {title:'数据并行的语义是同步同一更新',kind:'math',badge:'数据并行',lead:'每卡看不同样本，为什么最后参数仍保持一致？',body:'<p>每个 <code>rank</code>（一个参与训练的进程/设备编号）有完整模型副本，计算不同样本的本地梯度；<code>all-reduce</code> 是让所有 rank 共同求和并把结果发回每一方的集合通信。归约后，各 rank 用相同全局平均梯度更新，所以参数保持同步。</p><div class="dd-formula" data-formula-id="distributed-training-global-batch-gradient"><math display="block" aria-label="全局批量等于每卡局部批量 b 乘数据并行设备数 n 乘梯度累积步数 a；平均梯度等于各设备梯度之和除以 n"><mtable><mtr><mtd><msub><mi>B</mi><mtext>全局</mtext></msub><mo>=</mo><mi>b</mi><mo>·</mo><mi>n</mi><mo>·</mo><mi>a</mi></mtd></mtr><mtr><mtd><mover><mi>g</mi><mo>¯</mo></mover><mo>=</mo><mfrac><mn>1</mn><mi>n</mi></mfrac><munderover><mo>∑</mo><mrow><mi>r</mi><mo>=</mo><mn>1</mn></mrow><mi>n</mi></munderover><msub><mi>g</mi><mi>r</mi></msub></mtd></mtr></mtable></math></div><p class="dd-formula-note"><code>B全局</code> 是一次参数更新实际使用的样本数，<code>b</code> 是每卡每个微步的局部 batch，<code>n</code> 是数据并行副本数，<code>a</code> 是累计多少个微步才更新。<code>r</code> 是 rank 编号，<code>gᵣ</code> 是第 r 个 rank 的累计梯度，<code>ḡ</code> 是 all-reduce 后每个副本共同使用的平均梯度。</p><p>扩设备但不调 b/a 会增大全局批量，改变梯度噪声、学习率和训练 token 语义；不能把收敛变化归咎于硬件。梯度归约精度和顺序也影响数值。</p><div class="dd-note warn"><b>loss 看似相同不代表实验相同。</b>　数据顺序、随机流、dropout 和归约非结合性会改变轨迹。</div>'},
    {title:'运行示例：8 卡全局批量与强扩展效率',kind:'math',badge:'逐步演算',lead:'每卡 4 样本、累积 8 步，8 GPU 的一次优化步看了多少样本？',body:'<figure class="dd-fig"><svg viewBox="0 0 710 260" role="img" aria-label="八个数据并行 GPU 各做局部微批和梯度累积后通过 all-reduce 同步"><defs><marker id="dt1" markerWidth="8" markerHeight="8" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 z" fill="#6b7484"/></marker></defs><g fill="#21252d" stroke="#6b8cbe"><rect x="20" y="45" width="75" height="50" rx="6"/><rect x="105" y="45" width="75" height="50" rx="6"/><rect x="190" y="45" width="75" height="50" rx="6"/><rect x="275" y="45" width="75" height="50" rx="6"/><rect x="360" y="45" width="75" height="50" rx="6"/><rect x="445" y="45" width="75" height="50" rx="6"/><rect x="530" y="45" width="75" height="50" rx="6"/><rect x="615" y="45" width="75" height="50" rx="6"/></g><g class="svg-t" font-size="10" text-anchor="middle"><text x="57" y="75">GPU0</text><text x="142" y="75">GPU1</text><text x="227" y="75">GPU2</text><text x="312" y="75">GPU3</text><text x="397" y="75">GPU4</text><text x="482" y="75">GPU5</text><text x="567" y="75">GPU6</text><text x="652" y="75">GPU7</text></g><g stroke="#6b7484"><path d="M57,95 L310,150"/><path d="M142,95 L325,150"/><path d="M227,95 L340,150"/><path d="M312,95 L350,150"/><path d="M397,95 L360,150"/><path d="M482,95 L375,150"/><path d="M567,95 L390,150"/><path d="M652,95 L405,150"/></g><rect x="275" y="150" width="170" height="62" rx="8" fill="#21252d" stroke="#4f9d78"/><text x="360" y="175" text-anchor="middle" class="svg-t">梯度 all-reduce</text><text x="360" y="197" text-anchor="middle" class="svg-t" font-size="11">每卡 4×累积8×8卡=256 样本</text></svg><figcaption>图 1　累积期间不必每个 micro-step 通信；第 8 步同步后才进行一次全局更新。</figcaption></figure><p>若单卡处理固定工作耗时 800ms，8 卡同样总工作理想 100ms，实测 140ms，则强扩展效率 <code>800/(8×140)=71.4%</code>。额外 40ms 来自通信、同步和负载不均。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>设备</th><th>同一总工作时间</th><th>加速</th><th>效率</th></tr></thead><tbody><tr><td>1</td><td>800ms</td><td>1×</td><td>100%</td></tr><tr><td>8 理想</td><td>100ms</td><td>8×</td><td>100%</td></tr><tr><td>8 实测</td><td>140ms</td><td>5.71×</td><td>71.4%</td></tr></tbody></table></div>'},
    {title:'张量并行拆一层，通信发生在层内',kind:'eng',badge:'张量并行',lead:'单个 4096×16384 MLP 放不下一张卡，怎样沿行列拆？',body:'<p>张量并行（TP）把同一层矩阵或注意力头分给多张卡共同计算。列并行把输出列分给设备，输入共享，各卡算局部输出；后续行并行可消费局部特征并用 all-reduce 汇总。<code>collective</code> 是 all-reduce 等多设备共同参与的集合通信；NVLink 是同一服务器内常见的高速 GPU 互连。</p><p>输入是同一批激活和被分片的层权重，输出要经过集合通信拼回与单卡计算语义一致的层结果。TP 降低每卡权重与部分激活，却使小 batch、慢链路和跨节点延迟显著。度数太高时局部矩阵太小，GPU 利用率下降。通信/计算需通过算子融合和异步重叠隐藏。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>轴</th><th>拆什么</th><th>通信频率</th><th>适合</th></tr></thead><tbody><tr><td>数据并行</td><td>样本</td><td>每次梯度同步</td><td>模型单卡可放/状态可分片</td></tr><tr><td>张量并行</td><td>单层矩阵/头</td><td>几乎每层</td><td>单层太大、高速互连</td></tr><tr><td>序列并行</td><td>序列维激活</td><td>与 TP 配合</td><td>长序列激活</td></tr></tbody></table></div><p>每卡内存下降而 tokens/s/GPU 也下降，通常说明通信或局部算子太小抵消了收益；张量并行更适合高速互连域，不应仅为“多用几张卡”跨慢网络扩大。</p>'},
    {title:'流水线并行拆层，气泡由 micro-batch 填充',kind:'math',badge:'流水线',lead:'4 个阶段、8 个 micro-batch 的理想气泡比例是多少？',body:'<p>流水线并行把连续模型层分成 p 个阶段；一个大 batch 再拆成 m 个 micro-batch，依次在阶段间前向和反向。“气泡”是某阶段等待数据、没有有效计算的时间。最简单同步 GPipe 调度中，单方向直觉下效率近似 m/(m+p−1)；p=4、m=8 为 8/11≈72.7%，气泡约 27.3%。</p><div class="dd-formula" data-formula-id="distributed-training-pipeline-bubble"><math display="block" aria-label="理想流水线气泡比例约等于阶段数 p 减一，除以 micro-batch 数 m 加阶段数 p 减一"><mrow><msub><mi>f</mi><mtext>气泡</mtext></msub><mo>≈</mo><mfrac><mrow><mi>p</mi><mo>−</mo><mn>1</mn></mrow><mrow><mi>m</mi><mo>+</mo><mi>p</mi><mo>−</mo><mn>1</mn></mrow></mfrac></mrow></math></div><p class="dd-formula-note"><code>f气泡</code> 是理想化空闲时间比例，<code>p</code> 是流水线阶段数，<code>m</code> 是一个批次拆出的 micro-batch 数。式子只描述阶段成本相近的简化调度，不包括通信、前后向差异和交错调度细节。</p><p>增加 m 减气泡但增加激活驻留、调度开销和过小 micro-batch 的内核低效；1F1B 表示稳定阶段交替执行一次前向和一次反向，可降低峰值激活。阶段算力不均时最慢 stage 决定节拍，应按层成本而非层数平均切。</p><div class="dd-note warn"><b>流水线状态有版本。</b>　异步或交错调度可能让不同 micro-batch 使用不同权重版本，需要明确一致性语义。</div>'},
    {title:'ZeRO / FSDP 分片数据并行冗余状态',kind:'eng',badge:'状态分片',lead:'数据并行每卡都复制 Adam 状态，怎样按 rank 分摊？',body:'<p>ZeRO 与 FSDP 都让数据并行 rank 不再各自长期保存完整训练状态。ZeRO-1 分片优化器状态，ZeRO-2 再分片梯度，ZeRO-3/FSDP 再分片参数；计算某层前用 <code>all-gather</code> 从各卡收集本层所需参数，反向后用 <code>reduce-scatter</code> 求和梯度并只把不同分片留在各卡。</p><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>阶段</th><th>优化器</th><th>梯度</th><th>参数</th></tr></thead><tbody><tr><td>普通 DP</td><td>复制</td><td>复制</td><td>复制</td></tr><tr><td>ZeRO-1</td><td>分片</td><td>复制</td><td>复制</td></tr><tr><td>ZeRO-2</td><td>分片</td><td>分片</td><td>复制</td></tr><tr><td>ZeRO-3/FSDP</td><td>分片</td><td>分片</td><td>分片/按需聚合</td></tr></tbody></table></div><p>输入是按 rank 分片的参数与状态，输出仍应等价于一次同步数据并行更新。参数 all-gather 的粒度、预取和重计算需匹配网络与层大小；offload 是把部分状态移到 CPU 内存或 NVMe 存储，能容纳更大模型却可能被搬运带宽限制。</p><p>每卡峰值下降而集合通信占比上升，是用通信换内存的预期结果；若恢复检查点或不同 world size 时无法重分片，则部署弹性仍不合格。</p>'},
    {title:'3D 并行是按拓扑组合，而非随意相乘',kind:'eng',badge:'组合',lead:'64 GPU 怎样分成 DP=8、TP=4、PP=2，通信域如何不同？',body:'<p>DP、TP、PP 分别表示数据并行、张量并行和流水线并行的度数。总设备 8×4×2=64。TP 组内每层频繁通信，优先放同节点；PP 相邻阶段传激活；DP 组同步梯度或状态，频率相对低但数据量大。专家并行还可能加入 all-to-all，让每个设备同时向多方发送并接收 token。</p><p>rank 映射必须拓扑感知，避免 TP 跨慢网。全局 batch 只由 DP 副本、局部 micro-batch 与累积步数决定，不乘 TP/PP；因为 TP/PP 合作处理同一批样本。</p><div class="dd-formula" data-formula-id="distributed-training-3d-global-batch"><math display="block" aria-label="全局批量等于局部微批量乘梯度累积步数乘数据并行度，不乘张量并行度或流水线并行度"><mrow><msub><mi>B</mi><mtext>全局</mtext></msub><mo>=</mo><msub><mi>B</mi><mtext>微批</mtext></msub><mo>·</mo><mi>a</mi><mo>·</mo><msub><mi>N</mi><mtext>数据并行</mtext></msub><mspace width="0.8em"/><mo>(</mo><mtext>不乘 TP 或 PP</mtext><mo>)</mo></mrow></math></div><p class="dd-formula-note"><code>B微批</code> 是每个数据并行副本一次处理的样本数，<code>a</code> 是梯度累积步数，<code>N数据并行</code> 是处理不同样本的数据副本数。TP 与 PP 只是共同完成同一副本的模型计算，因此不增加本次更新看过的样本。</p><p>输入是设备拓扑、三种并行度和 batch 配置，输出是 rank 分组、通信域与不变的优化器语义。64 GPU 全乘入 batch 会把全局批量算大 8 倍或更多，并错误改变学习率和训练轨迹。</p>'},
    {title:'检查点与故障恢复是规模化的一部分',kind:'eng',badge:'可靠性',lead:'训练两周后一个节点故障，怎样保证不是从头再来或恢复出错？',body:'<p>保存模型、优化器、调度器、随机数、数据游标、损失缩放和并行元数据。分片检查点需支持在不同 world size 重分片或明确限制；写入采用临时产物+原子清单，避免一半 rank 成功的损坏快照。</p><p>checkpoint 间隔在写入开销与丢失计算间取舍；大规模故障率随设备数上升。定期执行真实恢复演练，比较恢复后 loss/样本序列，而不只检查文件存在。弹性训练还需处理成员变化的 batch 与随机流。</p><div class="dd-note warn"><b>数据恰好一次语义很难。</b>　恢复可能重复或跳过样本，必须持久化 sampler 状态和全局 step。</div>'},
    {title:'扩展评测要区分强扩展和弱扩展',badge:'评测',lead:'GPU 翻倍，应该保持总问题大小还是每卡问题大小？',body:'<p>强扩展固定总模型与总工作，测增加设备后同一任务加速多少；弱扩展固定每卡工作，让总问题随设备增加，测单位效率能否保持。输入是不同设备数下的同一实验协议，输出是速度、效率、内存、功耗、恢复与最终质量的联合报告。</p><p><code>tokens/s</code> 是集群每秒处理 token 数，<code>tokens/s/GPU</code> 是每卡效率；MFU 是实际有效模型计算相对硬件理论峰值的比例；collective 占比是集合通信占 step 时间的比例。还应报告数据等待、气泡、重计算、显存峰值、功耗和恢复时间。</p><p>同时验证训练语义：global batch、学习率、token 数、数据顺序、dropout 随机流、loss scaling 与归约精度。吞吐提高但最终质量下降不是成功扩展。MFU 低也只说明硬件未充分用于模型计算，不能单独判断瓶颈来自数据、通信还是小算子。</p><div class="dd-note"><b>瓶颈定位：</b>用 trace 将一个 step 拆成数据读取、前向、反向、集合通信、优化器和检查点，再把时间占比与等待依赖对应起来；GPU 利用率低只是症状。</div>'},
    {title:'常见误区与概念依赖',badge:'常见误区与学习路线',lead:'分布式训练的核心是数据与状态语义，而不只是设备数量。',body:'<div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>误区</th><th>更准确的理解</th></tr></thead><tbody><tr><td>GPU 翻倍速度翻倍</td><td>通信、同步、输入和小矩阵限制效率</td></tr><tr><td>权重放下即可训练</td><td>梯度、优化器、激活和缓冲常更大</td></tr><tr><td>所有 GPU 都乘入全局批量</td><td>只乘数据并行副本，不乘 TP/PP</td></tr><tr><td>ZeRO 只省内存无代价</td><td>按需聚合增加通信与检查点复杂度</td></tr><tr><td>loss 连续即可证明恢复正确</td><td>还需数据游标、随机流和状态一致</td></tr></tbody></table></div><div class="dd-table-wrap"><table class="dd-table"><thead><tr><th>层级</th><th>概念依赖与延伸学习</th></tr></thead><tbody><tr><td>先修</td><td>反向传播、优化器、集合通信与 GPU 内存</td></tr><tr><td><b>本页核心</b></td><td>DP/TP/PP、ZeRO/FSDP、全局批量、拓扑和恢复</td></tr><tr><td>架构</td><td>MoE 专家并行、长上下文、梯度检查点</td></tr><tr><td>规划</td><td>缩放定律、训练数据治理与可观测性</td></tr></tbody></table></div>'}
  ],
  chain:['核算参数/状态/激活峰值','选择 DP/TP/PP/分片组合','按拓扑映射通信域','固定全局批量与数值语义','trace 优化计算通信重叠','真实恢复并以质量/效率联合验收'],
  quiz:[{q:'8卡、每卡4、累积8的全局批量？',a:'4×8×8=256。'},{q:'8卡实测140ms相对单卡800ms效率？',a:'800/(8×140)≈71.4%。'},{q:'TP/PP 为什么不乘入 global batch？',a:'它们协作处理同一批样本，只有 DP 副本处理不同样本。'},{q:'4阶段8 micro-batch 气泡近似多少？',a:'3/(8+3)=27.3%。'},{q:'ZeRO-3 分片什么？',a:'优化器状态、梯度和参数。'}],
  sources:[{title:'Megatron-LM',url:'https://arxiv.org/abs/1909.08053',note:'张量模型并行'},{title:'ZeRO',url:'https://arxiv.org/abs/1910.02054',note:'训练状态分片'},{title:'GPipe',url:'https://arxiv.org/abs/1811.06965',note:'流水线并行与 micro-batch'},{title:'PyTorch FSDP',url:'https://pytorch.org/docs/stable/fsdp.html',note:'全分片数据并行实现语义'}]
});
