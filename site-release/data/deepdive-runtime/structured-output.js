(window.DEEPDIVE=window.DEEPDIVE||{})["structured-output"]={"title":"结构化输出：把概率文本接入确定性软件契约","subtitle":"从最小 schema、约束生成和分层校验，到版本迁移、有限修复与安全执行。","thesis":"结构化输出把模型回答转换为可解析、可校验、可版本化的数据契约。可靠链路不是“看起来像 JSON”，而是<b>生成时约束、完整性判断、schema 校验、业务/事实校验、执行时鉴权</b>；任何前一层通过都不能替代后一层。","quality":{"contractVersion":2,"examples":[{"section":4,"evidence":{"setup":"amount=120","rule":"最多可退 80","steps":"可退上限 ¥80","interpretation":"不能静默截成 80 执行"}}],"formulas":[{"id":"structured-output-refundable","section":4,"formulaIndex":1,"symbols":[{"name":"R","meaning":"订单当前最多可退金额","evidence":"R 是本订单当前最多可退金额"},{"name":"P","meaning":"订单实付金额","evidence":"P 是订单实付金额"},{"name":"D","meaning":"此前已退款金额","evidence":"D 是此前已退款金额"},{"name":"L","meaning":"政策允许的金额上限","evidence":"L 是政策允许的金额上限"},{"name":"min","meaning":"取候选上限中的较小值","evidence":"取两个候选上限中较小者"}]},{"id":"structured-output-constrained-proportional","section":5,"formulaIndex":1,"symbols":[{"name":"P","meaning":"原始或约束后的 token 概率","evidence":"原始 token t 的概率"},{"name":"t","meaning":"当前候选 token","evidence":"token t 的概率"},{"name":"s","meaning":"当前解析状态","evidence":"状态 s"},{"name":"M","meaning":"当前合法 token 集","evidence":"当前仍合法的 token 集"}]}],"sectionContracts":[{"section":1,"definition":{"answer":"结构化输出把模型回答变成可解析、校验、拒绝和版本化的数据对象。","evidence":"结构化输出输入任务语义和版本化数据契约"},"problem":{"answer":"它解决自然语言变体对程序接口造成解析和状态歧义的问题。","evidence":"为什么人能理解而程序会崩"},"inputOutput":{"answer":"输入任务语义与契约，输出可解析校验且可拒绝的对象。","evidence":"输出可解析、可校验并可被程序拒绝的对象"},"mechanism":{"answer":"schema、约束生成和解析器把字段类型边界变成机械检查。","evidence":"把自然语言容错转成明确字段和状态"},"interpretation":{"answer":"格式有效只表示机器能读，不表示内容真实。","evidence":"格式有效只表示机器能读取"},"boundary":{"answer":"结构化对象仍不能自动获得执行权限。","evidence":"不表示对象内容可信或允许执行"}},{"section":2,"definition":{"answer":"四道门依次验证完整语法、schema、业务事实和权限副作用。","evidence":"四道门必须逐层通过"},"problem":{"answer":"它说明 JSON 可解析距离安全执行退款还差哪些检查。","evidence":"离执行退款还差多远"},"inputOutput":{"answer":"输入模型对象、schema、业务数据和主体，输出通过、错误或升级。","evidence":"输出通过、字段错误、补信息、拒绝或升级"},"mechanism":{"answer":"按语法、形状、事实关系、身份权限的顺序逐层检查。","evidence":"依次检查完整语法、schema、业务事实和权限副作用"},"interpretation":{"answer":"前一门通过只表示该层合格，不能替代后一层。","evidence":"前一层通过不能替代后一层"},"boundary":{"answer":"任何一层失败都必须阻断执行，不允许下游猜测。","evidence":"任何失败都禁止下游猜测执行"}},{"section":3,"definition":{"answer":"最小 schema 只要求模型生成必须进行语义判断的字段。","evidence":"最小 schema 比大而全更可靠"},"problem":{"answer":"它减少大对象的无效路径、消费者耦合和版本迁移风险。","evidence":"为什么会提高错误率和耦合"},"inputOutput":{"answer":"输入业务决策字段和兼容要求，输出最小字段及显式分支。","evidence":"输出只含模型必须判断的字段与显式分支"},"mechanism":{"answer":"服务器填权威派生值，枚举与 discriminated union 拆小任务。","evidence":"服务器填身份、时间和派生值"},"interpretation":{"answer":"字段更少表示模型责任更窄，不表示业务信息被删除。","evidence":"只让模型产生需要语义判断的字段"},"boundary":{"answer":"过度嵌套会增加状态空间、无效路径与迁移成本。","evidence":"过度嵌套会增加无效路径、版本耦合和迁移成本"}},{"section":4,"definition":{"answer":"本案例展示语法和 schema 合法的退款对象仍可在事实与权限层失败。","evidence":"合法对象如何在后两层失败"},"problem":{"answer":"它解释 amount 120 在通用范围内却不能安全执行的原因。","evidence":"为什么仍不能执行"},"inputOutput":{"answer":"输入对象、订单政策和角色，输出业务错误码或安全动作。","evidence":"输出业务错误码或安全动作"},"mechanism":{"answer":"用实付、已退和政策上限计算实际可退额，再检查批准权限。","evidence":"公式算得最多可退 80"},"interpretation":{"answer":"schema 的 0 到 500 只表示通用形状范围，不代表订单可退额。","evidence":"违反订单不变量"},"boundary":{"answer":"不能静默把 120 改成 80 执行，须解释或转人工。","evidence":"不能静默截成 80 执行"}},{"section":5,"definition":{"answer":"生成时约束和生成后校验分别减少无效路径与捕获语义错误。","evidence":"约束生成与生成后校验是互补的"},"problem":{"answer":"它解释服务端会校验时为何仍值得在生成阶段约束。","evidence":"为什么还要生成时约束"},"inputOutput":{"answer":"输入模型分布、schema 与业务规则，输出受约束且复验的对象。","evidence":"输出格式受约束且经后验验证的对象"},"mechanism":{"answer":"生成时掩码非法 token，生成后检查实现、版本和语义。","evidence":"生成时掩码减少无效重试"},"interpretation":{"answer":"结构通过率上升不保证业务正确率或任务质量也上升。","evidence":"不能只庆祝结构通过率"},"boundary":{"answer":"自动修复仅适合纯语法且能证明语义等价的情况。","evidence":"只有纯语法且可证明等价的修复"}},{"section":6,"definition":{"answer":"失败恢复按失败层选择局部、有限且无副作用的下一动作。","evidence":"失败恢复要局部、有限且无副作用"},"problem":{"answer":"它避免无限重试增加延迟、注入面和重复执行风险。","evidence":"无限重试会发生什么"},"inputOutput":{"answer":"输入字段路径错误码范围和幂等键，输出修复、澄清、拒绝或升级。","evidence":"输出局部修复、澄清、重取事实、拒绝或人工升级"},"mechanism":{"answer":"针对错误类型有限重试，校验通过前绝不执行副作用。","evidence":"重试次数必须有限"},"interpretation":{"answer":"连续同类失败表示当前 schema 或任务路径不适合继续生成。","evidence":"同类连续失败应降级"},"boundary":{"answer":"校验完成前不得产生副作用，执行重试还须复用幂等键。","evidence":"校验前不产生副作用"}},{"section":7,"definition":{"answer":"schema 版本演进是生产者与所有消费者共同参与的分布式契约迁移。","evidence":"版本演进是一项分布式契约"},"problem":{"answer":"它防止新字段或枚举让旧消费者拒绝或静默误执行。","evidence":"旧消费者为什么可能静默误读"},"inputOutput":{"answer":"输入新旧 schema、组件版本和 trace，输出兼容矩阵与迁移计划。","evidence":"输出兼容矩阵与 expand/contract 发布计划"},"mechanism":{"answer":"先更新消费者兼容新旧，再切生产者，最后退役旧契约。","evidence":"先让消费者接受新旧"},"interpretation":{"answer":"新增语法项不自动等于向后兼容，需按消费者行为判断。","evidence":"不能仅按“新增”判断兼容"},"boundary":{"answer":"提示、schema、编译器和消费者版本必须一起记录回放。","evidence":"回放旧 trace 才能复现当时规则"}},{"section":8,"definition":{"answer":"安全执行用认证主体、工具白名单、策略与审批重新验证合法参数。","evidence":"安全执行不信任模型提供的身份与工具名"},"problem":{"answer":"它防止提示注入诱导模型选择语法允许但不合适的动作或金额。","evidence":"为什么提示注入仍可能造成伤害"},"inputOutput":{"answer":"输入合法参数和权威安全状态，输出最小权限执行或拒绝。","evidence":"输出以最小权限执行或拒绝"},"mechanism":{"answer":"服务器重取认证身份并校验对象、金额、工具、速率和审批。","evidence":"服务器重新鉴权并限制金额与对象"},"interpretation":{"answer":"schema 合法只限制形状，字符串中仍可能携带攻击载荷。","evidence":"结构化字符串仍可携带注入载荷"},"boundary":{"answer":"模型不能继承服务账号权限，每次工具调用都要重新鉴权。","evidence":"每次工具调用都以最小权限和当前主体重新鉴权"}},{"section":9,"definition":{"answer":"端到端契约评测沿生成、校验、执行和业务后果逐层定位失败。","evidence":"评测应沿整条契约定位失败"},"problem":{"answer":"它补足结构化输出成功率无法反映事实、权限与副作用的缺陷。","evidence":"还缺哪些能决定上线的数字"},"inputOutput":{"answer":"输入输出、日志、工具结果和后果，输出多层质量、延迟和成本指标。","evidence":"输出语法、schema、字段、事实、权限、幂等、任务质量、延迟与成本指标"},"mechanism":{"answer":"先查完整，再查形状、事实关系和当前主体能否执行。","evidence":"先问输出是否完整"},"interpretation":{"answer":"最终损失决定错误严重性，不能把越权与无害重试等权平均。","evidence":"错误预算按最终损失分级"},"boundary":{"answer":"单次越权必须设独立硬门和即时回滚，不能被平均成功率抵消。","evidence":"不能被大量无害格式成功平均掉"}}]},"html":"<div class=\"dd-goals\"><b>读完你应该能：</b>区分语法/schema/语义/权限；设计最小可演进 schema；演算字段校验与修复；安全连接工具和数据库。</div>\n<ol class=\"dd-chain\"><li>定义最小版本化 schema</li><li>生成时约束并等待完整接受态</li><li>服务端重新解析与 schema 校验</li><li>验证事实和跨字段业务不变量</li><li>以当前主体鉴权后幂等执行</li><li>按失败层记录并有限恢复/回滚</li></ol>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">1</span>自然语言和程序接口的容错方式不同<span class=\"dd-badge intuition\">直觉</span></h2>\n<div class=\"dd-agent-response\"><p>人理解一段话，靠的是语境与意图。消息里多一句“好的”、少一个引号、把逗号写成中文全角，只要意思没变，人几乎不会察觉。程序恰好相反：它不揣测意图，只按事先声明的规则逐字比对。调用方期望的是一个对象，收到的是一个字符串，程序就在类型检查处崩溃；字段叫 userName 而约定写 username，后面的逻辑就拿到空值。自然语言的容错来自共同语境下的宽容，程序的容错来自边界的精确：只要边界声明得足够清楚，异常就可以被机械地识别和拒绝，而不需要人去猜测“模型这次大概是什么意思”。</p>\n<p>结构化输出正是把前一种沟通方式改造成后一种。它的输入是两部分：任务语义，即“这次调用要回答什么问题、返回什么内容”；以及版本化的数据契约，即“回答必须长成什么形状”。它的输出不是自由文本，而是可解析、可校验、并且可以被程序在字段级别拒绝的对象。生成约束与解析器一起完成这个转换：把自然语言里靠语境消解的容错，转成明确的字段和明确的状态——字段有没有、类型对不对、值在不在允许范围内，每一项都有确定答案。</p>\n<p>这个转换落地为一条逐级收紧的链条。在提示里写“返回 JSON”只提高输出为 JSON 的概率，模型仍可能加注释、加解释文字，或者根本不返回 JSON。JSON 模式通常能保住语法：输出被约束为合法的 JSON 文本。但合法 JSON 只保证机器读得动，不保证内容可用——字段可以少、类型可以错、值可以超范围。JSON Schema 在这一层之上进一步约束形状，声明字段、必填项、枚举值、长度限制和版本号，消费者据此机械地拒绝异常对象，而不是用脆弱的正则表达式去猜模型的意图。链条的末端是约束解码：在生成过程中就阻止非法路径，让不符合契约的 token 序列根本没有机会被采样出来。四层手段的收紧程度依次提高，但每层的支持范围依具体实现而异，因此无论上游宣称支持到哪一层，接收侧都必须做服务端复验。</p>\n<p>这里需要把“格式有效”与“内容可信”分开。一个对象通过了所有语法和形状校验，只说明机器能读取它、能定位到约定的字段，不说明字段里的值是真的，更不说明允许执行。契约把校验问题的答案变成了布尔值，但模型对事实的判断仍然是概率性的。数据契约的价值正在于让失败显式、局部、可计数：哪次调用、哪个字段、违反哪条约束，都可以被记录和定位，而不是让错误像错别字一样融进一大段自然语言里事后无从追查。它约束的是输出形态，不是模型的知识可靠性；它让概率模型可以被确定性软件安全地消费，却不会把概率模型变成可信数据库。</p></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">2</span>四道门必须逐层通过<span class=\"dd-badge eng\">分层</span></h2>\n<div class=\"dd-agent-response\"><p>能被 JSON.parse 解析，距离“执行退款”还很远。解析成功只证明这段文本是合法 JSON，它回答不了任何业务问题：字段在不在、值合不合理、请求者有没有资格。要把一个模型生成的字符串变成一次真正执行的退款，中间要过四道门，而且必须逐层通过——前一层通过不能替代后一层，任何一层失败，下游都不该“尽量猜着继续”。</p>\n<p>分层验证的输入有四样：模型返回的对象、约定好的 schema、权威的业务数据（来自数据库或服务端状态）、以及当前请求的主体（谁在发起这次操作）。输出是五种判定之一：通过、字段错误、需要补充信息、拒绝、或升级。四道门各自检查一类不同性质的问题：</p>\n\n<p>第一道门只做纯语法判断：括号配对、引号闭合、转义合法、文本在正确的位置结束。模型输出经常被截断或夹带解释文字，这一层把“不是完整 JSON”的输入挡在门外，失败就拒绝整条消息或触发重新生成。</p>\n<p>第二道门对照 schema 检查形状：该有的字段是否齐全、类型是否匹配、枚举值是否在允许集合内、数值是否落在声明范围内。action 字段只允许 refund 和 ask 两个值，模型写了别的字符串，错误被精确定位到字段级别，而不是让下游代码拿着错误值继续跑。</p>\n<p>第三道门处理业务事实。schema 正确不代表内容正确：金额字段可以合法地是数字，却超过订单实际可退额度。这一层检查跨字段规则（两个字段各自合法、组合起来矛盾）、查询数据库核对状态、要求证据支撑，例子里就是 amount ≤ 订单可退额。失败时系统该做的是向用户补充询问或转人工，而不是自行猜一个“看起来合理”的值。</p>\n<p>第四道门处理权限与副作用：执行会改变现实状态，所以必须确认主体是谁、策略允不允许、请求是否幂等、是否需要审批。例子里要回答的问题是“当前客服能否批准这笔退款”。失败意味着禁止执行或升级到有权的人。</p>\n<p>四道门各回答一个下游无法代答的问题，且必须按序通过：语法层无法替 schema 层判断字段合法性，schema 层无法替业务层核对事实，业务层也无法替权限层决定谁能执行。任何一层的失败都让流程在这一层停下，明确地拒绝或升级。值得特别强调的是，模型给出的 user_id、金额和 action 全都是不可信建议：它们只是被结构化了的文本，没有因此获得任何可信度。执行层必须从认证上下文重新取得主体与权限，而不能拿模型声称的身份去查库、去授权。</p></div>\n<table class=\"dd-table\"><thead><tr><th>层</th><th>检查</th><th>例子</th><th>失败处理</th></tr></thead><tbody><tr><td>完整语法</td><td>括号、引号、转义、结束态</td><td>不是半截 JSON</td><td>拒绝/重生</td></tr><tr><td>Schema</td><td>字段、类型、枚举、范围</td><td>action∈refund/ask</td><td>字段级错误</td></tr><tr><td>业务与事实</td><td>跨字段规则、查库、证据</td><td>amount≤订单可退额</td><td>补信息/转人工</td></tr><tr><td>权限与副作用</td><td>主体、策略、幂等、审批</td><td>当前客服能否批准</td><td>禁止或升级</td></tr></tbody></table>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">3</span>最小 schema 比大而全更可靠<span class=\"dd-badge eng\">设计</span></h2>\n<div class=\"dd-agent-response\"><p>把所有可能用到的字段都塞进一个嵌套对象，看起来一步到位，实际同时抬高了错误率和耦合：每多一个可选字段，就多一批“合法但无意义”的组合；每个字段的改动都可能牵动其他字段的校验、消费者的解析和将来的迁移。最小 schema 的思路反着来：输入是业务上真正需要模型判断的字段、服务器本来就知道的事实、以及兼容性要求，输出只包含模型必须判断的字段和显式分支，其余一概不交给模型。</p>\n<p>切分的第一条原则是：只让模型产生需要语义判断的内容。时间戳、用户 ID、总价、派生状态这些值服务器在请求上下文里已经掌握，模型转述反而可能抄错，由服务器填充。模型负责的只是它独有的贡献——从用户语言里判断出的意图、参数和理由。身份由服务器填写，而不是相信模型声称的身份，与验证层的结论一致。</p>\n<p>切分的第二条原则是用枚举和 discriminated union 缩小状态空间。动作名不要用自由字符串：enum 让消费者不用猜拼写，但也留下一个后续成本——新增枚举值需要兼容处理，旧消费者遇到新值必须能安全降级。互斥的任务用 discriminated union 表达：先用 kind 字段在 refund 和 ask 之间做选择，再套用对应的小 schema。两个小 schema 各自只声明自己需要的字段，比一个装着几十个可选字段的大对象更容易约束、校验和迁移，因为无效组合在一开始就不可能被表达出来。</p>\n<p>字段声明本身也有一套收紧规则：区分 missing 与 null——字段缺失和显式为 null 是两种状态，混为一谈会丢失信息；限制字符串与数组的长度，挡住资源耗尽和荒谬的超长值；默认拒绝 additionalProperties，让未知字段直接暴露 schema 漂移，而不是被静默吞掉。每一条规则的收益都对应一个必须管理的风险：</p>\n\n<p>必填字段保证状态完整，代价是面对未知值时不能图省事填一个假默认值——宁可显式报错或询问；长度与范围限制挡住极端输入，但边界值必须跟业务规则同步更新，否则校验器会拒绝真实的合法请求；禁止额外字段让新旧版本的差异立刻可见，代价是每次改动都要在提供方与消费者之间协商。</p>\n<p>这些规则合起来指向同一个结论：schema 越小，模型要做的选择越少，每项声明都对应一个真实存在的业务决策；schema 越大，声明越像一纸空文，错误率随路径数量一起膨胀。同时每个字段都应附带业务含义说明和正反例子，让校验失败时的错误信息能告诉人“缺什么、为什么错”，而不是只报一行堆栈。</p></div>\n<table class=\"dd-table\"><thead><tr><th>设计选择</th><th>收益</th><th>风险</th></tr></thead><tbody><tr><td>枚举动作</td><td>消费者无需猜拼写</td><td>新增值需兼容</td></tr><tr><td>必填字段</td><td>状态完整</td><td>未知值不要用假默认</td></tr><tr><td>长度/范围</td><td>限制资源与荒谬值</td><td>边界需与业务同步</td></tr><tr><td>禁止额外字段</td><td>发现 schema 漂移</td><td>前后版本需协商</td></tr></tbody></table>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">4</span>运行示例：合法对象如何在后两层失败<span class=\"dd-badge math\">案例推演</span></h2>\n<div class=\"dd-agent-response\"><p>模型返回 action=refund、amount=120。这段输出通过了语法门——它是完整合法的 JSON；也通过了 schema 门——action 在允许的枚举里，amount 落在 schema 声明的通用范围 0–500 之内。两个门都放行，却仍然不能执行。问题出在后面两道门上，而它们检查的对象已经不再是模型输出本身。</p>\n<p>案例的输入有四样：结构合法的对象、订单的实付与退款记录、政策金额上限、以及当前操作者的角色。输出是业务错误码或安全动作。业务事实门要核对一条订单不变量：可退金额 R 等于实付金额 P 减去已退款金额 D，与政策上限 L 两者中取较小者，即</p>\n<p>R = min(P − D, L)</p>\n<p>其中 P 是订单实付金额，D 是此前已退款金额，L 是政策允许的金额上限，min 表示取两个候选上限中较小的一个。代入本订单的数据，算得最多可退 80。120 超过了 80，违反订单不变量。注意这条判断依赖权威的订单与政策数据，schema 的通用范围无法代替它：0–500 里的 120 在形状上毫无问题，问题的性质完全不同。权限门接着给出第二个拒绝理由：当前角色的审批权限覆盖不了这笔金额，没有批准权。</p>\n<p>图示把这条路径画成退款结构化输出依次经过语法、schema、业务事实和权限四道验证门的流程。图传达的核心关系是：格式保证只让错误更容易被检测，数据库里的事实和当前主体的权限才最终决定能否执行。前两道门是模型输出对契约的符合性检查，后两道门把输出放回真实世界对照。</p>\n<p>失败后的正确恢复是返回业务错误码 amount_exceeds_remaining，让模型据此改为解释说明或引导转人工，而不是“悄悄处理掉”：把 120 静默截成 80 再执行，等于系统擅自改写模型的意图——模型可能确实认为该退 120，截断掩盖了上游某处的问题，也把“系统发现了违规”变成“系统按自己的理解执行了另一笔退款”。宁可让流程以显式错误停在原地，也不能用猜测修补一个校验失败的对象。</p></div>\n<figure class=\"dd-fig\"><svg viewBox=\"0 0 710 260\" role=\"img\" aria-label=\"退款结构化输出经过语法、schema、业务事实和权限四道验证门\"><defs><marker id=\"so1\" markerWidth=\"8\" markerHeight=\"8\" refX=\"6\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L6,3 L0,6 z\" fill=\"#6b7484\"/></marker></defs><rect x=\"16\" y=\"85\" width=\"135\" height=\"88\" rx=\"8\" fill=\"#21252d\" stroke=\"#6b8cbe\"/><text x=\"83\" y=\"109\" text-anchor=\"middle\" class=\"svg-t\">模型对象</text><text x=\"83\" y=\"131\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">action=refund</text><text x=\"83\" y=\"150\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">amount=120</text><path d=\"M151,129 L188,129\" stroke=\"#6b7484\" marker-end=\"url(#so1)\"/><rect x=\"190\" y=\"85\" width=\"112\" height=\"88\" rx=\"8\" fill=\"#21252d\" stroke=\"#4f9d78\"/><text x=\"246\" y=\"110\" text-anchor=\"middle\" class=\"svg-t\">语法 ✓</text><text x=\"246\" y=\"132\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">完整 JSON</text><text x=\"246\" y=\"151\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">无截断</text><path d=\"M302,129 L339,129\" stroke=\"#6b7484\" marker-end=\"url(#so1)\"/><rect x=\"341\" y=\"85\" width=\"112\" height=\"88\" rx=\"8\" fill=\"#21252d\" stroke=\"#4f9d78\"/><text x=\"397\" y=\"110\" text-anchor=\"middle\" class=\"svg-t\">Schema ✓</text><text x=\"397\" y=\"132\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">枚举合法</text><text x=\"397\" y=\"151\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">0≤amount≤500</text><path d=\"M453,129 L490,129\" stroke=\"#6b7484\" marker-end=\"url(#so1)\"/><rect x=\"492\" y=\"57\" width=\"196\" height=\"70\" rx=\"8\" fill=\"#21252d\" stroke=\"#c77b72\"/><text x=\"590\" y=\"82\" text-anchor=\"middle\" class=\"svg-t\">业务/事实 ✕</text><text x=\"590\" y=\"104\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">订单实付 ¥80，可退上限 ¥80</text><rect x=\"492\" y=\"145\" width=\"196\" height=\"70\" rx=\"8\" fill=\"#21252d\" stroke=\"#c77b72\"/><text x=\"590\" y=\"170\" text-anchor=\"middle\" class=\"svg-t\">权限 ✕</text><text x=\"590\" y=\"192\" text-anchor=\"middle\" class=\"svg-t\" font-size=\"11\">当前角色只能建议，不能批准</text><path d=\"M590,127 L590,143\" stroke=\"#6b7484\" marker-end=\"url(#so1)\"/></svg><figcaption>图 1　格式保证只让错误更容易检测；数据库事实和当前主体权限仍决定能否执行。</figcaption></figure>\n<div class=\"dd-formula\" data-formula-id=\"structured-output-refundable\"><math display=\"block\" aria-label=\"可退金额 R 等于实付金额 P 减已退款金额 D 与政策上限 L 两者中的最小值，本例为八十\"><mrow><mi>R</mi><mo>=</mo><mi>min</mi><mo>(</mo><mi>P</mi><mo>−</mo><mi>D</mi><mo>,</mo><mi>L</mi><mo>)</mo><mo>=</mo><mi>min</mi><mo>(</mo><mn>80</mn><mo>−</mo><mn>0</mn><mo>,</mo><mn>100</mn><mo>)</mo><mo>=</mo><mn>80</mn></mrow></math></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">5</span>约束生成与生成后校验是互补的<span class=\"dd-badge math\">机制</span></h2>\n<div class=\"dd-agent-response\"><p>既然服务端最终都会校验，为什么还要在生成时约束？因为生成时约束和服务端校验守的是两类不同的错误，省的是两种不同的成本。这套双层机制的输入是模型分布、schema 和服务端业务规则，输出是格式受约束且经过后验验证的对象：约束解码减少语法与 schema 层面无效的输出，从而减少无效重试；生成后校验则防实现缺陷、版本错配和语义错误——这些是解码器承诺与实际情况不符时才会出现的问题，在生成时根本不可见。</p>\n<p>约束解码的机制是受限采样：在每一步，把当前状态下非法的 token 概率置零，再对剩下的合法 token 重新归一化。写成公式，状态 s 下 token t 的受限概率 P′(t|s) 与原概率 P(t|s) 乘一个指示函数成正比：</p>\n<p>P′(t|s) ∝ P(t|s) × 1[t ∈ M(s)]</p>\n<p>其中 P(t|s) 是状态 s 下原始 token t 的概率，P′(t|s) 是应用约束后的概率，M(s) 是当前仍合法的 token 集，1[·] 是合法取 1、非法取 0 的指示函数，符号 ∝ 表示右侧还要除以全部合法候选的概率总和才能归一化为概率分布。</p>\n<p>这个公式同时说明了约束的边界。指示函数只是把非法候选的概率砍掉、把剩下的按比例放大，它没有新增任何信息。如果模型原本把大部分概率压在非法候选上，归一化之后，它只能被迫选择概率很低的合法值——格式通过了，内容却可能变差。所以不能只看结构通过率：格式成功但业务质量下降，恰恰是约束把分布问题推到了台面上。正确的度量要同时看首次结构通过率、业务正确率和任务质量，三者缺一不可。</p>\n<p>正因两层机制各管一摊，生成后校验不可省。底层 API 对 schema 特性的支持范围依实现而异，宣称支持不等于真的支持：某些嵌套结构、某些关键字可能在运行时被忽略。稳妥的做法是在启动时跑契约测试验证声明，并在每次生成后复验，而不是默认相信文档。与之配套的原则是不要用自动修复掩盖分布问题：一个“修 JSON”的模型很可能在补引号的同时改了字段含义。只有纯语法、且可证明与原意等价的修复——比如补齐被截断的收尾括号——才适合自动应用；任何涉及字段值、类型的“修复”都必须回到显式失败与重试的流程里。</p></div>\n<div class=\"dd-formula\" data-formula-id=\"structured-output-constrained-proportional\"><math display=\"block\" aria-label=\"状态 s 下 token t 的受限概率 P prime 与原概率 P 乘合法集合指示函数成正比\"><mrow><msup><mi>P</mi><mo>′</mo></msup><mo>(</mo><mi>t</mi><mo>∣</mo><mi>s</mi><mo>)</mo><mo>∝</mo><mi>P</mi><mo>(</mo><mi>t</mi><mo>∣</mo><mi>s</mi><mo>)</mo><mn>1</mn><mo>[</mo><mi>t</mi><mo>∈</mo><mi>M</mi><mo>(</mo><mi>s</mi><mo>)</mo><mo>]</mo></mrow></math></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">6</span>失败恢复要局部、有限且无副作用<span class=\"dd-badge eng\">可靠性</span></h2>\n<div class=\"dd-agent-response\"><p>校验失败之后最差的处理方式，是把整个错误对象原样塞回模型无限重试。它同时抬高延迟和成本，把不可信文本反复送回上下文，扩大提示注入面，更危险的是每次重试都可能被当成一次新的动作重复执行。失败恢复要同时做到局部、有限、无副作用，三样缺一不可。</p>\n<p>恢复流程的输入不是原始错误消息，而是机器生成的字段路径、错误代码、允许范围，以及请求的幂等键；输出是五种动作之一：局部修复、向用户澄清、重取事实、拒绝、或人工升级。其中“回显给模型什么”有明确规则：只回传机器生成的错误信息——哪个字段违反了哪条约束、允许值是什么——不回显模型自己产生的大段不可信文本。错误对象里可能藏着用户注入的内容，把它原样送回模型等于给注入一个直达的通道。</p>\n<p>无副作用是时间上的硬约束：第一次校验通过之前绝不执行任何副作用。校验本质上是纯读操作，所以它可以免费重试；副作用一旦发生就不可免费撤销。重试必须复用请求 ID 和幂等键，保证同一逻辑请求无论重试多少次，业务系统只认一次。有限则是次数上的硬约束：重试次数必须封顶，同类连续失败不能再硬试，而是降级到更简单的 schema 或确定性的表单——让任务的难度随模型的持续失败而下降，而不是让失败无限循环。</p>\n<p>按失败原因分派动作，恢复才谈得上局部。纯语法失败，比如输出被截断或收尾括号缺失，用约束重生或一次修复处理。缺必填字段且可以向用户询问的，返回 ask 把问题还给用户，而不是替用户编一个值。事实或业务冲突时重新获取权威数据核对，不让模型猜。权限不足则禁止执行，必要时升级人工。每一类失败都停在它自己的层，不扩散成对整个任务的重来。</p>\n<p>最后，这套机制的运转要靠度量支撑：记录首次通过率、每个字段的错误、修复后通过率、最终任务失败率、重试次数和重试带来的额外延迟（p95）。没有这些数字，局部、有限、无副作用就只是口号——只有看到错误集中在哪个字段、重试集中在哪一层，才能知道是 schema 声明得太苛刻、还是模型在这个任务上本就不该被赋予直接生成的责任。</p></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">7</span>版本演进是一项分布式契约<span class=\"dd-badge eng\">版本</span></h2>\n<div class=\"dd-agent-response\"><p>生产者升级了 schema，旧消费者为什么可能静默误读？因为契约不是双方坐下来同时更新的：生产者和消费者各自独立部署，中间必然存在版本交错。旧消费者解析新对象时，可能把新增字段直接丢弃、把新增枚举值塞进错误的默认分支——不报错，却执行了错误逻辑。契约迁移的输入是旧 schema、新 schema、生产者和消费者各自的版本，以及历史 trace；输出是一份兼容矩阵和 expand/contract 的发布计划。</p>\n<p>判断兼容性的第一个陷阱，是认为“新增等于兼容”。新增可选字段通常向后兼容，前提是消费者不拒绝额外字段——一个默认拒绝 additionalProperties 的严格消费者遇到新字段会直接失败，这就不再兼容。新增枚举值同样危险：旧消费者的 switch 语句不认识新值，会落入 default 分支，而 default 分支里往往写着错误的兜底动作。真正的破坏性变化更直接：删除字段、改变字段类型都会破坏读写两端。范围收紧也是一种破坏：把金额上限从 0–500 收到 0–200，旧数据就不再合法。</p>\n<p>因此 schema 必须带版本号和明确的兼容政策，发布采用 expand/contract 三步：先扩展——让消费者同时接受新旧两种格式；再切换——让生产者开始发送新格式；最后收缩——退役旧格式。每类变更的风险和迁移路径如下：</p>\n\n<p>新增可选字段要先把所有消费者更新到能接受该字段，再让生产者输出。新增枚举值的同时，所有消费者要补上显式 unknown 分支，保证遇到不认识的值得不到“默认照常执行”的结果。字段改名或改类型需要双写、双读的过渡期，两套格式并存一段时间后再退役旧字段。收紧范围则要先把存量数据迁移到新范围内，并且校验逻辑按数据所属的版本分别执行，而不是用新规则去审判旧数据。</p>\n<p>版本不止属于 schema 一个文件：提示词、schema、约束编译器和消费者版本要一起记录。出问题时，只有把这几样恢复到当时的组合、回放旧 trace，才能复现当时的规则与行为；只看今天的提示和今天的 schema，永远解释不了昨天的错误。</p></div>\n<table class=\"dd-table\"><thead><tr><th>变更</th><th>典型风险</th><th>迁移</th></tr></thead><tbody><tr><td>新增可选字段</td><td>严格消费者拒绝</td><td>先更新消费者</td></tr><tr><td>新增枚举值</td><td>未知值误执行</td><td>显式 unknown 分支</td></tr><tr><td>字段改名/类型</td><td>破坏读写</td><td>双写/双读后退役</td></tr><tr><td>收紧范围</td><td>旧数据不再合法</td><td>迁移数据并分版本校验</td></tr></tbody></table>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">8</span>安全执行不信任模型提供的身份与工具名<span class=\"dd-badge eng\">安全</span></h2>\n<div class=\"dd-agent-response\"><p>schema 已经把 action 限定在 refund 和 ask 之间，为什么提示注入仍能造成伤害？因为攻击者的目标从来不是让模型输出非法 JSON，而是诱导它选择语法完全合法、内容却有害的参数：refund 是合法动作，但对象可以是别人的订单，金额可以顶到上限，理由里可以藏一段给下游 agent 的指令。结构化约束把“格式非法”这道口子焊死了，剩下的是“格式合法但意图被劫持”的口子。</p>\n<p>安全执行的输入是合法参数、认证主体、工具白名单、策略与审批状态，输出只有两种：以最小权限执行，或拒绝。第一原则是模型提供的三样东西全部不可信：身份、工具名、自由文本。服务器从会话认证取得 user/tenant，不接受模型自报的身份；工具注册表只暴露当前流程允许的能力，模型“指名调用”某个工具名只意味着它建议了那个名字，最终映射到哪个真实能力由注册表和策略决定；每个参数都要过对象级授权、速率、金额、目的与审批校验。高风险动作先由模型生成计划，经过确定性的策略门和人工确认后才提交——计划和建议可以来自模型，决定权必须在确定性代码或人手里。</p>\n<p>自由文本字段是结构化对象里最容易被低估的通道。攻击载荷不需要破坏 schema 也能放进去：SQL 片段、HTML 片段、或者写给下一个 agent 的指令，放进一个字符串字段完全合法。结构化不等于净化，它只是把攻击载荷装进了一个合法字符串。任何消费者在使用这些字段前，必须按目标上下文转义并标记为不可信——渲染成 HTML 前要转义，拼进 SQL 前要参数化，传给下一个 agent 前要隔离。</p>\n<p>这些规则合起来防的是 confused deputy 问题：模型代表用户建议动作，但它不能继承服务账号的全部权限。如果执行层拿模型的输出直接调用一个权限全开的内部接口，模型就成了被操纵的代理人，注入者通过它借到了远大于用户的权限。因此每次工具调用都要以最小权限和当前主体重新鉴权——不是“这一轮已经鉴权过了”，而是每个动作、每个对象、每次调用独立核定。模型输出的身份声明、工具选择和自由文本，全都按不可信数据对待，安全边界才不会随着一个合法的 refund 对象一起被绕过。</p></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">9</span>评测应沿整条契约定位失败<span class=\"dd-badge intuition\">评测</span></h2>\n<div class=\"dd-agent-response\"><p>“结构化输出成功率 99.9%”听起来接近完美，却回答不了上线前最关键的问题：那 0.1% 的失败发生在哪一层、造成了什么后果。这个数字很可能把“重试了一次的格式错误”和“一次越权退款”平均在同一个分母里，而后者的代价远不是前者的数量级。端到端评测的输入是模型输出、验证日志、工具结果和业务后果，输出是沿整条契约展开的指标：语法、schema、字段、事实、权限、幂等、任务质量、延迟与成本。</p>\n<p>按层报告意味着每个门单独有数：完整语法率、schema 首次通过率、死路（重试耗尽的请求）比例、字段级错误分布、业务/事实通过率、权限拦截次数、工具成功次数、重复副作用次数、最终任务质量、额外延迟和成本。一个请求在语法门被拦住还是在权限门被拦住，是性质完全不同的两类失败，混在一起就失去了定位能力。回归集要专门覆盖边界和对抗场景：断流输出、Unicode 文本、超长数组、未知枚举值、旧消费者、新政策生效后的存量数据，以及恶意自由文本。</p>\n<p>错误预算必须按最终后果分级。大量无害的格式重试和一次越权退款不能放在同一个平均失败率里被互相抵消：前者只是成本问题，后者是安全事件。安全的做法是对高危类别设独立的硬门槛——越权类错误的可接受数量是零或趋近于零，并且配即时回滚，而不是“总体 99.9% 达标”。</p>\n<p>诊断顺序也由契约结构决定：先问输出是否完整，再问形状是否正确，再问事实与关系是否成立，最后问当前主体是否能安全执行。四个问题的顺序对应从便宜到昂贵、从格式到后果的排查路径。沿着这个顺序，任何一次失败都能被归类到它真正发生的层，而不是被笼统地记成“模型输出错了”；也只有这样，“成功率 99.9%”剩下的 0.1% 才能被分解成具体、可修复、可按后果排优先级的问题清单。</p></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">10</span>把因果链连起来<span class=\"dd-badge intuition\">综合</span></h2>\n<div class=\"dd-agent-response\"><p>结构化输出解决的原始问题是：自然语言的容错与程序的确定性要求之间存在落差。从这个落差出发，每一条设计决策都由上一条的缺陷引出，一路连接到可验证的实践，整条因果链由六步串成。</p>\n<p>起点是最小版本化 schema。它把“模型要判断什么”缩减到只含真实业务决策的字段，身份、时间、派生值留给服务器；同时带上版本号，为将来 expand/contract 的演进留出坐标。schema 越小，生成与校验两端的状态空间越小。</p>\n<p>第二步是生成时约束并等待完整接受态。约束解码在采样时屏蔽非法 token，减少无效重试；“等待完整接受态”意味着不把流式输出的半截文本当结果用，只有生成器给出完整结束信号，对象才进入下一步。</p>\n<p>第三步是服务端重新解析并做 schema 校验。服务端不信任上游的自我声明，重新解析一遍并对照 schema 检查字段、类型、枚举与范围，把实现缺陷和版本错配挡在外面。格式在此被机器确认可读，但仍不意味着内容可信。</p>\n<p>第四步是验证事实和跨字段业务不变量。这里引入数据库事实与政策数据，核对 R = min(P − D, L) 这类依赖权威数据的规则；失败返回业务错误码，而不是静默修正。</p>\n<p>第五步是以当前主体鉴权后幂等执行。身份来自会话认证而非模型自报，工具调用以最小权限和当前主体重新鉴权，执行携带幂等键，保证重试不会重复产生副作用。</p>\n<p>第六步是按失败层记录并有限恢复或回滚。每一层有自己的失败形态与恢复动作：语法失败用约束重生或一次修复，缺必填字段转向 ask_clarification，业务冲突重取权威数据，权限不足拒绝或升级，同类连续失败降级 schema。恢复局部、有限、无副作用，且每层的通过率、错误与延迟都被记录，成为沿整条契约定位失败的评测数据。</p>\n<p>链条的因果方向是单向的：schema 约束生成，生成约束服务端解析，解析通往业务验证，业务验证通往鉴权执行，执行结果反过来成为下一轮评测与版本演进的输入。任何一环绕过前一环——跳过 schema 直接执行、跳过业务验证直接截断金额、跳过鉴权直接调用工具——链条就断在那一处。这个概念从“程序与自然语言容错方式不同”这一观察出发，最终落到的不是某个模型参数，而是一整套可以被机械验证、按层定位、有限恢复的工程结构。</p></div>\n</section>\n<div class=\"dd-src\"><b>资料来源与改编说明</b><ul><li><a href=\"https://json-schema.org/specification\" target=\"_blank\" rel=\"noopener\">JSON Schema Specification</a>：JSON 数据契约与验证语义</li><li><a href=\"https://arxiv.org/abs/2109.05093\" target=\"_blank\" rel=\"noopener\">PICARD</a>：生成过程中拒绝非法 token</li><li><a href=\"https://arxiv.org/abs/2501.10868\" target=\"_blank\" rel=\"noopener\">JSONSchemaBench</a>：约束解码覆盖与效率评测</li><li><a href=\"https://owasp.org/API-Security/\" target=\"_blank\" rel=\"noopener\">OWASP API Security Top 10</a>：对象授权、资源与接口安全边界</li></ul><div class=\"dd-src-date\">访问日期：2026-07-22</div></div>","publication":{"schemaVersion":1,"status":"published-provisional","reviewStatus":"manual-review","label":"未通过审计 · 暂行版本","blockerCount":1,"candidateHash":"sha256:56a5dc3a24de3a4009ee2ba50068337a7ed8c22e1921b62a3045499ecb9e9642","publishedAt":"2026-08-19T23:13:45.731Z","reason":"用户批量授权：节点 4.9 (structured-output) 显示缺陷返修（枚举写法统一+编号重排）后定向复核通过，以红色暂行版本重新发布（publish-provisional），非最终批准，非 L3 Pass。"},"websitePublication":{"publicAccess":true,"reviewStatus":"pending-review"}};