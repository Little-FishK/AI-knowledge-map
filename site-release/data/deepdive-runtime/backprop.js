(window.DEEPDIVE=window.DEEPDIVE||{})["backprop"]={"title":"反向传播：把一个结果的责任高效分回全部参数","subtitle":"从计算图、局部导数和上游梯度开始，完整手算一次反向遍历，并理解分叉累加、向量—雅可比积、激活缓存和梯度检查。","thesis":"反向传播不是另一种优化器，而是<b>标量输出上的反向模式自动微分</b>：前向保存必要中间值，反向从损失 1 开始，把上游梯度乘以局部导数；共享路径的贡献相加，从而用接近少数次前向计算的代价得到大量参数的梯度。","html":"<div class=\"dd-goals\"><div class=\"dd-goals-h\">读完这一页，你应该能自己回答：</div><ul><li>逐个扰动参数的有限差分为什么不适合训练大模型？</li><li>计算图怎样把复杂函数拆成可复用的局部求导？</li><li>上游梯度、局部导数和参数梯度之间是什么关系？</li><li>变量被多条路径使用时，梯度为什么必须相加？</li><li>怎样手算完整前向与反向，并用有限差分检查实现？</li></ul></div>\n<ol class=\"dd-chain\"><li>前向按计算图得到预测与标量损失，并保存必要中间值。</li><li>损失节点以 <code>∂L/∂L=1</code> 作为反向种子。</li><li>逆拓扑遍历让每个节点接收上游梯度。</li><li>节点用局部导数计算向量—雅可比积。</li><li>多条下游路径的贡献在共享变量处相加。</li><li>遍历结束后，每个可训练参数得到 <code>∂L/∂θ</code>。</li><li>有限差分和局部测试抽查梯度实现。</li><li>优化器随后才读取梯度并更新参数；目标是否正确仍由损失与评测负责。</li></ol>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">1</span>为什么不能把每个参数轻轻推一下试错<span class=\"dd-badge intuition\">动机</span></h2>\n<div class=\"dd-agent-response\"><p>这一节在回答一个核心问题：既然有限差分可以近似导数，为什么训练神经网络还需要反向传播？</p>\n<p>有限差分采用纯数值方法。对某个参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ_(i)\">θ<sub>i</sub></span>，保持其他参数不变，分别把它向正、负方向移动一个很小的步长 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span>，然后比较两次损失：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ θ_(i) ≈ (L(θ_(i)+ε)-L(θ_(i)-ε))/2ε\"><span>∂ L/∂ θ<sub>i</sub> ≈ (L(θ<sub>i</sub>+ε)-L(θ<sub>i</sub>-ε))/2ε</span></div>\n<p>这个公式称为中心差分。它的输入是参数当前值和人为选择的扰动步长 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span>，输出是损失沿第 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"i\">i</span> 个参数方向的近似斜率。</p>\n<p>其计算过程可以分为三步：</p>\n<ol><li>将 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ_(i)\">θ<sub>i</sub></span> 改为 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ_(i)+ε\">θ<sub>i</sub>+ε</span>，执行一次完整前向计算，得到损失 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L(θ_(i)+ε)\">L(θ<sub>i</sub>+ε)</span>。</li><li>将 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ_(i)\">θ<sub>i</sub></span> 改为 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ_(i)-ε\">θ<sub>i</sub>-ε</span>，再次执行完整前向计算，得到损失 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L(θ_(i)-ε)\">L(θ<sub>i</sub>-ε)</span>。</li><li>用两个损失之差除以参数两次取值之间的距离 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"2ε\">2ε</span>，估计单位参数变化引起的损失变化。</li></ol>\n<p>中心差分使用扰动点两侧的信息，通常比只从一侧计算的前向差分更准确。但是，它仍然是一种近似方法，而不是从计算过程直接得到的解析梯度。</p>\n<p>有限差分首先面临计算成本问题。假设模型有 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"P\">P</span> 个参数，检查每个参数的中心差分需要大约 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"2P\">2P</span> 次完整前向计算。百万参数意味着约两百万次前向，十亿参数则需要约二十亿次前向。每次只改变一个参数，却要重新执行整个模型，绝大多数共享计算被反复重做，因此无法承担日常训练。</p>\n<p>其次，步长 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 存在数值上的两难选择。</p>\n<p>当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 太大时，损失函数在这一段范围内可能已经明显弯曲。差商测得的是一段区间的平均斜率，而不是当前点的瞬时斜率，因此产生截断误差。</p>\n<p>当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 太小时，两个损失值会极其接近。在有限精度浮点数中，对两个相近数做减法容易丢失有效数字；得到的微小误差再除以很小的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"2ε\">2ε</span>，还可能被进一步放大。这就是舍入误差或消减误差。</p>\n<p>因此，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 并非越小越好。实际检查中常尝试多个数量级的步长，观察数值梯度是否在某个区间内保持稳定。通常随着 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 减小，截断误差会先下降；当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 过小时，浮点误差又会上升。</p>\n<p>这些限制决定了有限差分的合适用途：它适合做梯度检查，不适合直接训练模型。可以随机抽取少量参数，将有限差分结果与自动求导得到的梯度比较，以发现反向公式、路径累加或广播处理中的错误。</p>\n<p>反向传播的效率来自不同的思路。神经网络的所有参数最终都影响同一个标量损失，而且它们到损失之间共享大量中间计算。有限差分把每个参数视为一次独立实验，因而不断重复这些计算；反向传播则保存前向计算的结构和必要中间值，再从损失出发，按照链式法则逆向遍历一次计算图。</p>\n<p>在一次反向遍历中，共享节点对损失的敏感度只需计算一次，随后便可复用于所有上游参数。因此，反向传播能够以与少数几次前向计算相近的成本，得到全部参数的梯度。</p>\n<p>两种方法的差别可以概括为：</p>\n<ul><li>有限差分逐个询问：“单独轻推这个参数，损失会怎样变化？”</li><li>反向传播统一计算：“损失的敏感度怎样沿共享计算路径传回所有参数？”</li></ul>\n<p>所以，有限差分的问题不是不能求梯度，而是不能高效、稳定地为海量参数反复求梯度。反向传播真正利用的是计算图中的共享结构，把原本需要逐参数重复进行的试错，转化成一次系统性的敏感度传播。</p></div>\n<div class=\"dd-formula\">∂L/∂θᵢ ≈ [L(θᵢ+ε)−L(θᵢ−ε)]/(2ε)</div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">2</span>计算图怎样把复杂函数拆成局部问题<span class=\"dd-badge intuition\">直觉</span></h2>\n<div class=\"dd-agent-response\"><p>这一节解释反向传播的组织基础：计算图如何把一个复杂函数拆成许多简单、可复用的局部求导问题。</p>\n<p>以</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ŷ=wx, L=1/2(ŷ-y)^(2)\"><span>ŷ=wx, L=1/2(ŷ-y)<sup>2</sup></span></div>\n<p>为例。虽然可以把损失直接写成：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2(wx-y)^(2)\"><span>L=1/2(wx-y)<sup>2</sup></span></div>\n<p>并对整个复合公式求导，但真实神经网络包含大量矩阵乘法、偏置、归一化和非线性函数。若把整个模型展开为一个巨型公式，推导、实现和维护都会变得困难。</p>\n<p>计算图采用不同的表示方法：把复合函数拆成连续的基本运算。例如：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=wx\"><span>u=wx</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"e=u-y\"><span>e=u-y</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2e^(2)\"><span>L=1/2e<sup>2</sup></span></div>\n<p>这里的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 就是预测值 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ŷ\">ŷ</span>，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span> 是预测误差。乘法、减法和半平方各自构成一个局部运算节点，节点之间的边表示数据依赖。由于一个运算必须等待其输入产生，普通前向计算形成一张有向无环图。</p>\n<p>前向传播沿依赖方向计算数值：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w,x→ u→ e→ L\"><span>w,x→ u→ e→ L</span></div>\n<p>反向传播则沿相反方向传递敏感度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L→ e→ u→ w,x\"><span>L→ e→ u→ w,x</span></div>\n<p>这里传递的不是前向数值，而是最终损失对当前节点输出的梯度，也就是“当前变量稍微变化时，损失会怎样变化”。</p>\n<p>反向传播从：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ L=1\"><span>∂ L/∂ L=1</span></div>\n<p>开始。随后，每个节点只需把收到的上游梯度与自己的局部导数组合。</p>\n<p>对于半平方节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2e^(2)\"><span>L=1/2e<sup>2</sup></span></div>\n<p>局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ e=e\"><span>∂ L/∂ e=e</span></div>\n<p>对于减法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"e=u-y\"><span>e=u-y</span></div>\n<p>局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ e/∂ u=1, ∂ e/∂ y=-1\"><span>∂ e/∂ u=1, ∂ e/∂ y=-1</span></div>\n<p>对于乘法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=wx\"><span>u=wx</span></div>\n<p>局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ u/∂ w=x, ∂ u/∂ x=w\"><span>∂ u/∂ w=x, ∂ u/∂ x=w</span></div>\n<p>按照链式法则拼接后，可以得到：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w = ∂ L/∂ e ∂ e/∂ u ∂ u/∂ w = e·1· x = (wx-y)x\"><span>∂ L/∂ w = ∂ L/∂ e ∂ e/∂ u ∂ u/∂ w = e·1· x = (wx-y)x</span></div>\n<p>以及：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = e·1· w = (wx-y)w\"><span>∂ L/∂ x = e·1· w = (wx-y)w</span></div>\n<p>整个过程中，没有任何一个节点需要知道完整模型。乘法节点只负责乘法的反向规则，减法节点只负责减法的反向规则，平方节点只负责平方的反向规则。复杂网络的梯度就是这些局部规则按照图结构组合后的结果。</p>\n<p>因此，计算图不只是公式的可视化，它还是一份可执行的求导计划。它至少需要记录两类信息。</p>\n<p>第一类是运算之间的连接关系。例如 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 由 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span> 相乘得到，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span> 又依赖于 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"y\">y</span>。这些关系决定反向传播应按什么顺序经过哪些节点。</p>\n<p>第二类是局部反向公式需要的前向值。例如：</p>\n<ul><li>计算乘法节点中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的梯度需要前向值 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span>；</li><li>计算 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span> 的梯度需要前向值 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span>；</li><li>计算半平方损失的反向需要误差 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e=ŷ-y\">e=ŷ-y</span>；</li><li>激活函数可能需要知道前向输入位于哪个区域。</li></ul>\n<p>这说明“图结构还存在”并不等于“反向一定能够执行”。如果框架仍然知道某个节点做过乘法，却已经丢失或覆盖了乘法反向所需的输入值，便无法正确计算局部梯度。</p>\n<p>计算图与中间状态的关系也解释了训练显存高于推理显存的原因。推理完成一层后，若其激活不再参与后续前向，通常可以释放；训练却要保留反向所需的中间值，直到对应节点完成反向计算。</p>\n<p>它也解释了原地修改为何危险。若某个张量在前向后被直接覆盖，而反向公式需要原来的值，反向读取到的就不再是产生当前损失时的状态。即使前向结果看起来正常，梯度也可能报错或失真。</p>\n<p>当计算图存在分支时，同一个变量可能沿多条路径影响损失。各条路径会分别传回梯度贡献，并在共享节点处求和。这是多元链式法则在图结构中的表现，也是计算图能够处理残差连接、权重共享和循环结构的基础。</p>\n<p>所以，计算图解决复杂求导问题的方法不是推导更大的整体公式，而是建立清晰的局部协作机制：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"记录前向运算 → 保存必要状态 → 逆序调用局部反向规则 → 按链式法则组合梯度\"><span>记录前向运算 → 保存必要状态 → 逆序调用局部反向规则 → 按链式法则组合梯度</span></div>\n<p>它把全局求导转化为许多小型局部问题，同时也划定了一条硬边界：反向传播的正确性依赖于前向计算图和必要中间值都保持完整。</p></div>\n<figure class=\"dd-fig\"><svg viewBox=\"0 0 720 260\" role=\"img\" aria-label=\"ŷ=wx 与半平方损失的计算图和反向梯度\"><defs><marker id=\"bpa\" markerWidth=\"8\" markerHeight=\"8\" refX=\"7\" refY=\"3\" orient=\"auto\"><path d=\"M0,0 L0,6 L8,3 z\" fill=\"context-stroke\"/></marker></defs><g fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><rect x=\"35\" y=\"45\" width=\"76\" height=\"42\" rx=\"10\"/><rect x=\"35\" y=\"150\" width=\"76\" height=\"42\" rx=\"10\"/><rect x=\"180\" y=\"92\" width=\"92\" height=\"50\" rx=\"12\"/><rect x=\"342\" y=\"92\" width=\"92\" height=\"50\" rx=\"12\"/><rect x=\"505\" y=\"92\" width=\"120\" height=\"50\" rx=\"12\"/><path d=\"M111 66 L180 105\" marker-end=\"url(#bpa)\"/><path d=\"M111 171 L180 130\" marker-end=\"url(#bpa)\"/><path d=\"M272 117 L342 117\" marker-end=\"url(#bpa)\"/><path d=\"M434 117 L505 117\" marker-end=\"url(#bpa)\"/></g><g class=\"svg-t\"><text x=\"65\" y=\"72\">w=2</text><text x=\"65\" y=\"177\">x=2</text><text x=\"205\" y=\"122\">乘法</text><text x=\"365\" y=\"122\">ŷ=4</text><text x=\"526\" y=\"122\">L=½(ŷ−6)²=2</text></g><g fill=\"#ef4444\" class=\"svg-tn\"><text x=\"518\" y=\"78\">起点：∂L/∂L=1</text><text x=\"345\" y=\"174\">∂L/∂ŷ=−2</text><text x=\"175\" y=\"174\">∂L/∂w=−2×x=−4</text></g></svg><figcaption>前向从左到右算值；反向从右到左传“损失对这里有多敏感”。图里每个方框只表示一次局部运算，没有任何一处需要对整条公式做一次性求导。</figcaption></figure>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">3</span>上游梯度乘局部导数是什么意思<span class=\"dd-badge math\">链式法则</span></h2>\n<div class=\"dd-agent-response\"><p>这一节把反向传播缩小到一个运算节点，说明梯度在节点之间究竟如何传递。</p>\n<p>设某个节点执行：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z=f(u)\"><span>z=f(u)</span></div>\n<p>最终损失 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L\">L</span> 又依赖于 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z\">z</span>。反向传播经过该节点时使用链式法则：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ u = ∂ L/∂ z ∂ z/∂ u\"><span>∂ L/∂ u = ∂ L/∂ z ∂ z/∂ u</span></div>\n<p>这三个量各有明确含义：</p>\n<ul><li><span class=\"dd-inline-math\" role=\"math\" aria-label=\"∂ L/∂ z\">∂ L/∂ z</span> 是节点收到的上游梯度，表示 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z\">z</span> 发生微小变化时，最终损失会怎样变化；</li><li><span class=\"dd-inline-math\" role=\"math\" aria-label=\"∂ z/∂ u\">∂ z/∂ u</span> 是节点的局部导数，表示 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 发生微小变化时，节点输出 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z\">z</span> 会怎样变化；</li><li><span class=\"dd-inline-math\" role=\"math\" aria-label=\"∂ L/∂ u\">∂ L/∂ u</span> 是节点传给输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的梯度，表示 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的微小变化最终会怎样影响损失。</li></ul>\n<p>两种敏感度之所以相乘，可以从微小变化理解：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ z≈∂ z/∂ uΔ u\"><span>Δ z≈∂ z/∂ uΔ u</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ L≈∂ L/∂ zΔ z\"><span>Δ L≈∂ L/∂ zΔ z</span></div>\n<p>将第一式代入第二式：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ L ≈ ∂ L/∂ z ∂ z/∂ u Δ u\"><span>Δ L ≈ ∂ L/∂ z ∂ z/∂ u Δ u</span></div>\n<p>因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ u = ∂ L/∂ z ∂ z/∂ u\"><span>∂ L/∂ u = ∂ L/∂ z ∂ z/∂ u</span></div>\n<p>第一项描述从 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z\">z</span> 到损失的影响，第二项描述从 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 到 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z\">z</span> 的影响。两段首尾相接后，就得到从 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 到损失的完整局部影响。</p>\n<p>常用的横杠记号可以把公式写得更紧凑：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z̄=∂ L/∂ z, ū=∂ L/∂ u\"><span>z̄=∂ L/∂ z, ū=∂ L/∂ u</span></div>\n<p>于是单个节点的反向规则写成：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ū=z̄∂ z/∂ u\"><span>ū=z̄∂ z/∂ u</span></div>\n<p>也就是：先接收上游梯度，再用本节点的局部导数对它进行变换，最后把结果传给输入。</p>\n<p>对于加法：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z=a+b\"><span>z=a+b</span></div>\n<p>局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ z/∂ a=1, ∂ z/∂ b=1\"><span>∂ z/∂ a=1, ∂ z/∂ b=1</span></div>\n<p>所以：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ā=z̄, b̄=z̄\"><span>ā=z̄, b̄=z̄</span></div>\n<p>两个输入都收到完整的上游梯度。这不是把梯度平均分配给两个输入，而是因为任意一个输入增加一点，输出都会增加同样的量。</p>\n<p>对于乘法：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z=ab\"><span>z=ab</span></div>\n<p>局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ z/∂ a=b, ∂ z/∂ b=a\"><span>∂ z/∂ a=b, ∂ z/∂ b=a</span></div>\n<p>因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ā=z̄ b, b̄=z̄ a\"><span>ā=z̄ b, b̄=z̄ a</span></div>\n<p>计算 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"a\">a</span> 的梯度需要前向时的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span>，计算 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span> 的梯度需要前向时的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"a\">a</span>。这说明为什么反向传播必须保存某些前向中间值。</p>\n<p>对于平方：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z=u^(2)\"><span>z=u<sup>2</sup></span></div>\n<p>局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ z/∂ u=2u\"><span>∂ z/∂ u=2u</span></div>\n<p>所以：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ū=z̄·2u\"><span>ū=z̄·2u</span></div>\n<p>上游梯度在通过平方节点时，会按照当前输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的数值和符号被缩放。</p>\n<p>对于 ReLU：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z=max(0,u)\"><span>z=max(0,u)</span></div>\n<p>当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u&gt;0\">u&gt;0</span> 时，局部导数为 1；当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u&lt;0\">u&lt;0</span> 时，局部导数为 0。因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ū= { z̄, u&gt;0; 0, u&lt;0 }\"><span>ū= { z̄, u&gt;0; 0, u&lt;0 }</span></div>\n<p>ReLU 可以被理解为一个梯度门：正区间让上游梯度原样通过，负区间将其阻断。</p>\n<p>在 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u=0\">u=0</span> 处，ReLU 的左右导数不同，经典导数不存在。自动求导框架必须约定一个值，通常选择某个次梯度。这个值并非由普通链式法则唯一决定，而是算子的实现约定。</p>\n<p>对于矩阵乘法：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Z=WX\"><span>Z=WX</span></div>\n<p>设上游梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Z̄=∂ L/∂ Z\"><span>Z̄=∂ L/∂ Z</span></div>\n<p>则输入梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"W̄=Z̄ X^(T)\"><span>W̄=Z̄ X<sup>T</sup></span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"X̄=W^(T)Z̄\"><span>X̄=W<sup>T</sup>Z̄</span></div>\n<p>假设：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"W∈ℝ^(m× n), X∈ℝ^(n× p), Z∈ℝ^(m× p)\"><span>W∈ℝ<sup>m× n</sup>, X∈ℝ<sup>n× p</sup>, Z∈ℝ<sup>m× p</sup></span></div>\n<p>那么：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"W̄∈ℝ^(m× n), X̄∈ℝ^(n× p)\"><span>W̄∈ℝ<sup>m× n</sup>, X̄∈ℝ<sup>n× p</sup></span></div>\n<p>所得梯度分别与 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"W\">W</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"X\">X</span> 的形状一致。检查梯度形状是判断矩阵反向公式中转置位置是否正确的一种基本方法。</p>\n<p>在标量例子里，“上游梯度乘局部导数”看起来只是普通乘法；在向量和张量情形下，局部导数可能是雅可比矩阵。严格来说，节点执行的是上游梯度与局部雅可比的乘积。框架通常不会显式创建完整雅可比，而是直接实现这一乘积。</p>\n<p>如果节点有多个输入，它需要分别为每个输入计算梯度。如果同一个输入通过多条路径影响损失，各条路径返回的贡献还必须相加。例如 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 同时影响 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z_(1)\">z<sub>1</sub></span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"z_(2)\">z<sub>2</sub></span>，则：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ u = ∂ L/∂ z_(1) ∂ z_(1)/∂ u + ∂ L/∂ z_(2) ∂ z_(2)/∂ u\"><span>∂ L/∂ u = ∂ L/∂ z<sub>1</sub> ∂ z<sub>1</sub>/∂ u + ∂ L/∂ z<sub>2</sub> ∂ z<sub>2</sub>/∂ u</span></div>\n<p>因此，单条路径遵循“上游梯度乘局部导数”，多条路径在共享节点处还要执行“各路径贡献求和”。</p>\n<p>反向传播没有创造新的微积分规则。它只是把链式法则组织成一种高效执行过程：每个节点只实现自己的局部反向规则，框架再按照计算图的逆序将这些规则组合起来。</p>\n<p>这种机制也有明确前提：局部导数必须存在或有清晰约定，并且反向规则必须正确实现。如果自定义算子的前向计算正确、反向公式错误，模型仍能产生正常的前向数值，但错误梯度会从该节点继续传向所有上游变量，使整条训练链路受到污染。</p></div>\n<table class=\"dd-table\"><thead><tr><th>局部操作</th><th>前向</th><th>反向给输入</th></tr></thead><tbody><tr><td>加法</td><td><code>z=a+b</code></td><td>两边都接收上游梯度</td></tr><tr><td>乘法</td><td><code>z=ab</code></td><td><code>ā=ż·b, b̄=ż·a</code></td></tr><tr><td>平方</td><td><code>z=u²</code></td><td><code>ū=ż·2u</code></td></tr><tr><td>ReLU</td><td><code>z=max(0,u)</code></td><td><code>u&gt;0</code> 时传递，否则为 0</td></tr><tr><td>矩阵乘</td><td><code>Z=WX</code></td><td>按转置乘法得到 <code>W̄</code> 与 <code>X̄</code></td></tr></tbody></table>\n<div class=\"dd-formula\">∂L/∂u = ∂L/∂z · ∂z/∂u</div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">4</span>完整手算：−4 怎样一步步出现<span class=\"dd-badge math\">数值例子</span></h2>\n<div class=\"dd-agent-response\"><p>这一节通过完整手算说明：梯度 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"-4\">-4</span> 并不是突然出现的，而是损失端的种子梯度经过多个局部节点逐步缩放后得到的结果。</p>\n<p>给定：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"x=2, y=6, w=2\"><span>x=2, y=6, w=2</span></div>\n<p>模型和损失为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=wx, e=u-y, L=1/2e^(2)\"><span>u=wx, e=u-y, L=1/2e<sup>2</sup></span></div>\n<p>它们共同表示复合函数：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2(wx-y)^(2)\"><span>L=1/2(wx-y)<sup>2</sup></span></div>\n<p>计算时不直接对整个复合公式求导，而是先完成前向传播，保存中间值，再沿相反顺序执行反向传播。</p>\n<p>首先计算前向。</p>\n<p>乘法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=wx=2×2=4\"><span>u=wx=2×2=4</span></div>\n<p>这里 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 是模型预测值。</p>\n<p>减法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"e=u-y=4-6=-2\"><span>e=u-y=4-6=-2</span></div>\n<p>这里 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e=-2\">e=-2</span> 表示预测值比目标值低 2。</p>\n<p>半平方损失节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2e^(2) =1/2(-2)^(2) =1/2×4 =2\"><span>L=1/2e<sup>2</sup> =1/2(-2)<sup>2</sup> =1/2×4 =2</span></div>\n<p>前向传播最终得到：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=4, e=-2, L=2\"><span>u=4, e=-2, L=2</span></div>\n<p>其中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span> 不只是临时计算结果，也是反向传播所需的原料。平方节点的局部导数依赖 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span>，乘法节点的局部导数依赖 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span>。</p>\n<p>反向传播从最终损失开始。其种子梯度是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ L=1\"><span>∂ L/∂ L=1</span></div>\n<p>这是因为任何变量对自身的导数都等于 1。这个 1 不是学习率，也不是人为猜测的梯度大小，而是启动反向传播的一单位敏感度。</p>\n<p>接下来经过半平方节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2e^(2)\"><span>L=1/2e<sup>2</sup></span></div>\n<p>该节点对输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span> 的局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ e=e\"><span>∂ L/∂ e=e</span></div>\n<p>把前向保存的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e=-2\">e=-2</span> 代入，并乘以上游梯度 1：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ e = 1× e = 1×(-2) = -2\"><span>∂ L/∂ e = 1× e = 1×(-2) = -2</span></div>\n<p>这一步中的负号来自前向误差 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e=-2\">e=-2</span>。平方运算虽然让最终损失变成正数，但其导数仍保留误差的方向信息。</p>\n<p>然后经过减法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"e=u-y\"><span>e=u-y</span></div>\n<p>该节点关于 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的局部导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ e/∂ u=1\"><span>∂ e/∂ u=1</span></div>\n<p>因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ u = ∂ L/∂ e ∂ e/∂ u = (-2)×1 = -2\"><span>∂ L/∂ u = ∂ L/∂ e ∂ e/∂ u = (-2)×1 = -2</span></div>\n<p>梯度经过通往 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的减法路径时，大小和符号都没有改变。</p>\n<p>最后经过乘法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=wx\"><span>u=wx</span></div>\n<p>通往参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的局部导数是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ u/∂ w=x\"><span>∂ u/∂ w=x</span></div>\n<p>因为前向时 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x=2\">x=2</span>，所以：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w = ∂ L/∂ u ∂ u/∂ w = (-2)×2 = -4\"><span>∂ L/∂ w = ∂ L/∂ u ∂ u/∂ w = (-2)×2 = -4</span></div>\n<p>因此，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"-4\">-4</span> 的完整来源可以写成：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w = 1（损失种子） × (-2)（平方节点的局部导数e） × 1（减法节点对u的局部导数） × 2（乘法节点对w的局部导数x） = -4\"><span>∂ L/∂ w = 1（损失种子） × (-2)（平方节点的局部导数e） × 1（减法节点对u的局部导数） × 2（乘法节点对w的局部导数x） = -4</span></div>\n<p>同一个乘法节点还可以把梯度传给输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span>。由于：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ u/∂ x=w\"><span>∂ u/∂ x=w</span></div>\n<p>并且 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w=2\">w=2</span>，所以：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = ∂ L/∂ u ∂ u/∂ x = (-2)×2 = -4\"><span>∂ L/∂ x = ∂ L/∂ u ∂ u/∂ x = (-2)×2 = -4</span></div>\n<p>若把目标值 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"y\">y</span> 也视为需要求导的变量，那么减法节点在通往 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"y\">y</span> 的路径上具有局部导数：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ e/∂ y=-1\"><span>∂ e/∂ y=-1</span></div>\n<p>因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ y = ∂ L/∂ e ∂ e/∂ y = (-2)×(-1) = 2\"><span>∂ L/∂ y = ∂ L/∂ e ∂ e/∂ y = (-2)×(-1) = 2</span></div>\n<p>不过在普通监督学习中，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"y\">y</span> 通常是数据，优化器只更新可训练参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span>。</p>\n<p>梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w=-4\"><span>∂ L/∂ w=-4</span></div>\n<p>表示在当前点 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w=2\">w=2</span> 附近，如果让 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 增加一个很小的量 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"Δ w\">Δ w</span>，损失的变化近似为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ L≈-4Δ w\"><span>Δ L≈-4Δ w</span></div>\n<p>因此，稍微增大 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 会使损失下降。这与当前预测偏低相符：因为 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x=2&gt;0\">x=2&gt;0</span>，增大 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 会增大预测值 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u=wx\">u=wx</span>，使其从 4 向目标值 6 靠近。</p>\n<p>但 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"-4\">-4</span> 并不表示应该直接让 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 增加 4。若采用最简单的梯度下降，更新公式是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w_(new) = w-η∂ L/∂ w\"><span>w<sub>new</sub> = w-η∂ L/∂ w</span></div>\n<p>代入当前梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w_(new) = 2-η(-4) = 2+4η\"><span>w<sub>new</sub> = 2-η(-4) = 2+4η</span></div>\n<p>真正的更新幅度由学习率 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"η\">η</span> 和优化器规则决定。反向传播只提供当前点附近的方向和变化率，不负责决定实际步长。</p>\n<p>最后，这些前向值和梯度只对：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"x=2, y=6, w=2\"><span>x=2, y=6, w=2</span></div>\n<p>这一组取值成立。参数更新后，预测值 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span>、误差 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span>、损失 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L\">L</span> 和各局部梯度都会变化。因此，每次参数更新后都需要重新执行前向传播，再基于新的中间状态进行反向传播。</p></div>\n<table class=\"dd-table\"><thead><tr><th>方向</th><th>节点</th><th>计算</th><th>值</th></tr></thead><tbody><tr><td>前向</td><td><code>u=wx</code></td><td><code>2×2</code></td><td>4</td></tr><tr><td>前向</td><td><code>e=u−y</code></td><td><code>4−6</code></td><td>−2</td></tr><tr><td>前向</td><td><code>L=½e²</code></td><td><code>½×4</code></td><td>2</td></tr><tr><td>反向</td><td>损失种子</td><td><code>∂L/∂L</code></td><td>1</td></tr><tr><td>反向</td><td>经过平方</td><td><code>∂L/∂e=1×e</code></td><td>−2</td></tr><tr><td>反向</td><td>经过减法</td><td><code>∂L/∂u=−2×1</code></td><td>−2</td></tr><tr><td>反向</td><td>经过乘法到 w</td><td><code>∂L/∂w=−2×x</code></td><td>−4</td></tr><tr><td>反向</td><td>经过乘法到 x</td><td><code>∂L/∂x=−2×w</code></td><td>−4</td></tr></tbody></table>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">5</span>分叉路径为什么要累加梯度<span class=\"dd-badge math\">多路径</span></h2>\n<div class=\"dd-agent-response\"><p>这一节说明多元链式法则中的一个关键规则：当同一个变量通过多条计算路径影响最终结果时，它的总梯度等于所有路径贡献之和。</p>\n<p>以：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=w^(2)+w\"><span>u=w<sup>2</sup>+w</span></div>\n<p>为例。参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 在计算图中有两条通往 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的路径：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w→ w^(2)→ u\"><span>w→ w<sup>2</sup>→ u</span></div>\n<p>以及：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w→ u\"><span>w→ u</span></div>\n<p>第一条路径经过平方运算，第二条路径直接进入加法。只要 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 发生微小变化，这两条路径的输出就会同时变化，因此 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的总变化必须包括两部分。</p>\n<p>平方路径的导数贡献为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"d(w^(2))/dw=2w\"><span>d(w<sup>2</sup>)/dw=2w</span></div>\n<p>直接路径的导数贡献为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"dw/dw=1\"><span>dw/dw=1</span></div>\n<p>所以：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"du/dw=2w+1\"><span>du/dw=2w+1</span></div>\n<p>这里的加号不是代数展开后的偶然结果，而是计算图中分叉路径反向传播的一般规则：一个变量若通过多条下游路径影响输出，各条路径返回的梯度贡献必须在该变量处求和。</p>\n<p>如果最终还有损失 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L\">L</span> 依赖于 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span>，设从下游传到 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的上游梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ū=∂ L/∂ u\"><span>ū=∂ L/∂ u</span></div>\n<p>平方路径传回 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的贡献为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w̄_(平方) = ū·2w\"><span>w̄<sub>平方</sub> = ū·2w</span></div>\n<p>直接路径传回 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的贡献为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w̄_(直接) = ū·1\"><span>w̄<sub>直接</sub> = ū·1</span></div>\n<p>因此，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的完整梯度是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w̄ = w̄_(平方) + w̄_(直接)\"><span>w̄ = w̄<sub>平方</sub> + w̄<sub>直接</sub></span></div>\n<p>即：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w = ∂ L/∂ u(2w+1)\"><span>∂ L/∂ w = ∂ L/∂ u(2w+1)</span></div>\n<p>这就是多元链式法则在计算图中的路径求和形式。</p>\n<p>从微小变化的角度也可以得到同样结论。假设 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 变化了一个很小的量 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"Δ w\">Δ w</span>。平方分支的变化近似为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ(w^(2))≈2wΔ w\"><span>Δ(w<sup>2</sup>)≈2wΔ w</span></div>\n<p>直接分支的变化为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ w=1·Δ w\"><span>Δ w=1·Δ w</span></div>\n<p>由于加法节点把两个分支的结果相加，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 的总变化为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Δ u ≈ 2wΔ w+Δ w = (2w+1)Δ w\"><span>Δ u ≈ 2wΔ w+Δ w = (2w+1)Δ w</span></div>\n<p>因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"du/dw=2w+1\"><span>du/dw=2w+1</span></div>\n<p>如果只沿平方路径反向传播，就会错误地得到：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"du/dw=2w\"><span>du/dw=2w</span></div>\n<p>这漏掉了直接路径的贡献 1。错误结果仍可能形状正确、数值有限，看起来像一个合理梯度，因此比直接报错更隐蔽。</p>\n<p>漏掉路径也不一定总是让梯度变小。不同路径的贡献可能同号，也可能异号。例如一条路径贡献 5，另一条路径贡献 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"-3\">-3</span>，正确总梯度是 2。若漏掉其中一条，结果可能变成 5 或 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"-3\">-3</span>，不只改变大小，还可能改变符号。因此，漏掉路径可能使训练变慢，也可能使参数朝错误方向更新。</p>\n<p>在自动求导的实现中，某个变量的梯度缓冲区不能在每次收到新贡献时直接覆盖：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w̄←新贡献\"><span>w̄←新贡献</span></div>\n<p>而应执行累加：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w̄←w̄+新贡献\"><span>w̄←w̄+新贡献</span></div>\n<p>如果使用赋值覆盖，后到达的路径会抹掉先到达的路径，最终只保留一部分导数。正确的反向遍历需要等待或汇总所有下游路径的贡献，得到完整总梯度。</p>\n<p>这种路径累加在神经网络中十分常见。</p>\n<p>残差连接通常写成：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y=F(x)+x\"><span>y=F(x)+x</span></div>\n<p>输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span> 一条路径经过变换 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"F\">F</span>，另一条路径通过恒等连接直接到达加法节点。因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = ∂ L/∂ y ( ∂ F(x)/∂ x+I )\"><span>∂ L/∂ x = ∂ L/∂ y ( ∂ F(x)/∂ x+I )</span></div>\n<p>其中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"I\">I</span> 是恒等路径的局部导数。反向传播必须同时保留变换分支和捷径分支的贡献。</p>\n<p>权重共享也会产生路径累加。若同一个参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 在前向中被使用多次，例如：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u=f(w,x_(1))+g(w,x_(2))\"><span>u=f(w,x<sub>1</sub>)+g(w,x<sub>2</sub>)</span></div>\n<p>那么：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w = ∂ L/∂ u ( ∂ f/∂ w + ∂ g/∂ w )\"><span>∂ L/∂ w = ∂ L/∂ u ( ∂ f/∂ w + ∂ g/∂ w )</span></div>\n<p>虽然它们引用的是同一个参数对象，但每次使用都形成了一条独立的下游路径。所有使用位置产生的梯度贡献都必须累加到同一个参数上。</p>\n<p>循环神经网络中，同一组权重在多个时间步反复使用。将循环沿时间展开后，每个时间步都形成一条使用共享权重的路径，因此该权重的最终梯度是所有时间步贡献的总和。这是通过时间反向传播中的基本机制。</p>\n<p>还需要区分图内路径求和与跨 mini-batch 梯度累积。</p>\n<p>图内路径求和发生在同一次前向和反向过程中。它来自函数本身的依赖结构，是计算正确总导数不可缺少的一部分。只要计算图中存在分叉、共享或重复使用，自动求导系统就必须执行这种累加。</p>\n<p>跨 mini-batch 梯度累积则是一种训练策略。它在多次独立的前向—反向之后，暂时不清空参数的梯度缓冲区，从而将多个批次的梯度相加，用来模拟更大的有效批量。例如连续累积 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"K\">K</span> 个微批次后再更新参数。</p>\n<p>这两类累加都使用加法，但原因不同：</p>\n<ul><li>图内分叉求和是链式法则的要求，不能省略；</li><li>跨批次梯度累积是训练配置，可以选择是否使用；</li><li>图内求和由计算图自动完成；</li><li>跨批次累积需要训练代码明确决定何时保留梯度、何时更新以及何时清零。</li></ul>\n<p>因此，理解分叉路径的关键是：一个变量的梯度不是任选一条路径得到的局部结果，而是该变量通过所有下游路径对最终损失造成的总影响。只有把每条路径的贡献完整相加，所得梯度才是真正的总导数。</p></div>\n<div class=\"dd-formula\">du/dw = 2w + 1</div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">6</span>张量很多时为什么不显式构造巨大雅可比<span class=\"dd-badge math\">向量化</span></h2>\n<div class=\"dd-agent-response\"><p>这一节解释反向模式自动微分为什么能够处理高维张量：它通常不构造完整雅可比矩阵，而是直接计算向量—雅可比积。</p>\n<p>设某一层为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y=f(x)\"><span>y=f(x)</span></div>\n<p>其中：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"x∈ℝ^(n), y∈ℝ^(m)\"><span>x∈ℝ<sup>n</sup>, y∈ℝ<sup>m</sup></span></div>\n<p>这个函数的完整雅可比矩阵是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"J=∂ y/∂ x ∈ℝ^(m× n)\"><span>J=∂ y/∂ x ∈ℝ<sup>m× n</sup></span></div>\n<p>矩阵中的元素为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"J_(ij)=∂ y_(i)/∂ x_(j)\"><span>J<sub>ij</sub>=∂ y<sub>i</sub>/∂ x<sub>j</sub></span></div>\n<p>它记录每个输入分量对每个输出分量的局部影响。如果输入和输出都很大，显式存储 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"J\">J</span> 会非常昂贵。例如 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"m=n=10 000\">m=n=10 000</span> 时，雅可比包含一亿个元素；更大的张量会产生更加难以承受的存储和计算成本。</p>\n<p>但神经网络训练真正需要的通常不是 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"J\">J</span> 本身。最终目标是一个标量损失：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=L(y)\"><span>L=L(y)</span></div>\n<p>当反向传播到当前层时，下游已经给出了损失对层输出的梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"v^(T) = ∂ L/∂ y\"><span>v<sup>T</sup> = ∂ L/∂ y</span></div>\n<p>根据链式法则，损失对层输入的梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = ∂ L/∂ y ∂ y/∂ x = v^(T)J\"><span>∂ L/∂ x = ∂ L/∂ y ∂ y/∂ x = v<sup>T</sup>J</span></div>\n<p>这个运算称为向量—雅可比积，即 VJP。</p>\n<p>如果统一使用列向量表示梯度，同一个关系也可以写成：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(x)L=J^(T)∇_(y)L\"><span>∇<sub>x</sub>L=J<sup>T</sup>∇<sub>y</sub>L</span></div>\n<p>两种形式只采用了不同的行列向量约定，含义完全相同。</p>\n<p>关键在于，最终结果只是一个与输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span> 同形的向量，包含 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"n\">n</span> 个元素。既然只需要 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"v^(T)J\">v<sup>T</sup>J</span>，就没有必要先创建含有 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"mn\">mn</span> 个元素的完整雅可比，再执行乘法。框架可以利用每种运算的结构，直接从上游梯度计算输入梯度。</p>\n<p>以线性变换为例：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y=Wx\"><span>y=Wx</span></div>\n<p>损失对输出的梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(y)L\"><span>∇<sub>y</sub>L</span></div>\n<p>则输入梯度可以直接计算为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(x)L=W^(T)∇_(y)L\"><span>∇<sub>x</sub>L=W<sup>T</sup>∇<sub>y</sub>L</span></div>\n<p>如果还需要参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"W\">W</span> 的梯度，则：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(W)L=(∇_(y)L)x^(T)\"><span>∇<sub>W</sub>L=(∇<sub>y</sub>L)x<sup>T</sup></span></div>\n<p>两个结果都可以通过矩阵乘法直接得到，无须创建输出对 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span> 或 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"W\">W</span> 中每个元素的完整偏导数组。</p>\n<p>对于逐元素激活函数：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y_(i)=f(x_(i))\"><span>y<sub>i</sub>=f(x<sub>i</sub>)</span></div>\n<p>其雅可比在理论上是对角矩阵：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"J= diag ( f&#39;(x_(1)),…,f&#39;(x_(n)) )\"><span>J= diag ( f&#39;(x<sub>1</sub>),…,f&#39;(x<sub>n</sub>) )</span></div>\n<p>显式创建这个对角矩阵会存储大量无用的零。反向传播只需计算：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(x)L = ∇_(y)L⊙ f&#39;(x)\"><span>∇<sub>x</sub>L = ∇<sub>y</sub>L⊙ f&#39;(x)</span></div>\n<p>其中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"⊙\">⊙</span> 表示逐元素乘法。整个过程只处理与输入同等规模的张量。</p>\n<p>卷积也无须展开成庞大的矩阵。卷积的反向可以利用卷积或相关运算直接得到输入和卷积核的梯度。注意力、归一化和其他复杂算子同样会实现专门的反向规则，直接完成所需的 VJP。</p>\n<p>因此，自动求导框架为算子实现的接口可以理解为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"输入：与节点输出同形的上游梯度\"><span>输入：与节点输出同形的上游梯度</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"输出：与各节点输入同形的梯度\"><span>输出：与各节点输入同形的梯度</span></div>\n<p>完整雅可比只存在于数学描述中，不必作为真实张量出现在显存里。这种做法同时节省了存储、构造雅可比的时间以及随后进行大规模乘法的成本。</p>\n<p>反向模式尤其适合神经网络训练，是因为训练问题通常具有大量输入和少量输出。若模型有 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"P\">P</span> 个参数：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"θ=(θ_(1),…,θ_(P))\"><span>θ=(θ<sub>1</sub>,…,θ<sub>P</sub>)</span></div>\n<p>而最终损失只有一个标量：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=L(θ)\"><span>L=L(θ)</span></div>\n<p>则完整导数是一个含 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"P\">P</span> 个元素的梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(θ) L = ( ∂ L/∂θ_(1), …, ∂ L/∂θ_(P) )\"><span>∇<sub>θ</sub> L = ( ∂ L/∂θ<sub>1</sub>, …, ∂ L/∂θ<sub>P</sub> )</span></div>\n<p>反向模式从：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ L=1\"><span>∂ L/∂ L=1</span></div>\n<p>开始，一次逆向遍历便能得到损失对全部参数的梯度。它的成本主要随计算图规模增长，而不是为每个参数单独执行一次求导。</p>\n<p>前向模式自动微分传播的是另一种乘积。给定输入方向 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"r\">r</span>，前向模式计算：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Jr\"><span>Jr</span></div>\n<p>这称为雅可比—向量积，即 JVP。它回答的是：如果输入沿方向 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"r\">r</span> 发生微小变化，所有输出会怎样变化。</p>\n<p>两种模式的区别可以概括为：</p>\n<ul><li>反向模式计算 VJP，适合大量输入、少量输出；</li><li>前向模式计算 JVP，适合少量输入、大量输出。</li></ul>\n<p>如果函数只有几个输入，却有大量输出，前向模式可以针对一个输入方向一次得到所有输出的变化。若用反向模式获得完整雅可比，则通常需要针对多个输出方向反复执行反向。</p>\n<p>相反，神经网络训练有数百万甚至数十亿个参数，却通常只有一个标量损失。若采用前向模式计算每个参数方向的导数，就需要许多次传播；反向模式只需从标量损失反传一次，因此更为合适。</p>\n<p>当被求导的输出不是标量时，反向模式必须提供一个与输出同形的种子向量 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"v\">v</span>。此时计算的是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"v^(T)J\"><span>v<sup>T</sup>J</span></div>\n<p>而不是整个 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"J\">J</span>。它等价于先将向量输出按照 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"v\">v</span> 加权组合成一个标量，再求这个标量对输入的梯度。标准训练中的损失已经是标量，因此初始种子自然是 1。</p>\n<p>完整雅可比并非绝对不能获得。某些自动微分框架提供显式 Jacobian 接口，其实现通常通过多次 VJP 或 JVP 调用，将不同方向的结果组合成完整矩阵。但当输入和输出维度都很大时，这种计算的时间和空间成本仍然很高，因此不适合作为普通训练的默认方式。</p>\n<p>所以，反向传播的高效之处不仅在于应用链式法则，还在于只计算训练真正需要的对象：沿标量损失给出的方向，把上游梯度直接变换为输入梯度，而不显式保存每个输出对每个输入的全部偏导。</p></div>\n<table class=\"dd-table\"><thead><tr><th>模式</th><th>适合的输出/输入关系</th><th>直觉</th></tr></thead><tbody><tr><td>反向模式</td><td>大量输入参数 → 少量标量输出</td><td>一次反向得到损失对所有参数梯度</td></tr><tr><td>前向模式</td><td>少量输入 → 大量输出</td><td>沿给定输入方向向前传播导数</td></tr></tbody></table>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">7</span>反向为什么比纯推理占更多内存<span class=\"dd-badge eng\">工程</span></h2>\n<div class=\"dd-agent-response\"><p>这一节解释训练显存高于纯前向推理的根本原因：反向传播需要使用前向阶段的中间状态，因此许多在推理中可以立即释放的张量，在训练中必须继续保留。</p>\n<p>模型参数数量虽然没有改变，但训练和推理对中间结果生命周期的要求不同。</p>\n<p>在纯前向推理中，一层的输出交给下一层后，如果上一层的激活不再参与后续计算，其内存通常就可以释放或复用。系统主要需要保留当前仍在前向路径上使用的张量，因此中间状态可以像流水线一样逐步产生、逐步丢弃。</p>\n<p>训练除了前向计算，还要沿计算图逆序执行反向传播。许多局部反向公式都依赖前向时的输入、输出或统计量，不能只凭算子名称和上游梯度凭空计算。</p>\n<p>例如，对于 ReLU：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y=max(0,x)\"><span>y=max(0,x)</span></div>\n<p>反向规则为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = ∂ L/∂ y · 𝟙_(x&gt;0)\"><span>∂ L/∂ x = ∂ L/∂ y · 𝟙<sub>x&gt;0</sub></span></div>\n<p>因此，反向阶段必须知道前向输入的哪些位置大于 0。框架可以保存输入、输出或压缩后的掩码，但总要保留足以恢复这个判断的信息。</p>\n<p>对于矩阵层：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Y=WX\"><span>Y=WX</span></div>\n<p>其反向公式为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ W = ∂ L/∂ YX^(T)\"><span>∂ L/∂ W = ∂ L/∂ YX<sup>T</sup></span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ X = W^(T)∂ L/∂ Y\"><span>∂ L/∂ X = W<sup>T</sup>∂ L/∂ Y</span></div>\n<p>计算权重梯度需要前向输入激活 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"X\">X</span>，计算输入梯度则需要参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"W\">W</span>。因此，输入激活不能在前向完成后立即丢弃。</p>\n<p>归一化层的反向可能需要前向计算中的输入、均值、方差或标准化结果。其他激活函数、注意力和卷积算子也都有各自必须保存的中间状态。</p>\n<p>训练前向因此承担两项任务：</p>\n<ul><li>计算预测和损失；</li><li>为未来的反向传播保存必要材料。</li></ul>\n<p>这些等待反向使用的激活，是训练相较推理增加的第一大类内存占用。</p>\n<p>训练时主要的内存项目通常包括：</p>\n<ul><li>模型参数；</li><li>前向激活和局部反向所需的中间状态；</li><li>参数梯度；</li><li>优化器状态；</li><li>算子执行期间使用的临时工作空间。</li></ul>\n<p>其中，参数是推理和训练都需要的基础占用。训练额外增加的主要是激活、梯度和优化器状态。</p>\n<p>参数梯度通常与对应参数具有相同形状。对于每个可训练参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ\">θ</span>，反向传播需要保存：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂θ\"><span>∂ L/∂θ</span></div>\n<p>因此，仅参数和梯度两项就可能需要两份同等规模的存储，具体大小还取决于各自的数据类型。</p>\n<p>优化器还可能为每个参数维护跨训练步骤持续存在的状态。例如动量类优化器会保存历史梯度统计量。这些状态不是一次反向结束后就能释放的临时数据，而是下一次更新仍要继续使用的训练状态。</p>\n<p>激活内存则通常受以下因素影响：</p>\n<ul><li>批量大小；</li><li>序列长度；</li><li>特征维度；</li><li>网络深度；</li><li>算子需要保存的中间状态种类。</li></ul>\n<p>批量越大，每一层的激活包含的样本越多。序列越长，注意力和序列模型中的中间张量通常越大。网络越深，同时等待反向使用的层数也越多。因此，即使模型参数完全不变，增大批量大小或上下文长度也可能显著增加训练显存。</p>\n<p>一次训练迭代中的激活生命周期大致为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"前向产生激活 → 保存反向所需状态 → 得到损失 → 反向逐层读取状态 → 释放已完成节点的状态\"><span>前向产生激活 → 保存反向所需状态 → 得到损失 → 反向逐层读取状态 → 释放已完成节点的状态</span></div>\n<p>反向刚开始时，大量层的激活仍然同时保存在显存中，所以峰值占用通常远高于只做前向推理。</p>\n<p>梯度检查点改变了“保存全部必要激活”的策略。它把网络划分成若干计算段，前向时只保存少数边界位置的激活，也就是检查点。段内其他中间值在前向使用后即可释放。</p>\n<p>当反向传播到某一段时，系统从最近的检查点出发，重新执行该段前向，临时恢复反向所需的中间值，然后完成这段反向。</p>\n<p>这种方法的核心权衡是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"减少激活存储 ↔ 增加前向重算\"><span>减少激活存储 ↔ 增加前向重算</span></div>\n<p>保存的检查点越少，通常能释放的激活越多，但反向时需要重复计算的内容也越多。因此，梯度检查点用额外计算时间换取更低的显存占用。</p>\n<p>在理想条件下，梯度检查点不会改变数学上的梯度。第一次前向与重算前向若得到完全相同的中间值，后续执行的仍是同一套局部导数和链式法则。</p>\n<p>但这要求重算过程能够复现原前向结果。</p>\n<p>例如 dropout 在前向中生成随机掩码。若第一次前向使用掩码 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"M_(1)\">M<sub>1</sub></span>，重算时却使用另一张掩码 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"M_(2)\">M<sub>2</sub></span>，那么恢复的中间值就不属于产生当前损失的那次计算。反向传播随后计算出的梯度也不再对应原损失。因此，检查点机制通常需要保存或恢复随机数状态，使重算得到相同掩码。</p>\n<p>原地修改同样危险。如果检查点保存的输入或重算所依赖的张量后来被直接覆盖，那么重算得到的就不是原来的前向过程。</p>\n<p>带副作用的算子也可能破坏可重复性。例如算子会修改外部状态、更新计数器、读取变化中的可变数据，或者依赖无法恢复的随机行为。第二次执行即使输入看似相同，也可能产生不同结果。</p>\n<p>因此，使用梯度检查点时需要特别注意：</p>\n<ul><li>随机层能否恢复相同随机状态；</li><li>原地操作是否覆盖了重算所需的输入；</li><li>算子是否修改外部状态；</li><li>两次前向是否读取相同数据和模型状态；</li><li>实现中是否存在无法复现的非确定性行为。</li></ul>\n<p>还要区分“评估模式”和“关闭梯度记录”。将模型切换到评估模式，通常只会改变 dropout、归一化等层的行为，不一定停止计算图构建。如果程序仍要求梯度，框架仍可能保存中间状态。要获得纯推理的低内存特征，通常还需要明确禁用梯度记录。</p>\n<p>因此，训练显存高并不是模型参数突然变多，而是系统同时承担了更多职责：保存前向历史、存放反向梯度，并维护优化器状态。梯度检查点则通过选择性遗忘和按需重算，重新安排了计算与存储之间的权衡。</p></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">8</span>哪些代码操作会悄悄切断或污染计算图<span class=\"dd-badge eng\">失败模式</span></h2>\n<div class=\"dd-agent-response\"><p>这一节讨论自动求导的工程边界：即使数学公式完全正确，程序实际执行的张量操作仍可能切断或污染计算图，使梯度变成 <code>None</code>、全零、<code>NaN</code>、<code>Inf</code>，或者产生形状正确但数值错误的结果。</p>\n<p>排查这类问题时，首先要区分不同症状可能表达的含义：</p>\n<ul><li>梯度为 <code>None</code>，通常说明参数与损失之间不存在可追踪的计算路径，或者该参数没有参与此次前向计算；</li><li>梯度恒为零，可能是真实导数为零，也可能来自梯度阻断、激活饱和、不可微操作或低精度下溢；</li><li>梯度出现 <code>NaN</code> 或 <code>Inf</code>，通常与数值溢出、非法运算或不稳定的计算有关；</li><li>梯度方向似乎合理但大小异常，可能来自漏掉路径、错误广播、旧梯度累积或缩放处理错误；</li><li>梯度随训练步骤不断增大，可能是忘记清零，也可能是模型本身发生梯度爆炸。</li></ul>\n<p>症状只能缩小排查范围，不能单独确定根因。可靠诊断必须回到代码实际执行的张量操作和独立的梯度检查结果。</p>\n<p>第一类故障是计算图被切断。</p>\n<p>设：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"a=f(w)\"><span>a=f(w)</span></div>\n<p>如果随后执行分离操作：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"b=detach(a)\"><span>b=detach(a)</span></div>\n<p>再计算：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=g(b)\"><span>L=g(b)</span></div>\n<p>虽然 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span> 的数值与 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"a\">a</span> 相同，但 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span> 已经不再保留 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"a\">a</span> 的计算历史。损失 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L\">L</span> 无法沿：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L→ b→ a→ w\"><span>L→ b→ a→ w</span></div>\n<p>这条路径返回到参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span>，因此 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的梯度可能为 <code>None</code>。</p>\n<p>把张量转换为普通数组、普通数值，或者交给不受自动求导系统追踪的外部程序，也可能产生相同效果。即使之后再把结果转换回张量，新张量通常也只是一个没有原计算历史的独立对象，原先的依赖关系不会自动恢复。</p>\n<p>排查时应检查：</p>\n<ul><li>参数是否启用了梯度追踪；</li><li>损失是否确实依赖该参数；</li><li>中间是否使用了 <code>detach</code> 或等价操作；</li><li>是否把张量转成普通数组或标量后又重新构造张量；</li><li>相关计算是否发生在禁用梯度记录的作用域中；</li><li>参数是否只存在于未实际执行的条件分支；</li><li>模型中是否存在此次样本没有使用到的参数。</li></ul>\n<p>需要注意，梯度为 <code>None</code> 与梯度为零并不相同。<code>None</code> 通常表示没有形成可求导路径；零梯度则表示路径可能存在，但当前局部导数或总路径贡献为零。</p>\n<p>第二类故障是原地修改保存的激活。</p>\n<p>反向传播经常需要读取前向阶段保存的张量。若代码直接在原内存上修改该张量，反向阶段看到的可能不再是产生当前损失时的值。</p>\n<p>例如某个节点的反向公式需要原始输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span>，但前向之后代码执行了：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"u← h(u)\"><span>u← h(u)</span></div>\n<p>如果这是原地覆盖，那么反向读取的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u\">u</span> 已经发生变化。局部导数可能无法计算，或者根据错误状态计算。</p>\n<p>成熟框架通常会通过张量版本信息检测部分危险修改并报错，但没有报错并不保证所有情况都安全。共享存储、视图、自定义算子和绕过框架检查的操作仍可能造成隐蔽污染。</p>\n<p>排查方法包括：</p>\n<ul><li>暂时把原地操作改成生成新张量的非原地形式；</li><li>启用自动求导异常检测；</li><li>检查多个张量是否共享底层存储；</li><li>查看报错指向的前向节点，而不只关注反向时报错的位置；</li><li>检查自定义算子是否保存后又修改了同一个张量。</li></ul>\n<p>第三类故障是忘记清零参数梯度。</p>\n<p>许多自动求导框架默认把新梯度累加到参数已有的梯度缓冲区中：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(buffer) ← g_(buffer)+g_(new)\"><span>g<sub>buffer</sub> ← g<sub>buffer</sub>+g<sub>new</sub></span></div>\n<p>这是为了支持同一计算图中的路径累加和跨 mini-batch 梯度累积。但如果训练代码希望每个批次独立更新，却没有在正确时机清零，那么后续步骤看到的就不是当前批次梯度，而是多个批次贡献之和。</p>\n<p>典型训练顺序为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"清零梯度 → 前向 → 计算损失 → 反向 → 参数更新\"><span>清零梯度 → 前向 → 计算损失 → 反向 → 参数更新</span></div>\n<p>如果有意进行梯度累积，则应明确：</p>\n<ul><li>累积多少个微批次；</li><li>损失或梯度是否需要按累积步数缩放；</li><li>何时执行参数更新；</li><li>更新后何时清零。</li></ul>\n<p>排查时可以记录每一步反向后的梯度值或梯度范数。如果梯度在没有数学原因的情况下持续包含历史贡献，应检查清零时机。</p>\n<p>第四类故障来自混合精度中的下溢和溢出。</p>\n<p>低精度浮点格式的可表示范围和有效数字有限。很小的梯度可能下溢为 0，很大的激活或梯度可能溢出为 <code>Inf</code>，后续运算又可能产生 <code>NaN</code>。</p>\n<p>损失缩放的基本方法是先把损失乘以一个尺度 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"s\">s</span>：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L&#39;=sL\"><span>L&#39;=sL</span></div>\n<p>于是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(θ) L&#39; = s∇_(θ) L\"><span>∇<sub>θ</sub> L&#39; = s∇<sub>θ</sub> L</span></div>\n<p>原本过小的梯度被放大，降低下溢风险。参数更新前再把梯度除以 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"s\">s</span>，恢复原来的尺度。</p>\n<p>诊断时应检查：</p>\n<ul><li>梯度和激活中有限值所占比例；</li><li>零值、<code>NaN</code> 和 <code>Inf</code> 的数量；</li><li>问题是否只在低精度训练中出现；</li><li>临时切换到更高精度后问题是否消失；</li><li>损失缩放和反缩放顺序是否正确；</li><li>是否在反缩放之前错误地进行梯度裁剪或阈值判断。</li></ul>\n<p>如果动态损失缩放检测到非有限梯度，通常会跳过当前参数更新并调整缩放因子。这种跳步本身可能是正常保护行为，但若频繁发生，说明数值范围仍有问题。</p>\n<p>第五类问题是不可微离散操作。</p>\n<p>取整、硬阈值、离散采样、取类别索引和某些选择操作，可能使输出在输入的一大片区域内保持不变。此时局部导数通常为零；在跳变位置，经典导数可能不存在。</p>\n<p>这并非计算图损坏，而是所选择的运算本身没有适合普通梯度下降的连续导数。解决方法取决于任务，可以采用：</p>\n<ul><li>平滑近似；</li><li>可微松弛；</li><li>代理梯度；</li><li>专门的梯度估计器；</li><li>重新设计损失或模型目标。</li></ul>\n<p>这些方法得到的可能是近似梯度、代理梯度或随机估计量，不应误认为原离散函数的精确普通导数。</p>\n<p>第六类问题是错误广播。</p>\n<p>广播允许不同形状的张量完成合法运算，但前向能够运行不等于轴语义正确。</p>\n<p>例如：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"Y=X+b\"><span>Y=X+b</span></div>\n<p>如果 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span> 在批次维度上被重复使用，那么反向传播计算 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span> 的梯度时，必须把所有批次位置的贡献求和回 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span> 的原始形状：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ b = Σ_(被广播维度) ∂ L/∂ Y\"><span>∂ L/∂ b = Σ<sub>被广播维度</sub> ∂ L/∂ Y</span></div>\n<p>如果张量轴排列错误，程序可能仍能广播成功，却在错误维度上重复参数。反向传播随后也会沿错误维度求和。最终梯度形状甚至可能仍然正确，但每个数值的实际含义已经错位。</p>\n<p>因此，应检查：</p>\n<ul><li>每个张量的完整形状；</li><li>各轴分别代表批次、时间、通道还是特征；</li><li>广播实际发生在哪些轴；</li><li>反向归约是否对应被扩展的轴；</li><li>改成尺寸为 1、2、3 的小例子后能否手工验证。</li></ul>\n<p>一个实用的排查顺序是：</p>\n<ol><li>确认参数与损失之间是否存在完整的计算图路径。</li><li>检查是否使用了分离操作、普通数组转换或禁用梯度作用域。</li><li>检查反向所需的激活是否被原地修改。</li><li>明确参数梯度的清零和有意累积时机。</li><li>检查梯度及激活中的零、<code>NaN</code>、<code>Inf</code> 和其他异常值。</li><li>用更高精度进行对照，判断是否存在混合精度问题。</li><li>检查离散或不可微节点。</li><li>检查广播、归约和张量轴语义。</li><li>在小型确定性例子中，将自动梯度与手算或有限差分比较。</li></ol>\n<p>执行梯度检查时，应尽量关闭随机性、固定数据和模型状态、使用双精度，并一次只检查少量参数。若数值梯度稳定而自动梯度明显不符，应优先怀疑局部反向规则、图路径、分支累加和广播归约。</p>\n<p>同一种症状可能由多个故障共同造成。例如，忘记清零可能与低精度下溢同时存在；修复清零问题后，零梯度现象仍可能继续。错误广播也可能与某个分支被分离同时出现。</p>\n<p>因此，每修复一处，都应重新运行最小前向—反向测试和梯度检查。报错消失、梯度不再为零或损失开始下降，都只能证明某个症状发生了变化，不能单独证明整条计算图和所有梯度已经正确。</p></div>\n<table class=\"dd-table\"><thead><tr><th>故障</th><th>机制</th><th>检查方法</th></tr></thead><tbody><tr><td><code>detach</code> / 转成普通数组</td><td>明确停止记录后续依赖</td><td>检查 requires-grad 和图边界</td></tr><tr><td>原地修改保存的激活</td><td>反向需要的前向值已被覆盖</td><td>启用异常检测，避免危险 in-place</td></tr><tr><td>忘记清零梯度</td><td>多数框架默认累加到旧值</td><td>记录每步梯度并明确 zero-grad 时机</td></tr><tr><td>混合精度下溢/溢出</td><td>小梯度变 0 或大值变 Inf</td><td>损失缩放、检查 finite 比例</td></tr><tr><td>不可微离散操作</td><td>局部没有可用连续导数</td><td>使用代理、估计器或改写目标</td></tr><tr><td>错误广播</td><td>形状合法但梯度在错误维度求和</td><td>检查张量形状和小例子手算</td></tr></tbody></table>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">9</span>运行示例：用有限差分给自动梯度做体检<span class=\"dd-badge eng\">验证</span></h2>\n<div class=\"dd-agent-response\"><p>这一节说明如何用有限差分检查自动求导：反向传播负责高效计算全部参数的梯度，有限差分则作为一条相对独立的数值路线，对少量参数进行抽查。</p>\n<p>仍使用贯穿示例：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"x=2, y=6\"><span>x=2, y=6</span></div>\n<p>模型和损失为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ŷ=wx\"><span>ŷ=wx</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L(w)=1/2(ŷ-y)^(2) =1/2(wx-y)^(2)\"><span>L(w)=1/2(ŷ-y)<sup>2</sup> =1/2(wx-y)<sup>2</sup></span></div>\n<p>反向传播给出的解析梯度是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g=∂ L/∂ w =(wx-y)x\"><span>g=∂ L/∂ w =(wx-y)x</span></div>\n<p>在 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w=2\">w=2</span> 处：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g=(2×2-6)×2 =(-2)×2 =-4\"><span>g=(2×2-6)×2 =(-2)×2 =-4</span></div>\n<p>现在不使用这条反向公式，而是只运行前向损失，从参数两侧估计数值梯度。中心差分为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(num) = (L(w+ε)-L(w-ε))/2ε\"><span>g<sub>num</sub> = (L(w+ε)-L(w-ε))/2ε</span></div>\n<p>取：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w=2, ε=10^(-4)\"><span>w=2, ε=10<sup>-4</sup></span></div>\n<p>向正方向扰动：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w+ε=2.0001\"><span>w+ε=2.0001</span></div>\n<p>对应的预测为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ŷ_(+)=2.0001×2=4.0002\"><span>ŷ<sub>+</sub>=2.0001×2=4.0002</span></div>\n<p>损失为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L(2.0001) = 1/2(4.0002-6)^(2) = 1/2(-1.9998)^(2) = 1.99960002\"><span>L(2.0001) = 1/2(4.0002-6)<sup>2</sup> = 1/2(-1.9998)<sup>2</sup> = 1.99960002</span></div>\n<p>向负方向扰动：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"w-ε=1.9999\"><span>w-ε=1.9999</span></div>\n<p>对应的预测为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ŷ_(-)=1.9999×2=3.9998\"><span>ŷ<sub>-</sub>=1.9999×2=3.9998</span></div>\n<p>损失为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L(1.9999) = 1/2(3.9998-6)^(2) = 1/2(-2.0002)^(2) = 2.00040002\"><span>L(1.9999) = 1/2(3.9998-6)<sup>2</sup> = 1/2(-2.0002)<sup>2</sup> = 2.00040002</span></div>\n<p>代入中心差分：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(num) = (1.99960002-2.00040002)/(2×10^(-4))\"><span>g<sub>num</sub> = (1.99960002-2.00040002)/(2×10<sup>-4</sup>)</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(num) = (-0.0008)/0.0002 = -4\"><span>g<sub>num</sub> = (-0.0008)/0.0002 = -4</span></div>\n<p>因此：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(num)≈ g=-4\"><span>g<sub>num</sub>≈ g=-4</span></div>\n<p>在这个例子中，损失关于 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 是二次函数。中心差分在理想算术下能消去二次项造成的一阶近似误差，所以结果特别准确；实际程序中仍会存在有限的浮点误差。</p>\n<p>数值梯度可以作为自动梯度的独立参照，是因为它只依赖：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L(w+ε) 和 L(w-ε)\"><span>L(w+ε) 和 L(w-ε)</span></div>\n<p>这两次前向计算的损失值。它不使用被检查节点的反向公式。如果自定义反向规则写错，而前向计算正确，自动梯度与数值梯度通常会出现差异。</p>\n<p>比较二者时，可以使用相对误差：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"E_(rel) = (|g-g_(num)|)/(|g|+|g_(num)|+δ)\"><span>E<sub>rel</sub> = (|g-g<sub>num</sub>|)/(|g|+|g<sub>num</sub>|+δ)</span></div>\n<p>其中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"δ\">δ</span> 是一个很小的正数，用于防止分母为零。</p>\n<p>相对误差考虑了梯度自身的尺度。例如，当梯度大小为 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"10^(3)\">10<sup>3</sup></span> 时，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"10^(-5)\">10<sup>-5</sup></span> 的绝对差通常可以忽略；当梯度大小为 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"10^(-6)\">10<sup>-6</sup></span> 时，同样的绝对差就可能很严重。</p>\n<p>不过，当两个梯度都接近零时，相对误差可能因分母很小而显得很大。因此，实际检查中应同时观察绝对误差：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"E_(abs) = |g-g_(num)|\"><span>E<sub>abs</sub> = |g-g<sub>num</sub>|</span></div>\n<p>只有结合绝对误差、相对误差和梯度尺度，才能合理判断结果。</p>\n<p>有限差分检查需要满足若干条件，否则即使反向实现正确，也可能出现不一致。</p>\n<p>第一，应尽量避开不可导点。以 ReLU 为例：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"f(u)=max(0,u)\"><span>f(u)=max(0,u)</span></div>\n<p>在 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"u=0\">u=0</span> 处，左右导数不同。中心差分会同时观察折点两侧，而自动求导框架会按照预先约定返回某个次梯度。二者可能不同，但这不一定说明反向实现有错。</p>\n<p>对于分段函数，应分别选择每个可导区域内的测试点，并对不可导边界按照算子约定单独处理。</p>\n<p>第二，前向计算必须具有确定性。若两次有限差分前向使用了不同的 dropout 掩码，那么：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L(w+ε)-L(w-ε)\"><span>L(w+ε)-L(w-ε)</span></div>\n<p>同时包含参数扰动和随机噪声，差商就无法可靠表示参数方向的导数。</p>\n<p>因此需要：</p>\n<ul><li>关闭 dropout 等随机行为，或严格恢复相同随机状态；</li><li>固定输入数据；</li><li>固定模型状态；</li><li>避免两次前向之间更新运行统计量；</li><li>避免带有不可控副作用的算子。</li></ul>\n<p>第三，<span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 必须处于合适范围。</p>\n<p>当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 太大时，差商测量的是较宽区间上的平均斜率，不能充分代表当前点的局部导数，会产生截断误差。</p>\n<p>当 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 太小时，两个损失值非常接近，相减时容易丢失有效数字。得到的舍入误差再除以很小的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"2ε\">2ε</span>，可能被进一步放大。</p>\n<p>因此，不应只尝试一个 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span>，而应测试多个数量级。典型现象是：</p>\n<ul><li>较大的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 受截断误差影响；</li><li>中间某个范围内，数值梯度相对稳定；</li><li>极小的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 开始受到浮点舍入误差影响。</li></ul>\n<p>检查应优先使用这个稳定区间中的结果。</p>\n<p>第四，梯度检查最好使用双精度。中心差分需要对两个非常接近的损失做减法，单精度更容易丢失有效数字。梯度检查的目标是验证求导逻辑，因此不必拘泥于正式训练所使用的低精度设置。</p>\n<p>第五，应把网络和参数规模缩小。有限差分每检查一个参数元素，都需要额外执行前向计算。检查完整大模型既昂贵，也不利于定位问题。</p>\n<p>对于张量参数，可以随机选取少量索引。每次只扰动其中一个元素，其他元素保持不变，再将数值梯度与自动梯度中同一位置的值进行比较。</p>\n<p>第六，应按照从局部到整体的顺序检查：</p>\n<ol><li>先检查单个自定义算子的各输入梯度；</li><li>再检查一层或少量算子的组合；</li><li>最后检查小型端到端网络；</li><li>对不同参数位置和不同输入区域重复抽查。</li></ol>\n<p>如果直接检查完整网络并发现不一致，问题可能来自任何一个节点、广播维度或分叉路径，很难定位。局部测试能更快判断是哪条反向规则出现错误。</p>\n<p>测试用例还应覆盖算子的不同分支。例如检查 ReLU 时，应分别选择正输入和负输入；检查广播时，应使用能暴露归约轴的小张量；检查共享参数时，应确保同一参数确实沿多条路径参与损失。</p>\n<p>若梯度检查失败，可以根据现象进一步定位：</p>\n<ul><li>多个 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 下数值梯度都稳定，但与自动梯度不一致：优先检查反向公式、路径累加和广播归约；</li><li>数值梯度随 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"ε\">ε</span> 剧烈变化：检查浮点精度、随机性、不可导点和损失尺度；</li><li>自动梯度为 <code>None</code>：检查参数是否参与损失以及计算图是否被切断；</li><li>自动梯度为零而数值梯度非零：检查分离操作、错误局部导数、不可微操作和低精度下溢；</li><li>单层检查通过但端到端检查失败：检查层间组合、共享变量、分叉求和和状态变化。</li></ul>\n<p>即使所有抽查都通过，也不能证明整个反向实现对所有输入和所有参数都正确。它只能说明：在所选择的参数元素、输入数据和取值点上，自动梯度与有限差分给出了相近结果。</p>\n<p>因此，梯度检查属于有针对性的抽样验证。它应当覆盖代表性的参数、数值范围和计算分支，并结合小模型、双精度、确定性前向、中心差分和多个步长，尽量让这份独立证据可靠。</p></div>\n<div class=\"dd-formula\">g<sub>num</sub>=[L(2+ε)−L(2−ε)]/(2ε) ≈ −4</div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">10</span>把整条因果链连起来<span class=\"dd-badge intuition\">综合</span></h2>\n<div class=\"dd-agent-response\"><p>这一节把一次训练迭代中的完整因果链连接起来，并划清反向传播与损失函数、优化器和评测之间的职责边界。</p>\n<p>整个过程可以概括为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"当前参数 → 前向预测 → 标量损失 → 反向梯度 → 优化器更新 → 新参数\"><span>当前参数 → 前向预测 → 标量损失 → 反向梯度 → 优化器更新 → 新参数</span></div>\n<p>反向传播位于损失计算和参数更新之间。它负责回答“当前损失对每个参数有多敏感”，但不负责定义训练目标，也不负责决定参数实际更新多少。</p>\n<p>第一步是使用当前参数执行前向传播。</p>\n<p>给定输入 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"x\">x</span>、目标 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"y\">y</span> 和模型参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ\">θ</span>，模型产生预测：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ŷ=f(x;θ)\"><span>ŷ=f(x;θ)</span></div>\n<p>损失函数再计算：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=ℓ(ŷ,y)\"><span>L=ℓ(ŷ,y)</span></div>\n<p>训练中通常把损失归约成一个标量。前向传播还会记录计算图的依赖关系，并保存反向公式需要的中间值，例如输入激活、误差、激活掩码和归一化统计量。</p>\n<p>此时得到的只是当前参数在当前数据上的预测和损失，参数本身尚未改变。</p>\n<p>第二步是从标量损失启动反向传播。</p>\n<p>最终节点是损失自身，因此反向种子为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ L=1\"><span>∂ L/∂ L=1</span></div>\n<p>这个 1 表示损失对自身的一单位敏感度。它不是学习率，也不是人为设定的优化信号，而是反向传播的数学起点。</p>\n<p>如果待求导输出不是标量，则需要额外提供一个与输出同形的种子梯度。标准训练通常已经将输出误差归约为标量损失，所以初始种子自然是 1。</p>\n<p>第三步是按照逆拓扑顺序遍历计算图。</p>\n<p>前向传播必须先得到节点输入，才能计算节点输出。反向传播则必须先知道损失对节点输出的梯度，才能求损失对节点输入的梯度。因此，反向遍历顺序与前向计算顺序相反。</p>\n<p>设某个节点执行：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z=f(u)\"><span>z=f(u)</span></div>\n<p>节点从下游收到上游梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"z̄=∂ L/∂ z\"><span>z̄=∂ L/∂ z</span></div>\n<p>再结合自身的局部导数，计算传给输入的梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"ū = ∂ L/∂ u = ∂ L/∂ z ∂ z/∂ u\"><span>ū = ∂ L/∂ u = ∂ L/∂ z ∂ z/∂ u</span></div>\n<p>每个算子只负责自己的局部规则。乘法节点处理乘法的导数，激活节点处理激活函数的导数，矩阵乘法节点处理对应的转置乘法。它们不需要知道完整模型的结构。</p>\n<p>第四步是直接计算向量—雅可比积。</p>\n<p>对于张量节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y=f(x)\"><span>y=f(x)</span></div>\n<p>完整局部导数是雅可比矩阵：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"J=∂ y/∂ x\"><span>J=∂ y/∂ x</span></div>\n<p>但反向传播不需要显式构造 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"J\">J</span>。如果下游梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"v^(T)=∂ L/∂ y\"><span>v<sup>T</sup>=∂ L/∂ y</span></div>\n<p>当前节点只需计算：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = v^(T)J\"><span>∂ L/∂ x = v<sup>T</sup>J</span></div>\n<p>这就是向量—雅可比积。算子利用自身结构直接完成这一计算，避免生成巨大的完整雅可比矩阵。</p>\n<p>第五步是在分叉和共享变量处累加梯度贡献。</p>\n<p>如果同一个变量通过多条路径影响损失，每条路径都会返回一份梯度。该变量的总梯度必须是所有路径贡献之和：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ u = Σ_(k) ( ∂ L/∂ u )_(k)\"><span>∂ L/∂ u = Σ<sub>k</sub> ( ∂ L/∂ u )<sub>k</sub></span></div>\n<p>残差连接、权重共享和循环网络中的参数复用都依赖这个规则。只保留某一条路径，或让后到达的贡献覆盖先前贡献，都会漏掉总导数的一部分。</p>\n<p>第六步是得到所有可训练参数的梯度。</p>\n<p>逆拓扑遍历完成后，每个参与当前损失计算且要求求导的参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"θ\">θ</span> 都会获得：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(θ) L = ∂ L/∂θ\"><span>∇<sub>θ</sub> L = ∂ L/∂θ</span></div>\n<p>这个梯度描述当前参数点附近，参数发生微小变化时损失的一阶变化：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L(θ+Δθ) ≈ L(θ) + ∇_(θ) L^(T)Δθ\"><span>L(θ+Δθ) ≈ L(θ) + ∇<sub>θ</sub> L<sup>T</sup>Δθ</span></div>\n<p>到这里，反向传播的主要职责已经完成。它交付的是梯度，不是新参数。</p>\n<p>第七步可以使用有限差分和局部测试验证梯度实现。</p>\n<p>有限差分通过参数两侧的前向损失估计数值梯度：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(num) = (L(θ_(i)+ε)-L(θ_(i)-ε))/2ε\"><span>g<sub>num</sub> = (L(θ<sub>i</sub>+ε)-L(θ<sub>i</sub>-ε))/2ε</span></div>\n<p>将它与反向传播得到的：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g=∂ L/∂θ_(i)\"><span>g=∂ L/∂θ<sub>i</sub></span></div>\n<p>比较，可以抽查局部反向规则、广播归约、路径累加或自定义算子是否正确。</p>\n<p>这类检查不是每次训练迭代的必要组成部分，也不能证明所有参数在所有取值上都正确。它是一种开发和诊断工具。</p>\n<p>第八步才是优化器更新参数。</p>\n<p>最简单的梯度下降规则为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"θ_(new) = θ-η∇_(θ) L\"><span>θ<sub>new</sub> = θ-η∇<sub>θ</sub> L</span></div>\n<p>其中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"η\">η</span> 是学习率。更复杂的优化器还可能结合动量、历史平方梯度、权重衰减或其他状态处理当前梯度。</p>\n<p>因此，反向传播和优化器的分工是：</p>\n<ul><li>反向传播计算梯度；</li><li>优化器读取并变换梯度；</li><li>优化器决定更新幅度并修改参数。</li></ul>\n<p>反向传播不会自动修改参数，也不会决定学习率、动量、梯度裁剪或权重衰减。</p>\n<p>即使梯度方向正确，一次实际更新也不保证损失下降。负梯度给出的是无穷小邻域中的局部下降方向：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"-∇_(θ) L\"><span>-∇<sub>θ</sub> L</span></div>\n<p>如果学习率太大，实际更新可能超出局部线性近似成立的区域，使损失震荡甚至上升。梯度提供局部方向和变化率，具体步长仍由优化器负责。</p>\n<p>反向传播也不负责判断训练目标是否正确。它只会忠实地计算给定损失函数的导数。如果损失函数没有表达真正目标，那么梯度仍可能在数学上完全正确，但模型会优化错误或不完整的目标。</p>\n<p>例如，训练损失持续下降而验证指标没有改善，可能来自：</p>\n<ul><li>损失只是实际评测目标的代理；</li><li>模型发生过拟合；</li><li>训练数据与实际使用数据分布不同；</li><li>标签或样本权重存在问题；</li><li>评测指标包含损失没有体现的约束；</li><li>模型利用了数据中的错误捷径。</li></ul>\n<p>这些问题需要由数据、损失设计和独立评测解决，不能仅靠反向传播发现。</p>\n<p>反向传播同样不保证找到全局最优解。它计算当前点的局部导数，而长期训练结果还取决于模型结构、初始化、数据顺序、优化器、学习率和随机性。</p>\n<p>一次完整训练迭代可以整理为：</p>\n<ol><li>按训练策略清零或保留旧梯度；</li><li>使用当前参数执行前向传播；</li><li>计算标量损失并保存必要中间状态；</li><li>从 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"∂ L/∂ L=1\">∂ L/∂ L=1</span> 启动反向；</li><li>逆序执行各节点的局部向量—雅可比积；</li><li>在共享变量处累加所有路径贡献；</li><li>得到各可训练参数的梯度；</li><li>必要时执行反缩放、非有限值检查或梯度裁剪；</li><li>优化器读取梯度并更新参数；</li><li>使用损失和独立评测判断训练是否真正改善目标。</li></ol>\n<p>三方职责可以概括为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"【损失函数定义优化目标】\"><span>【损失函数定义优化目标】</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"【反向传播计算目标对参数的梯度】\"><span>【反向传播计算目标对参数的梯度】</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"【优化器根据梯度更新参数】\"><span>【优化器根据梯度更新参数】</span></div>\n<p>评测则负责判断模型在真正关心的标准上是否变好。</p>\n<p>因此，反向传播的准确职责是：给定当前前向计算图、必要中间状态和最终标量损失，高效计算该损失对所有需要求导变量的梯度。它连接“损失已经算出”和“优化器可以行动”，但不替代目标设计、参数更新策略或效果评估。</p></div>\n</section>\n<section class=\"dd-sec\" data-source=\"content-generation\"><h2><span class=\"dd-n\">13</span>概念依赖与延伸学习<span class=\"dd-badge eng\">路线</span></h2>\n<div class=\"dd-agent-response\"><p>这一节给出反向传播的概念依赖与后续学习路线。理解反向传播之后，还需要继续回答五类问题：从什么目标开始求导、梯度如何转化为更新、深层路径为什么会削弱梯度、网络结构如何改善传播，以及大规模训练怎样权衡计算、通信与内存。</p>\n<p>第一条依赖是损失函数。反向传播必须从一个明确的输出开始，训练中通常是标量损失：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=ℓ(ŷ,y)\"><span>L=ℓ(ŷ,y)</span></div>\n<p>反向传播计算的是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∇_(θ) L = ∂ L/∂θ\"><span>∇<sub>θ</sub> L = ∂ L/∂θ</span></div>\n<p>因此，在讨论梯度之前必须先回答：究竟在对什么标量求导？</p>\n<p>损失函数规定模型优化的目标。不同损失函数会产生不同的梯度，即使模型、参数和数据完全相同，更换损失后，参数收到的训练信号也可能发生变化。</p>\n<p>损失函数与反向传播之间的关系是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"损失定义什么结果更好 → 反向计算怎样改变参数会影响这个目标\"><span>损失定义什么结果更好 → 反向计算怎样改变参数会影响这个目标</span></div>\n<p>如果损失没有准确表达真实任务目标，反向传播仍然可以得到数学上完全正确的梯度，但模型会高效地优化错误或不完整的目标。因此，梯度正确不等于目标正确。</p>\n<p>第二条依赖是梯度下降。反向传播得到：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(t)=∇_(θ) L_(t)\"><span>g<sub>t</sub>=∇<sub>θ</sub> L<sub>t</sub></span></div>\n<p>之后，参数还没有发生变化。优化器需要把梯度转换成具体更新。最简单的梯度下降规则是：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"θ_(t+1) = θ_(t)-η g_(t)\"><span>θ<sub>t+1</sub> = θ<sub>t</sub>-η g<sub>t</sub></span></div>\n<p>其中 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"η\">η</span> 是学习率。</p>\n<p>梯度提供当前参数点附近的方向和变化率，学习率决定实际步长。如果步长过小，训练可能进展缓慢；如果步长过大，参数可能越过局部下降区域，使损失震荡或发散。</p>\n<p>在 mini-batch 训练中，当前梯度通常只是总体目标梯度的随机估计：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"g_(t) ≈ ∇_(θ) 𝔼_((x,y))[ℓ(f(x;θ),y)]\"><span>g<sub>t</sub> ≈ ∇<sub>θ</sub> 𝔼<sub>(x,y)</sub>[ℓ(f(x;θ),y)]</span></div>\n<p>不同批次会产生不同梯度，因此更新中带有采样噪声。后续学习梯度下降时，需要继续理解：</p>\n<ul><li>学习率怎样控制稳定性和收敛速度；</li><li>批量大小怎样影响梯度噪声；</li><li>动量怎样整合多个步骤的梯度方向；</li><li>梯度裁剪怎样限制异常更新；</li><li>权重衰减怎样改变实际优化目标或更新规则。</li></ul>\n<p>反向传播负责把方向算出来，优化器负责决定如何使用这个方向。</p>\n<p>第三条延伸是梯度消失与梯度爆炸。深层网络中的梯度由许多局部雅可比连续相乘。</p>\n<p>设网络各层为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"h_(k)=f_(k)(h_(k-1))\"><span>h<sub>k</sub>=f<sub>k</sub>(h<sub>k-1</sub>)</span></div>\n<p>则损失对早期状态 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"h_(0)\">h<sub>0</sub></span> 的梯度包含：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ h_(0) = ∂ L/∂ h_(n) ∂ h_(n)/(∂ h_(n-1)) … ∂ h_(1)/∂ h_(0)\"><span>∂ L/∂ h<sub>0</sub> = ∂ L/∂ h<sub>n</sub> ∂ h<sub>n</sub>/(∂ h<sub>n-1</sub>) … ∂ h<sub>1</sub>/∂ h<sub>0</sub></span></div>\n<p>如果每个局部导数在相关方向上都将梯度缩小，例如每层大约乘以 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"0.5\">0.5</span>，经过 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"n\">n</span> 层后，梯度尺度大约变成：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"0.5^(n)\"><span>0.5<sup>n</sup></span></div>\n<p>这个值会随深度指数下降，导致早期层几乎收不到有效训练信号。这就是梯度消失的一种直观来源。</p>\n<p>反过来，如果连续雅可比在相关方向上的缩放因子长期大于 1，例如每层约乘以 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"1.5\">1.5</span>，梯度尺度可能增长为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"1.5^(n)\"><span>1.5<sup>n</sup></span></div>\n<p>从而产生梯度爆炸。</p>\n<p>在张量情形下，不能只看雅可比中的单个元素，还要关注雅可比对不同向量方向的缩放作用。某些方向可能被压缩，另一些方向可能被放大。</p>\n<p>梯度消失并不表示反向传播漏掉了路径。它可能正是链式法则正确计算出的结果，只是连续局部变换使信号变得极小。理解这一问题需要继续学习激活函数、初始化、归一化、雅可比谱和深层网络的数值稳定性。</p>\n<p>第四条延伸是残差连接。普通深层路径要求梯度连续经过所有非线性变换，而残差块增加了一条恒等捷径：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"y=F(x)+x\"><span>y=F(x)+x</span></div>\n<p>它对输入的导数为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ y/∂ x = ∂ F(x)/∂ x+I\"><span>∂ y/∂ x = ∂ F(x)/∂ x+I</span></div>\n<p>所以反向梯度为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ x = ∂ L/∂ y ( ∂ F(x)/∂ x+I )\"><span>∂ L/∂ x = ∂ L/∂ y ( ∂ F(x)/∂ x+I )</span></div>\n<p>这里的 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"I\">I</span> 来自恒等路径。梯度不仅可以经过变换分支 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"F\">F</span>，还可以沿捷径直接传回输入。</p>\n<p>从计算图角度看，残差连接正是分叉路径求和：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"输入梯度 = 变换分支贡献 + 恒等分支贡献\"><span>输入梯度 = 变换分支贡献 + 恒等分支贡献</span></div>\n<p>即使变换分支中的局部雅可比明显缩小了梯度，恒等分支仍能提供一条更短、更直接的传播路径。</p>\n<p>残差连接没有绕过链式法则，也不能保证梯度绝不消失。它通过改变计算图结构，增加了一条导数为恒等映射的路径，从而改善深层网络中的信息与梯度传播。</p>\n<p>第五条延伸是分布式训练中的内存权衡。训练内存通常包括：</p>\n<ul><li>模型参数；</li><li>前向激活；</li><li>参数梯度；</li><li>优化器状态；</li><li>临时计算空间。</li></ul>\n<p>当这些内容无法放入单个设备时，可以对不同对象采用分片、重算或跨设备调度。</p>\n<p>参数可以在每台设备上完整复制，也可以分片保存。完整复制使每个设备都能直接访问全部参数，但每台设备都承担完整模型内存；参数分片能降低单设备占用，却需要在计算时进行额外通信。</p>\n<p>梯度也可以在设备之间同步、求和、平均或分片。数据并行中，不同设备使用不同数据计算局部梯度，然后聚合为与整体批次一致的梯度。聚合规则必须与损失的求和或平均方式保持一致。</p>\n<p>优化器状态通常与参数规模成比例，而且一个参数可能对应多份历史统计量。将优化器状态分片到多个设备，可以避免每个设备都保存完整副本。</p>\n<p>激活则主要随批量大小、序列长度、网络深度和特征宽度增长。减少激活内存的方法包括：</p>\n<ul><li>使用梯度检查点，在反向时重算部分前向；</li><li>调整微批次大小，降低单次峰值内存；</li><li>进行流水线划分，让不同设备负责不同网络层；</li><li>沿张量或序列维度切分计算和激活；</li><li>在计算、通信和存储之间安排更复杂的调度。</li></ul>\n<p>这些方法共同面对一个基本权衡：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"更低的单设备内存 ↔ 更多重算、通信或调度复杂度\"><span>更低的单设备内存 ↔ 更多重算、通信或调度复杂度</span></div>\n<p>分布式训练没有改变反向传播的数学规则。每个节点仍执行局部向量—雅可比积，共享变量仍累加所有路径贡献。改变的是参数、激活、梯度和优化器状态存放在哪里，以及它们何时计算和如何跨设备汇总。</p>\n<p>本节的过关标准可以用一个小型计算图验证。设：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"a=w^(2)\"><span>a=w<sup>2</sup></span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"b=w\"><span>b=w</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"s=a+b\"><span>s=a+b</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"e=s-y\"><span>e=s-y</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"L=1/2e^(2)\"><span>L=1/2e<sup>2</sup></span></div>\n<p>前向传播依次计算 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"a\">a</span>、<span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span>、<span class=\"dd-inline-math\" role=\"math\" aria-label=\"s\">s</span>、<span class=\"dd-inline-math\" role=\"math\" aria-label=\"e\">e</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"L\">L</span>，并保存反向需要的中间值。</p>\n<p>反向从：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ L=1\"><span>∂ L/∂ L=1</span></div>\n<p>开始。经过半平方节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ e=e\"><span>∂ L/∂ e=e</span></div>\n<p>经过减法节点：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ s=e\"><span>∂ L/∂ s=e</span></div>\n<p>加法节点把上游梯度分别送给 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"a\">a</span> 和 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"b\">b</span>：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ a=e\"><span>∂ L/∂ a=e</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ b=e\"><span>∂ L/∂ b=e</span></div>\n<p>平方路径传回 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的贡献为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"( ∂ L/∂ w )_(平方) = e·2w\"><span>( ∂ L/∂ w )<sub>平方</sub> = e·2w</span></div>\n<p>直接路径传回 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 的贡献为：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"( ∂ L/∂ w )_(直接) = e·1\"><span>( ∂ L/∂ w )<sub>直接</sub> = e·1</span></div>\n<p>两条路径在共享参数 <span class=\"dd-inline-math\" role=\"math\" aria-label=\"w\">w</span> 处相加：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"∂ L/∂ w = 2we+e = e(2w+1)\"><span>∂ L/∂ w = 2we+e = e(2w+1)</span></div>\n<p>这个例子同时检查三项核心能力：</p>\n<ul><li>能把复合函数拆成局部计算图；</li><li>能从损失种子 1 开始逐节点反传；</li><li>能在共享变量处正确累加分叉路径的梯度。</li></ul>\n<p>最后，还需要清楚说明训练中的三方分工：</p>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"【损失函数定义优化目标】\"><span>【损失函数定义优化目标】</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"【反向传播计算目标对参数的梯度】\"><span>【反向传播计算目标对参数的梯度】</span></div>\n<div class=\"dd-equation\" role=\"math\" aria-label=\"【优化器根据梯度和自身状态更新参数】\"><span>【优化器根据梯度和自身状态更新参数】</span></div>\n<p>损失函数回答“什么结果更好”，反向传播回答“当前参数怎样影响这个目标”，优化器回答“根据这些梯度实际迈多大一步”。</p>\n<p>真正理解反向传播，不只是能够写出链式法则，还要知道它从什么目标出发、如何处理深层路径和分叉结构、结果怎样交给优化器，以及大规模训练系统如何为同一套数学规则安排计算、通信和内存。</p></div>\n<table class=\"dd-table\"><thead><tr><th>方向</th><th>接下来读</th><th>关键问题</th></tr></thead><tbody><tr><td>反向的起点</td><td><a href=\"#\" data-node=\"loss-function\">损失函数</a></td><td>我们究竟在对什么标量求导？</td></tr><tr><td>梯度怎样被使用</td><td><a href=\"#\" data-node=\"gradient-descent\">梯度下降</a></td><td>方向算对后，步长和噪声怎样影响更新？</td></tr><tr><td>深层路径为何变弱</td><td><a href=\"#\" data-node=\"vanishing-gradient\">梯度消失</a></td><td>连续雅可比相乘为何导致指数缩放？</td></tr><tr><td>结构怎样帮助反传</td><td><a href=\"#\" data-node=\"residual-connection\">残差连接</a></td><td>恒等捷径怎样提供更短梯度路径？</td></tr><tr><td>内存如何权衡</td><td><a href=\"#\" data-node=\"distributed-training\">分布式训练</a></td><td>激活、梯度和优化器状态怎样分片或重算？</td></tr></tbody></table>\n</section>\n<div class=\"dd-src\"><b>资料来源与改编说明</b><ul><li><a href=\"https://arxiv.org/abs/1502.05767\" target=\"_blank\" rel=\"noopener\">Automatic Differentiation in Machine Learning: a Survey</a>：前向/反向模式自动微分与复杂度。</li><li><a href=\"https://www.deeplearningbook.org/contents/mlp.html\" target=\"_blank\" rel=\"noopener\">Deep Learning — Deep Feedforward Networks</a>：计算图与反向传播。</li><li><a href=\"https://pytorch.org/docs/stable/notes/autograd.html\" target=\"_blank\" rel=\"noopener\">PyTorch Autograd Mechanics</a>：实际自动微分图、保存张量与梯度语义。</li><li><a href=\"https://arxiv.org/abs/1604.06174\" target=\"_blank\" rel=\"noopener\">Training Deep Nets with Sublinear Memory Cost</a>：激活重计算的内存—算力权衡。</li></ul><p>计算图、手算、表格和调试流程均为本项目原创组织。</p><div class=\"dd-src-date\">访问日期：2026-07-22</div></div>","quality":{"contractVersion":2,"examples":[{"section":4,"evidence":{"setup":"把前向拆成","rule":"从损失 1 开始倒着走","steps":"经过乘法到 w","interpretation":"dL/dL=1"}},{"section":9,"evidence":{"setup":"在 w=2 处取 ε=10⁻⁴","rule":"用中心差分而非单边差分","steps":"同时试几个数量级的 ε","interpretation":"应非常接近"}}],"formulas":[{"id":"backprop-central-difference","section":1,"formulaIndex":1,"symbols":[{"name":"L","meaning":"参数扰动后重新计算的标量损失","evidence":"所有参数最终汇入同一个标量损失"},{"name":"θ","meaning":"被检查导数的第 i 个模型参数","evidence":"中心差分每个参数"},{"name":"i","meaning":"待检查参数的编号","evidence":"每个参数至少要做两次前向"},{"name":"ε","meaning":"有限差分施加的微小参数扰动","evidence":"ε 太大会有截断误差"}]},{"id":"backprop-local-chain-rule","section":3,"formulaIndex":1,"symbols":[{"name":"L","meaning":"最终要对输入求敏感度的损失","evidence":"最终损失变多少"},{"name":"u","meaning":"当前局部节点的输入","evidence":"输入 u 变化一点"},{"name":"z","meaning":"局部函数 f 产生的节点输出","evidence":"输出 z 变化一点"}]},{"id":"backprop-branch-sum","section":5,"formulaIndex":1,"symbols":[{"name":"u","meaning":"由共享参数两条路径共同产生的输出","evidence":"设 u=w²+w"},{"name":"w","meaning":"同时进入平方和直接加法路径的共享参数","evidence":"参数 w 一条路径经过平方"}]},{"id":"backprop-numerical-check","section":9,"formulaIndex":1,"symbols":[{"name":"g","meaning":"由中心差分算出的数值梯度","evidence":"将数值梯度与反向梯度比较"},{"name":"num","meaning":"下标表示该梯度来自 numerical 数值近似","evidence":"数值梯度"},{"name":"L","meaning":"在扰动后参数位置计算的损失函数","evidence":"将数值梯度与反向梯度比较"},{"name":"ε","meaning":"中心差分的参数扰动步长","evidence":"ε=10⁻⁴"}]}],"sectionContracts":[{"section":1,"definition":{"answer":"有限差分不手写导数公式，只用微小扰动引起的损失变化去近似梯度。","evidence":"不手写任何导数公式，只把第 i 个参数向两侧各推一个微小扰动，用损失的变化去近似梯度"},"problem":{"answer":"它用于解决不手写导数也能估计梯度这一问题。","evidence":"这条路走的是纯数值路线：不手写任何导数公式"},"inputOutput":{"answer":"输入是参数当前取值和步长 ε，输出是该方向上损失的近似斜率。","evidence":"喂进去的只有某个参数当前的取值和一个人为选定的步长 ε，交回来的是损失在这个参数方向上的近似斜率"},"mechanism":{"answer":"先在参数两侧各跑一次前向，再让两个损失之差被 2ε 除。","evidence":"先在参数两侧各跑一次完整前向拿到两个损失，再让两者之差被 2ε 除"},"interpretation":{"answer":"差商越贴近真实导数，说明步长选得越合适。","evidence":"差商越贴近真实导数，说明步长选得越合适"},"boundary":{"answer":"但它只能抽查少量参数，扛不起训练规模，步长过大过小都会失真。","evidence":"抽查少量参数可以，承担训练规模不行"}},{"section":2,"definition":{"answer":"计算图把函数记成有向无环图，节点是局部运算，边是数据依赖。","evidence":"让整个函数变成一张有向无环图：每个节点是一次局部运算，每条边是一次数据依赖"},"problem":{"answer":"它用于解决不必一次推导整条巨大公式的问题。","evidence":"办法是干脆不去推那条整式。框架在前向执行时把每一步运算随手登记下来"},"inputOutput":{"answer":"输入是前向的连接关系与必要中间值，输出是可按链式法则拼接的梯度通路。","evidence":"这张图吃进去的是前向留下的两样东西：运算之间的连接关系，以及乘法节点上的 x、损失节点上的 ŷ−y 这类必要中间值；它交付的是一条能按链式法则逐段拼接的梯度通路"},"mechanism":{"answer":"各类运算只实现自己那一小块局部导数，梯度由局部规则拼接。","evidence":"加法、乘法、矩阵乘、激活函数各自只实现自己那一小块局部导数"},"interpretation":{"answer":"每个方框只表示一次局部运算，不是对整条公式的整体求导。","evidence":"图里每个方框只表示一次局部运算，没有任何一处需要对整条公式做一次性求导"},"boundary":{"answer":"但只有前向保存了中间值，反向才走得通。","evidence":"反向能不能走通，完全取决于前向有没有把它要用的中间值保存下来"}},{"section":3,"definition":{"answer":"这一步刻画敏感度如何在单个节点上被接力传下去。","evidence":"把镜头推近到单个节点，就能看清敏感度是怎样一站站接力传下去的"},"problem":{"answer":"它用于解决整张图的梯度怎样逐节点组合起来的问题。","evidence":"整张图的梯度都由这一个动作逐节点组合而成"},"inputOutput":{"answer":"输入是上游梯度与本地局部导数，输出是对本节点输入的梯度。","evidence":"这个节点拿到的是下游送来的上游梯度，用到的是自己那条局部导数，传出去的是对本节点输入的梯度"},"mechanism":{"answer":"先接住上游梯度，再乘上本地的局部导数。","evidence":"动作只有两步：先接住上游梯度，再乘上本地的局部导数"},"interpretation":{"answer":"乘积回答的是把输入挪动一点点最终损失会变多少。","evidence":"这个乘积回答的就是：把 u 挪动一点点，最终损失会跟着变多少"},"boundary":{"answer":"但它依赖局部导数存在且被正确实现，折点与写错的算子都会失效。","evidence":"它依赖局部导数存在且被正确实现"}},{"section":4,"definition":{"answer":"这次手算让损失种子沿同一串节点倒流回每个变量。","evidence":"再让损失种子沿同一串节点倒流回 w 和 x"},"problem":{"answer":"它用于解决 −4 这个梯度到底怎样一步步出现的问题。","evidence":"却没交代这个数是怎么冒出来的；这一节把它一步步摆开"},"inputOutput":{"answer":"输入是前向中间值与损失种子，输出是每个变量各自的梯度。","evidence":"上半段是反向的原料，u=4、e=−2 这些前向值连同 1 这个损失种子必须先到位；下半段是产物，每个变量最后各自拿到一份属于自己的梯度"},"mechanism":{"answer":"损失种子每经过一个节点就被该节点的局部导数缩放一次。","evidence":"损失种子每经过一个节点就被对应的局部导数乘一次——过平方乘 e 得 −2，过减法乘 1 保持不变，过乘法到 w 时乘 x=2"},"interpretation":{"answer":"负号表示把该参数稍微增大损失就会下降。","evidence":"最后一行那个负号可以直接读出意思：把 w 稍微增大一点，损失就会下降"},"boundary":{"answer":"但整张表只对当前这一组取值成立。","evidence":"整张表只对 x=2、y=6、w=2 这一组取值成立"}},{"section":5,"definition":{"answer":"分叉累加就是多条下游路径把各自贡献送回同一个变量节点。","evidence":"反向遍历真正做的事就藏在这个加号里：两条下游路径各自把 2w 和 1 送回 w 节点"},"problem":{"answer":"它用于解决只沿一条路径反传会漏掉贡献的问题。","evidence":"若只沿平方那条路反传，得到的 2w 会静悄悄比真值小一截"},"inputOutput":{"answer":"输入是各路径送回的梯度，输出是相加之后的总梯度。","evidence":"w 节点收下的不是其中任何一条，而是它们相加之后的总梯度"},"mechanism":{"answer":"先分别算出每条路径的贡献，再在该变量节点相加。","evidence":"两条路径都会把自己的影响带到 u 上，于是总导数是两份贡献之和"},"interpretation":{"answer":"相加表示这份梯度包含了全部下游影响。","evidence":"相加意味着这份梯度包含了全部下游影响"},"boundary":{"answer":"但图内分叉求和不是跨批次梯度累积。","evidence":"图内分叉是总导数必需的路径求和；跨 mini-batch 的梯度累积是为了模拟更大有效批量"}},{"section":6,"definition":{"answer":"本层交给上游的是向量与雅可比的乘积 vᵀJ，而非雅可比本身。","evidence":"本层要交给上游的是 vᵀJ，即向量—雅可比积，其中 J=∂y/∂x 才是那块完整雅可比"},"problem":{"answer":"它用于解决大张量层的雅可比过大无法存下的问题。","evidence":"中间那块动辄上亿元素的方阵从头到尾没在显存里出现过"},"inputOutput":{"answer":"输入是与该层输出同形的梯度向量，输出是与输入同形的梯度。","evidence":"接收的是与该层输出同形的梯度向量，交回的是与该层输入同形的梯度"},"mechanism":{"answer":"每种层用自己的专门规则直接算出这个乘积。","evidence":"矩阵乘用一次转置乘法完成，卷积用相关运算完成，注意力也有自己的融合规则"},"interpretation":{"answer":"参数极多而损失只有一个标量，说明反向模式尤其合适。","evidence":"神经网络训练正是参数极多、损失只有一个标量，一次反向就能把所有参数的梯度带回来，因此反向模式尤其合适"},"boundary":{"answer":"但少输入多输出时前向模式反而更省。","evidence":"若输入只有寥寥几个却要观察大量输出的变化，前向模式反而更省"}},{"section":7,"definition":{"answer":"激活缓存就是训练比推理多扣住的那部分前向中间值。","evidence":"训练比推理多出来的那部分占用，第一笔就是这些被扣住的激活"},"problem":{"answer":"它用于解决梯度检查点如何在内存与算力之间权衡的问题。","evidence":"梯度检查点给出另一种权衡：前向只保存少数几个检查点激活，其余的不存"},"inputOutput":{"answer":"占住显存的是激活、参数梯度和优化器状态三类。","evidence":"把训练时占住显存的东西点一遍，大致是三类：前向产生的激活、反向算出的参数梯度、优化器自己维护的状态"},"mechanism":{"answer":"前向保存激活并一路留到反向取用，推理则可随算随丢。","evidence":"推理算完一层就能把上一层的激活丢掉，训练却必须把它们一路留到反向来取"},"interpretation":{"answer":"显存远高于推理表示大量中间值正被保留待用。","evidence":"不是模型忽然变大了，而是大量中间值正被保留待用"},"boundary":{"answer":"但重算换内存要求过程可重复，随机数和原地操作处理不当会出错。","evidence":"代价是重算过程必须可重复——dropout 要能复现同一个掩码，原地操作和带副作用的算子要格外小心"}},{"section":8,"definition":{"answer":"转普通数组、原地覆盖、错误广播都会把计算图切断或污染。","evidence":"张量被转成普通数组、被原地覆盖、被以合法却错误的形状广播，图就在某处被切断或污染"},"problem":{"answer":"它用于解决公式正确梯度却仍然出错的问题。","evidence":"出错的不是数学，而是代码对计算图做了手脚"},"inputOutput":{"answer":"输入是实际张量操作与梯度检查信号，输出是对故障类型的定位。","evidence":"能拿来判断的只有两样：代码里实际执行的张量操作，和梯度检查给出的信号——是 None、是恒零、是 NaN，还是数值系统性偏小；最终要得到的，是把现象定位到某一类具体故障上"},"mechanism":{"answer":"先看图是否被切断，再依次查原地修改、未清零、溢出和广播。","evidence":"排查顺序建议由粗到细：先确认图是不是干脆被切断了，再依次看原地修改、梯度未清零、精度溢出和错误广播"},"interpretation":{"answer":"命中一行只表示排查范围缩小，不等于唯一根因。","evidence":"对上表里的某一行，只意味着排查范围缩小了，并不等于找到了唯一根因"},"boundary":{"answer":"但多个故障可能同时存在，修好一个现象仍在。","evidence":"混合精度下溢和忘记清零完全可能同时存在，修好一个之后现象照旧"}},{"section":9,"definition":{"answer":"梯度检查用有限差分为自动梯度提供一份独立证据。","evidence":"有限差分虽然扛不起全部参数的梯度，却能对少数几个参数给出一份与自动微分完全独立的证据"},"problem":{"answer":"它用于解决怎样确认反向实现没写错的问题。","evidence":"反向传播很快，但怎样获得一份独立证据证明实现没有写错？"},"inputOutput":{"answer":"输入是参数两侧的两个损失，输出是数值梯度及相对误差。","evidence":"喂给它的只是同一个前向在参数两侧算出的两个损失，吐出来的是数值梯度"},"mechanism":{"answer":"用中心差分算出数值梯度，再与反向梯度比较相对误差。","evidence":"随后将数值梯度与反向梯度比较相对误差"},"interpretation":{"answer":"相对误差小到只剩浮点噪声，说明实现大概率正确。","evidence":"相对误差小到只剩浮点噪声，就说明反向实现大概率正确"},"boundary":{"answer":"但折点、随机未固定或步长不当都会让检查误报，它只是抽查。","evidence":"若 ReLU 恰在折点、随机层未固定、ε 不合适或损失尺度极端，检查本身就会误报"}}]},"publication":{"schemaVersion":1,"status":"published-provisional","reviewStatus":"manual-review","label":"未通过审计 · 暂行版本","blockerCount":1,"candidateHash":"sha256:02efe9e242d3055adffcfd328ff17a8fdb730562570bae1ed82845c32ccc3d66","publishedAt":"2026-08-13T13:01:28.386Z","reason":"用户已授权 2.1 使用红色暂行审核页；公式显示问题返修并通过定向复核后重新发布"},"websitePublication":{"publicAccess":true,"reviewStatus":"pending-review"}};